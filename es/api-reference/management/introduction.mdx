---
title: "API de administración"
description: "Usa un token de administración para gestionar las claves API de la organización y consultar el uso y la facturación por clave."
---
## Resumen

La Management API te permite gestionar API Keys de organización y consultar uso y facturación por clave sin usar una API Key de inferencia estándar.

Usa un token de gestión desde la página **Settings** del Dashboard:

<Warning>
Los management tokens son distintos de las API Keys de inferencia. Usa `mt-...` para `/v1/management/*` y `sk-...` para endpoints de inferencia como `/v1/responses`.
</Warning>

## Endpoints disponibles

| Endpoint | Method | Descripción |
|----------|--------|-------------|
| /v1/management/api-keys | GET | List user-managed API keys in the current organization |
| /v1/management/api-keys | POST | Create a new user API key |
| /v1/management/api-keys/{keyId} | PATCH | Update name, usage limit, allowed models, expiry, or status |
| /v1/management/api-keys/{keyId}/usage | GET | Retrieve paginated usage details for a specific key |
| /v1/management/api-keys/{keyId}/billing | GET | Retrieve aggregated billing breakdowns for a specific key |

## Contrato de filtros de uso

`GET /v1/management/api-keys/{keyId}/usage` admite los siguientes parámetros de consulta:

| Parámetro | Tipo | Valores por defecto / límites | Notas |
|-----------|------|------------------|-------|
| page | integer | por defecto 1, mín. 1 | Número de página desde 1 |
| limit | integer | por defecto 50, mín. 1, máx. 100 | Tamaño de página |
| logicalModel | string | longitud máxima 100 | Nombre del modelo lógico solicitado |
| modelVendor | string | longitud máxima 100 | Proveedor público del modelo |
| scene | enum | - | chat, image, audio, video, embedding, rerank, translation, music, 3d |
| accessChannel | enum | - | platform o byok |
| startDate | string | - | Límite inferior inclusivo; acepta RFC3339 con zona horaria o YYYY-MM-DD |
| endDate | string | - | Límite superior inclusivo; acepta RFC3339 con zona horaria o YYYY-MM-DD |

Si se envían `startDate` y `endDate`, entonces `startDate` debe ser menor o igual que `endDate`.

## Contrato del cuerpo de API Key

### POST /v1/management/api-keys

| Campo | Tipo | Valores por defecto / límites | Notas |
|-------|------|------------------|-------|
| name | string | obligatorio, valor por defecto Default Key, longitud 1-50 | Nombre visible, recortado del lado del servidor |
| limitAmount | number \| null | mín. 0, máx. 100000 | null u omitido = ilimitado, 0 = cuota cero, valor positivo = tope en USD |
| models | string[] | por defecto [] | Lista opcional de modelos lógicos permitidos |
| expiresAt | string \| null | fecha y hora RFC3339 | null significa sin expiración |

### PATCH /v1/management/api-keys/{keyId}

| Campo | Tipo | Valores por defecto / límites | Notas |
|-------|------|------------------|-------|
| status | enum | - | active, inactive, revoked |
| name | string | longitud 1-50 | Nombre visible actualizado |
| limitAmount | number \| null | mín. 0, máx. 100000 | null significa ilimitado, 0 significa cuota cero |
| models | string[] | - | Lista actualizada de modelos lógicos permitidos |
| expiresAt | string \| null | fecha y hora RFC3339 | null elimina la expiración |

Debes enviar al menos un campo en la solicitud PATCH.

## Semántica de reportes

- logicalModel se refiere al modelo lógico público solicitado por el cliente.
- modelVendor se refiere al proveedor público del modelo, no a la ruta física oculta.
- scene es la escena pública de la solicitud derivada del endpoint o del tipo de tarea.
- accessChannel=platform significa que la solicitud se facturó a través del canal de plataforma de LemonData.
- accessChannel=byok significa que la solicitud usó tu propia clave del proveedor upstream.

Las respuestas solo exponen campos públicos de facturación y reporting; los detalles internos de routing y la metadata física permanecen ocultos.

## Nota sobre la paginación de billing

`/usage` está paginado. `/billing` es actualmente un endpoint agregado y no devuelve metadata de paginación tipo `page` / `limit`.

## Ejemplo rápido

Empieza listando las API Keys disponibles para el token de gestión actual:

<RequestExample>
```bash cURL
curl "https://api.lemondata.cc/v1/management/api-keys" \
  -H "Authorization: Bearer mt-your-management-token"
```
</RequestExample>

<ResponseExample>
```json Response (200)
{
  "object": "list",
  "data": [
    {
      "id": "key_abc123def456",
      "name": "Backend Worker",
      "key_prefix": "sk-abc123...",
      "status": "active",
      "limit_amount": 500.0,
      "used_amount": 148.25,
      "models": ["gpt-4o-mini", "claude-3-7-sonnet"],
      "expires_at": "2026-04-30T00:00:00.000Z",
      "last_used_at": "2026-03-27T08:12:45.000Z",
      "created_at": "2026-03-01T10:00:00.000Z"
    }
  ]
}
```
</ResponseExample>

## Siguientes pasos

- [List API Keys](/api-reference/management/list-api-keys)
- [Create API Key](/api-reference/management/create-api-key)
- [Update API Key](/api-reference/management/update-api-key)
- [Get API Key Usage](/api-reference/management/get-api-key-usage)
- [Get API Key Billing](/api-reference/management/get-api-key-billing)
