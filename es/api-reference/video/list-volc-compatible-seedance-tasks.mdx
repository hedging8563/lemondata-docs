---
title: "Listar tareas Seedance (compatible con Volc)"
openapi: "GET /api/v3/contents/generations/tasks"
description: "Lista tareas Seedance compatibles recientes de la organización actual."
---

## Resumen

Estos endpoints son para equipos que ya usan la forma de tarea Seedance estilo Volc. La autenticación usa tu API key Bearer de TokenLab. El ID devuelto empieza con `cgt-`; consulta el estado hasta que `status` sea `succeeded`, `failed`, `cancelled` o `expired`.

Ver también [Modelos de video Seedance 2.0](/es/guides/seedance-2-video) y [Generación de video](/es/guides/video-generation).

## Forma de respuesta

La respuesta de lista devuelve `items` y `total`. Se admiten filtros `filter.status`, `filter.task_ids`, `filter.model` y `filter.service_tier`, con `page_num` y `page_size` para paginación.

## Ejemplo

```bash
curl "https://api.tokenlab.sh/api/v3/contents/generations/tasks?page_num=1&page_size=20&filter.status=succeeded" \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```
