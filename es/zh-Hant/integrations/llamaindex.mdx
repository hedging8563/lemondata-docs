---
title: "LlamaIndex"
description: "Integra LemonData con LlamaIndex para aplicaciones RAG"
---

## Descripción general

LlamaIndex es un framework de datos para aplicaciones de LLM, especialmente potente para construir sistemas RAG (Retrieval Augmented Generation). LemonData funciona a la perfección con la integración de OpenAI de LlamaIndex.

## Instalación

```bash
pip install llama-index llama-index-llms-openai llama-index-embeddings-openai
```

## Configuración básica

```python
from llama_index.llms.openai import OpenAI
from llama_index.core import Settings

# Configure LLM
llm = OpenAI(
    model="gpt-4o",
    api_key="sk-your-lemondata-key",
    api_base="https://api.lemondata.cc/v1"
)

# Set as default
Settings.llm = llm

# Simple query
response = llm.complete("What is LemonData?")
print(response.text)
```

## Uso de diferentes modelos

```python
# OpenAI GPT-4o
gpt4 = OpenAI(
    model="gpt-4o",
    api_key="sk-your-key",
    api_base="https://api.lemondata.cc/v1"
)

# Anthropic Claude (via OpenAI-compatible endpoint)
claude = OpenAI(
    model="claude-sonnet-4-5",
    api_key="sk-your-key",
    api_base="https://api.lemondata.cc/v1"
)

# Google Gemini
gemini = OpenAI(
    model="gemini-2.5-flash",
    api_key="sk-your-key",
    api_base="https://api.lemondata.cc/v1"
)
```

## Interfaz de chat

```python
from llama_index.core.llms import ChatMessage

messages = [
    ChatMessage(role="system", content="You are a helpful assistant."),
    ChatMessage(role="user", content="What is the capital of France?")
]

response = llm.chat(messages)
print(response.message.content)
```

## Streaming

```python
# Streaming completion
for chunk in llm.stream_complete("Write a poem about AI"):
    print(chunk.delta, end="", flush=True)

# Streaming chat
for chunk in llm.stream_chat(messages):
    print(chunk.delta, end="", flush=True)
```

## Embeddings

```python
from llama_index.embeddings.openai import OpenAIEmbedding

embed_model = OpenAIEmbedding(
    model="text-embedding-3-small",
    api_key="sk-your-lemondata-key",
    api_base="https://api.lemondata.cc/v1"
)

# Set as default
Settings.embed_model = embed_model

# Get embeddings
embeddings = embed_model.get_text_embedding("Hello, world!")
print(f"Embedding dimension: {len(embeddings)}")
```

## RAG con documentos

```python
from llama_index.core import VectorStoreIndex, SimpleDirectoryReader

# Configure settings
Settings.llm = llm
Settings.embed_model = embed_model

# Load documents
documents = SimpleDirectoryReader("./data").load_data()

# Create index
index = VectorStoreIndex.from_documents(documents)

# Query
query_engine = index.as_query_engine()
response = query_engine.query("What is in my documents?")
print(response)
```

## Motor de chat

```python
# Create chat engine with memory
chat_engine = index.as_chat_engine(chat_mode="condense_question")

# Multi-turn conversation
response = chat_engine.chat("What is LemonData?")
print(response)

response = chat_engine.chat("How many models does it support?")
print(response)
```

## Uso asíncrono

```python
import asyncio

async def main():
    response = await llm.acomplete("Hello!")
    print(response.text)

asyncio.run(main())
```

## Variables de entorno

Para que el código sea más limpio, utiliza variables de entorno:

```bash
export OPENAI_API_KEY="sk-your-lemondata-key"
export OPENAI_API_BASE="https://api.lemondata.cc/v1"
```

```python
from llama_index.llms.openai import OpenAI

# Will automatically use environment variables
llm = OpenAI(model="gpt-4o")
```

## Mejores prácticas

<AccordionGroup>
  <Accordion title="Elegir el modelo adecuado">
    Utiliza modelos más rápidos (GPT-4o-mini) para tareas de Embedding y resúmenes, y reserva los modelos potentes (GPT-4o, Claude) para la respuesta final.
  </Accordion>

  <Accordion title="Optimizar el tamaño del fragmento">
    Ajusta el tamaño del fragmento (`chunk size`) según el tipo de documento. Utiliza fragmentos más pequeños para documentos técnicos densos y fragmentos más grandes para contenido narrativo.
  </Accordion>

  <Accordion title="Usar caché">
    Habilita la función de caché de LlamaIndex para evitar llamadas a la API innecesarias durante el desarrollo.
  </Accordion>
</AccordionGroup>