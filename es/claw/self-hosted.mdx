---
title: "Instalación autoalojada"
description: "Ejecuta LemonClaw en tu propio servidor con LemonData AI API — un comando, sin Docker"
---

<Note>
**Autoalojado** significa que ejecutas LemonClaw directamente en tu propio VPS o máquina. Solo pagas por el uso de modelos AI a través de tu saldo de LemonData — sin costos de instancia.
</Note>

## Instalación rápida

<CodeGroup>
```bash Linux / macOS
curl -fsSL https://raw.githubusercontent.com/hedging8563/lemonclaw/main/deploy/self-hosted/install.sh | bash
```

```bash No interactivo
LEMONDATA_API_KEY=sk-xxx curl -fsSL https://raw.githubusercontent.com/hedging8563/lemonclaw/main/deploy/self-hosted/install.sh | bash
```
</CodeGroup>

El instalador te pedirá tu **LemonData API Key**. Obtén una desde tu [Dashboard](https://lemondata.cc/dashboard).

## Qué se instala

<Steps>
  <Step title="Node.js 22+">
    Se instala automáticamente si no está presente (vía NodeSource en Linux, Homebrew en macOS)
  </Step>
  <Step title="LemonClaw">
    Paquete oficial de LemonClaw desde npm, preconfigurado con LemonData API
  </Step>
  <Step title="15 modelos AI">
    Claude, GPT-5, Gemini, DeepSeek y más — listos para usar
  </Step>
  <Step title="Plugins de IM">
    Plugins de DingTalk, WeCom y QQ preinstalados
  </Step>
  <Step title="Servicio del sistema">
    Se inicia automáticamente al arrancar (systemd en Linux, launchd en macOS)
  </Step>
</Steps>

## Requisitos

| Requisito | Detalles |
|-----------|----------|
| **Sistema operativo** | Linux (Ubuntu/Debian/CentOS/RHEL) o macOS |
| **Arquitectura** | x64 o arm64 |
| **RAM** | 1 GB mínimo, 2 GB recomendado |
| **API Key** | [Obtener aquí](https://lemondata.cc) |

## Después de la instalación

El instalador muestra tus credenciales de acceso:

```
✓ LemonClaw installed successfully!

  WebUI:        http://your-ip:18789
  Token:        your-gateway-token
  Auto-login:   http://your-ip:18789?token=your-gateway-token
  Model:        claude-sonnet-4-6
```

<Warning>
**¡Guarda tu token!** Lo necesitas para acceder a la WebUI y conectar plataformas de mensajería.
</Warning>

## Conectar plataformas de mensajería

Accede a la WebUI en `http://your-ip:18789` con tu token, luego sigue las guías específicas de cada plataforma:

<CardGroup cols={2}>
  <Card title="Telegram" icon="paper-plane" href="/claw/telegram" />
  <Card title="Discord" icon="discord" href="/claw/discord" />
  <Card title="WhatsApp" icon="whatsapp" href="/claw/whatsapp" />
  <Card title="Slack" icon="slack" href="/claw/slack" />
  <Card title="DingTalk" icon="bell" href="/claw/dingtalk" />
  <Card title="WeCom" icon="building" href="/claw/wecom" />
</CardGroup>

## Comandos de gestión

<Tabs>
  <Tab title="Linux">
    ```bash
    systemctl --user status lemonclaw    # Verificar estado
    journalctl --user -u lemonclaw -f    # Ver logs
    systemctl --user restart lemonclaw   # Reiniciar
    systemctl --user stop lemonclaw      # Detener
    ```
  </Tab>
  <Tab title="macOS">
    ```bash
    tail -f ~/.lemonclaw/lemonclaw.log    # Ver logs
    launchctl stop cc.lemondata.lemonclaw && launchctl start cc.lemondata.lemonclaw  # Reiniciar
    launchctl stop cc.lemondata.lemonclaw  # Detener
    ```
  </Tab>
</Tabs>

## Configuración

Ubicación del archivo de configuración: `~/.lemonclaw/config.json`

Después de editar, reinicia el servicio para aplicar los cambios.

### Cambiar el modelo predeterminado

```json
{
  "agents": {
    "defaults": {
      "model": {
        "primary": "lemondata-claude/claude-opus-4-6"
      }
    }
  }
}
```

Prefijos de modelos disponibles:
- `lemondata-claude/` — Modelos Claude (anthropic-messages API)
- `lemondata-minimax/` — Modelos MiniMax (anthropic-messages API)
- `lemondata/` — Todos los demás modelos (openai-completions API)

## Actualización

```bash
curl -fsSL https://raw.githubusercontent.com/hedging8563/lemonclaw/main/deploy/self-hosted/install.sh | bash
# Luego reiniciar el servicio
systemctl --user restart lemonclaw  # Linux
# o
launchctl stop cc.lemondata.lemonclaw && launchctl start cc.lemondata.lemonclaw  # macOS
```

## Desinstalación

```bash
# Detener y deshabilitar el servicio
systemctl --user stop lemonclaw && systemctl --user disable lemonclaw  # Linux
launchctl unload ~/Library/LaunchAgents/cc.lemondata.lemonclaw.plist  # macOS

# Eliminar LemonClaw
rm -rf ~/.lemonclaw
rm -rf ~/.lemonclaw
```

## Autoalojado vs Gestionado

| | Autoalojado | Gestionado (Claw) |
|---|---|---|
| **Costo de instancia** | Gratis | $20 / 30 días |
| **Uso de AI** | Pago por uso | Pago por uso |
| **Infraestructura** | Tu servidor | Nosotros gestionamos todo |
| **Actualizaciones** | Manuales (`npm update`) | Automáticas |
| **Soporte** | Comunidad | Prioritario |

<Info>
**Código fuente**: [github.com/hedging8563/lemonclaw](https://github.com/hedging8563/lemonclaw)
</Info>
