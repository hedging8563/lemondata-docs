---
title: "Créer une clé API"
openapi: "POST /v1/management/api-keys"
description: "Crée une nouvelle clé API utilisateur pour l'organisation courante."
---
## Vue d’ensemble

Cet endpoint crée une nouvelle API Key de type `type=user` dans l’organisation associée au management token courant.

## Corps de la requête

| Champ | Type | Valeurs par défaut / limites | Notes |
|-------|------|------------------|-------|
| name | string | required, default Default Key, length 1-50 | Display name, trimmed server-side |
| limitAmount | number \| null | min 0, max 100000 | null or omitted = unlimited, 0 = zero quota, positive = USD spending cap |
| models | string[] | default [] | Optional logical model allowlist |
| expiresAt | string \| null | RFC3339 datetime | null means no expiry |

## Notes

- La valeur complète de l’API Key n’est affichée qu’une seule fois dans la réponse.
- `models: []` signifie qu’aucune restriction de modèle supplémentaire n’est appliquée au-delà de la politique d’organisation.
- `limitAmount: 0` crée une clé valide mais sans quota exploitable tant qu’elle n’est pas mise à jour.

## Exemple

<RequestExample>
```bash cURL
curl -X POST "https://api.lemondata.cc/v1/management/api-keys" \
  -H "Authorization: Bearer mt-your-management-token" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Backend Worker",
    "limitAmount": 500,
    "models": ["veo3.1", "sora-2"],
    "expiresAt": "2026-12-31T23:59:59Z"
  }'
```
</RequestExample>

## Requête / Réponse

Utilisez le panneau OpenAPI interactif ci-dessus pour obtenir le schéma exact et les champs de réponse.
