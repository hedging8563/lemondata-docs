---
title: "Supprimer un actif matériel"
openapi: "DELETE /v1/videos/assets/{assetId}"
description: "Supprime un actif matériel Seedance appartenant à l'organisation actuelle."
---

Supprime un actif matériel. Cela ne supprime pas le groupe de matériel contenant. Après la suppression, l'ID de l'actif ne peut plus être utilisé en tant que `material_asset_id`.

## Exemple de requête

```bash
curl https://api.tokenlab.sh/v1/videos/assets/seedance_asset_123 \
  -X DELETE \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```

## Exemple de réponse

```json
{
  "deleted": true,
  "id": "seedance_asset_123"
}
```