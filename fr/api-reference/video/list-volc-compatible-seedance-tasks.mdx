---
title: "Lister les tâches Seedance (compatible Volc)"
openapi: "GET /api/v3/contents/generations/tasks"
description: "Liste les tâches Seedance compatibles récentes de l’organisation actuelle."
---

## Aperçu

Ces points de terminaison s’adressent aux équipes qui utilisent déjà la structure de tâche Seedance de style Volc. L’authentification utilise votre clé API TokenLab Bearer. L’ID de tâche retourné commence par `cgt-`; interrogez le statut jusqu’à ce que `status` devienne `succeeded`, `failed`, `cancelled` ou `expired`.

Voir aussi [Modèles vidéo Seedance 2.0](/fr/guides/seedance-2-video) et [Génération vidéo](/fr/guides/video-generation).

## Format de réponse

La réponse de liste contient `items` et `total`. Les filtres pris en charge sont `filter.status`, `filter.task_ids`, `filter.model` et `filter.service_tier`, avec `page_num` et `page_size` pour la pagination.

## Exemple

```bash
curl "https://api.tokenlab.sh/api/v3/contents/generations/tasks?page_num=1&page_size=20&filter.status=succeeded" \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```
