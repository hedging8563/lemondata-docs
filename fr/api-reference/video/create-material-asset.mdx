---
title: "Créer un actif matériel"
openapi: "POST /v1/videos/assets"
description: "Téléverse un actif matériel Seedance dans un groupe de matériel TokenLab."
---

Importez une URL source accessible publiquement dans un groupe de matériel Seedance. TokenLab stocke le matériel pour une utilisation ultérieure ; utilisez l' `id` de l'actif matériel renvoyé pour la génération, et non l'URL source.

Pour `library_type: "aigc_avatar"`, `group_id` est facultatif ; TokenLab utilise ou crée le groupe d'avatars virtuels par défaut de l'organisation. Pour `library_type: "liveness_face"`, `group_id` est obligatoire et doit correspondre au groupe TokenLab renvoyé par [Lier le résultat de la vérification de personne réelle](/fr/api-reference/video/bind-real-person-verification-result).

| Type | Entrée prise en charge |
|------|-----------------|
| Image | `jpeg`, `png`, `webp`, `bmp`, `tiff`, `gif`, `heic`, `heif` ; rapport d'aspect `(0.4, 2.5)` ; largeur et hauteur `(300, 6000)` px ; moins de 30 Mo. |
| Vidéo | `mp4`, `mov` ; `480p`, `720p` ou `1080p` ; 2-15 secondes ; rapport d'aspect `[0.4, 2.5]` ; largeur et hauteur `[300, 6000]` px ; nombre total de pixels compris entre 409600 et 2086876 ; jusqu'à 50 Mo ; 24-60 FPS. |
| Audio | `wav`, `mp3` ; 2-15 secondes ; jusqu'à 15 Mo. |

La création est asynchrone. Interrogez [Obtenir un actif matériel](/fr/api-reference/video/get-material-asset) jusqu'à ce que le `status` soit `ACTIVE`. Pour les groupes de personnes réelles, un groupe correspond à une personne vérifiée ; les téléversements contenant plusieurs visages ou un visage ne correspondant pas à la personne vérifiée peuvent échouer.

## Exemple de requête

```bash
curl https://api.tokenlab.sh/v1/videos/assets \
  -H "Authorization: Bearer $TOKENLAB_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"library_type":"aigc_avatar","group_id":"seedance_group_virtual_123","asset_url":"https://example.com/reference.png","asset_type":"Image","asset_name":"Front view"}'
```

## Exemple de réponse

```json
{
  "id": "seedance_asset_123",
  "object": "seedance.material_asset",
  "group_id": "seedance_group_virtual_123",
  "library_type": "aigc_avatar",
  "asset_name": "Front view",
  "asset_type": "Image",
  "status": "PROCESSING",
  "ready": false,
  "error_message": null,
  "metadata": null,
  "created_at": 1782998400000
}
```