---
title: "Obtenir une tâche (compatible Volc)"
openapi: "GET /api/v3/contents/generations/tasks/{id}"
description: "Récupère une tâche compatible Seedance par son ID cgt."
---

## Aperçu

Ces points de terminaison s’adressent aux équipes qui utilisent déjà la structure de tâche Seedance de style Volc. L’authentification utilise votre clé API TokenLab Bearer. L’ID de tâche retourné commence par `cgt-`; interrogez le statut jusqu’à ce que `status` devienne `succeeded`, `failed`, `cancelled` ou `expired`.

Voir aussi [Modèles vidéo Seedance 2.0](/fr/guides/seedance-2-video) et [Génération vidéo](/fr/guides/video-generation).

## Format de réponse

La réponse de tâche utilise `queued`, `running`, `succeeded`, `failed`, `cancelled` ou `expired`. Les tâches réussies retournent `content.video_url`; `content.last_frame_url`, les paramètres repris comme `return_last_frame` et `watermark`, `usage` et `progress` apparaissent lorsqu’ils sont disponibles. `error` vaut `null` sans échec.

## Exemple

```bash
curl https://api.tokenlab.sh/api/v3/contents/generations/tasks/cgt-0123456789abcdef0123456789abcdef \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```
