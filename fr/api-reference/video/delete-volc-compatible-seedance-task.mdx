---
title: "Annuler une tâche Seedance (compatible Volc)"
openapi: "DELETE /api/v3/contents/generations/tasks/{id}"
description: "Annule une tâche Seedance compatible en file d’attente lorsque l’annulation est encore possible."
---

## Aperçu

Ces points de terminaison s’adressent aux équipes qui utilisent déjà la structure de tâche Seedance de style Volc. L’authentification utilise votre clé API TokenLab Bearer. L’ID de tâche retourné commence par `cgt-`; interrogez le statut jusqu’à ce que `status` devienne `succeeded`, `failed`, `cancelled` ou `expired`.

Voir aussi [Modèles vidéo Seedance 2.0](/fr/guides/seedance-2-video) et [Génération vidéo](/fr/guides/video-generation).

## Format de réponse

Une annulation réussie retourne HTTP 200 avec un corps vide. Interrogez ensuite la tâche si vous devez confirmer son statut final.

## Exemple

```bash
curl -X DELETE https://api.tokenlab.sh/api/v3/contents/generations/tasks/cgt-0123456789abcdef0123456789abcdef \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```
