---
title: "✨ Claude Code Skill"
description: "Guide de démarrage rapide en 5 minutes - Laissez Claude Code intégrer automatiquement n'importe quelle API LemonData pour vous"
---

<Note>
Ce skill vous permet d'intégrer n'importe laquelle des **centaines d'API IA** de LemonData en décrivant simplement ce dont vous avez besoin en langage naturel. L'API est **Agent-First** — même si Claude devine mal un nom de modèle, la réponse d'erreur lui indique exactement comment corriger.
</Note>

## Que peut faire ce skill ?

Lorsque vous souhaitez utiliser des fonctionnalités IA dans votre code (comme GPT-4, la génération d'images, la reconnaissance vocale, etc.), ce skill va :

1. ✅ **Essayer l'API directement** — aucune recherche nécessaire
2. ✅ **S'auto-corriger** — les indications d'erreur structurées guident la tentative suivante
3. ✅ **Générer du code complet et exécutable** dans le langage de votre choix
4. ✅ **Choisir le meilleur modèle** en fonction des prix et de la disponibilité en temps réel
5. ✅ **Optimiser votre intégration** avec des suggestions d'endpoints natifs

**En résumé : Décrivez simplement vos besoins et obtenez du code d'intégration API fonctionnel — même si la première tentative n'est pas parfaite, l'API guide Claude vers la bonne réponse.**

## Étape 1 : Installer le skill

<Tabs>
  <Tab title="npx (Recommandé)">
    Le moyen le plus simple d'installer :

    ```bash
    npx add-skill hedging8563/lemondata-api-skill -y
    ```

    Cela installera automatiquement le skill pour tous les agents de codage détectés (Claude Code, Cursor, Copilot, etc.).
  </Tab>
  <Tab title="Partager l'URL GitHub">
    1. Ouvrez **Claude Code** dans votre terminal ou IDE
    2. Collez ce lien dans le chat :
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
    3. Claude Code le reconnaîtra comme un dépôt de skill et vous aidera à l'installer
  </Tab>
  <Tab title="Git Clone">
    **Installation personnelle** (disponible dans tous les projets) :
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git ~/.claude/skills/lemondata-api-integration
    ```

    **Installation spécifique au projet** (partagée avec l'équipe via git) :
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git .claude/skills/lemondata-api-integration
    ```
  </Tab>
</Tabs>

### Vérifier l'installation

Demandez à Claude Code :
```
What skills are available?
```

Si vous voyez `lemondata-api-integration`, l'installation a réussi !

## Étape 2 : Obtenir votre API Key

Avant d'utiliser le skill, vous avez besoin d'un API Key LemonData.

<Tip>
**Vous n'avez pas besoin de configurer l'API Key à l'avance !** Lorsque vous utilisez le skill, Claude Code vous demandera automatiquement votre API Key et l'inclura dans le code généré.
</Tip>

<Steps>
  <Step title="Visiter LemonData">
    Rendez-vous sur [lemondata.cc](https://lemondata.cc)
  </Step>
  <Step title="Se connecter">
    Créez un compte ou connectez-vous
  </Step>
  <Step title="Obtenir l'API Key">
    Accédez à [Dashboard → API Keys](https://lemondata.cc/dashboard/api) et créez une nouvelle clé
  </Step>
  <Step title="Copier la clé">
    Votre clé commence par `sk-...` — conservez-la en lieu sûr
  </Step>
</Steps>

<Warning>
**Important :**
- Toutes les API LemonData utilisent le même API Key
- Ne partagez pas votre clé avec d'autres personnes
- Ne committez pas votre clé sur GitHub
</Warning>

## Étape 3 : Commencer à utiliser

Utiliser le skill est aussi simple que de discuter !

### Exemple 1 : Utiliser GPT-4

**Vous dites :**
```
I want to use GPT-4 in my Python project
```

**Claude Code va :**
1. Appeler l'API directement avec la meilleure estimation de modèle
2. Si le nom du modèle est incorrect, lire `did_you_mean` dans la réponse d'erreur et corriger automatiquement
3. Générer du code Python complet avec le bon modèle

**Vous obtenez :**
```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.lemondata.cc/v1"
)

response = client.chat.completions.create(
    model="gpt-4o",
    messages=[{"role": "user", "content": "Hello!"}]
)

print(response.choices[0].message.content)
```

### Exemple 2 : Générer des images

**Vous dites :**
```
How do I generate images with Flux in Node.js?
```

**Claude Code va :**
- Appeler l'API de génération d'images
- Générer du code JavaScript avec une gestion d'erreurs appropriée
- Inclure le code de téléchargement et de sauvegarde des images

### Exemple 3 : Reconnaissance vocale

**Vous dites :**
```
Integrate a speech-to-text API
```

**Claude Code va :**
- Demander quel langage de programmation vous utilisez
- Générer du code avec le bon endpoint audio
- Fournir des exemples de traitement de fichiers audio

## Fonctionnalités Agent-First de l'API

L'API de LemonData est conçue pour les agents IA. Chaque réponse d'erreur inclut des indications structurées :

### Récupération intelligente des erreurs

| Erreur | Ce que l'API renvoie | Ce que Claude fait |
|--------|---------------------|-------------------|
| Mauvais nom de modèle | `did_you_mean` + `suggestions` | Corrige automatiquement et réessaie |
| Solde insuffisant | `balance_usd` + `suggestions` moins chers | Passe à un modèle abordable |
| Modèle indisponible | `alternatives` + `retry_after` | Passe à un modèle disponible |
| Limite de débit atteinte | `retry_after` (secondes exactes) | Attend et réessaie |
| Contexte trop long | `suggestions` avec des modèles plus grands | Passe à un modèle avec un contexte plus large |

### Découverte de modèles

Pas besoin de chercher dans la documentation. Utilisez ces endpoints API :

```bash
# Lire l'aperçu API lisible par machine
curl https://lemondata.cc/llms.txt

# Lister les modèles par catégorie
curl "https://api.lemondata.cc/v1/models?category=chat" -H "Authorization: Bearer sk-KEY"
curl "https://api.lemondata.cc/v1/models?category=image" -H "Authorization: Bearer sk-KEY"

# Filtrer par capacité
curl "https://api.lemondata.cc/v1/models?tag=coding" -H "Authorization: Bearer sk-KEY"
```

### Optimisation des endpoints natifs

Lorsque Claude utilise `/v1/chat/completions` avec un modèle Claude ou Gemini, l'API renvoie des en-têtes d'optimisation :

```
X-LemonData-Hint: This model supports native Anthropic format. Use POST /v1/messages for better performance.
X-LemonData-Native-Endpoint: /v1/messages
```

Claude Code passera automatiquement à l'endpoint natif pour de meilleures performances.

## Fonctionnalités prises en charge

Ce skill peut vous aider à intégrer toutes ces capacités IA :

| Type de fonctionnalité | Exemples |
|-----------------------|----------|
| 💬 Chat | GPT-4o, Claude, Gemini, DeepSeek |
| 🎨 Génération d'images | Midjourney, Flux, Stable Diffusion |
| 🎬 Génération de vidéos | Sora, Runway, Kling, Luma AI |
| 🎵 Génération de musique | Suno |
| 🗿 Modèles 3D | Tripo3D |
| 🎤 Audio | Text-to-Speech, Speech-to-Text |
| 📊 Embeddings | text-embedding-3 |
| 🔄 Rerank | bce-reranker, qwen3-rerank |

## Conseils d'utilisation

### 1. Exprimez simplement vos besoins

<Tabs>
  <Tab title="❌ Ne faites pas ça">
    ```
    Help me check if LemonData has an image generation API
    ```
  </Tab>
  <Tab title="✅ Faites plutôt ça">
    ```
    I want to generate images in Python
    ```
  </Tab>
</Tabs>

### 2. Précisez votre langage

Si vous avez une préférence de langage, dites-le simplement :

```
Use JavaScript to call GPT-4
Use Python for speech recognition
Use Go to generate images
```

### 3. Décrivez votre scénario

Si vous avez des besoins spécifiques, décrivez-les en détail :

```
I'm building a chatbot, need to use GPT-4
My website needs to let users upload and transform images
I'm making a voice notes app, need speech-to-text
```

## Points importants

### Sécurité de l'API Key

<CardGroup cols={2}>
  <Card title="❌ Ne jamais faire" icon="xmark" color="#ef4444">
    - Mettre l'API Key directement dans le code frontend
    - Committer l'API Key sur GitHub
    - Partager l'API Key avec d'autres personnes
  </Card>
  <Card title="✅ Bonnes pratiques" icon="check" color="#22c55e">
    - Utiliser des variables d'environnement
    - Appeler les API depuis le backend pour les sites web
    - Utiliser des frameworks comme Next.js, Express
  </Card>
</CardGroup>

### Gestion des coûts

- Chaque appel API consomme des crédits
- Définissez des limites d'utilisation dans votre [dashboard](https://lemondata.cc/dashboard)
- Utilisez de petits volumes de données pour les tests
- Vérifiez `error.balance_usd` et `error.estimated_cost_usd` pour surveiller les coûts de manière programmatique

### Gestion des erreurs

Le code généré inclut une gestion d'erreurs Agent-First qui :
- Lit les indications d'erreur structurées (`did_you_mean`, `suggestions`, `retry_after`)
- Récupère automatiquement des erreurs courantes
- Bascule vers des modèles alternatifs si nécessaire

## FAQ

<AccordionGroup>
  <Accordion title="Le skill ne se déclenche pas automatiquement ?">
    Essayez de mentionner « LemonData » ou « LemonData API » dans votre demande, ou décrivez votre besoin plus précisément :
    ```
    Use LemonData to integrate GPT-4 in my project
    ```
  </Accordion>

  <Accordion title="Comment l'API aide-t-elle Claude à trouver le bon modèle ?">
    L'API Agent-First de LemonData renvoie des indications d'erreur structurées. Si Claude devine un mauvais nom de modèle, la réponse d'erreur inclut `did_you_mean` avec le nom correct et `suggestions` avec des modèles alternatifs. Claude les lit et se corrige automatiquement lors de la tentative suivante.
  </Accordion>

  <Accordion title="Le code généré contient des erreurs ?">
    Vérifiez ces points :
    1. L'API Key est-elle correcte ?
    2. Les bibliothèques requises sont-elles installées ? (ex. `openai` pour Python)
    3. Pouvez-vous accéder aux serveurs de LemonData ?
    4. Votre API Key dispose-t-elle de crédits suffisants ?
  </Accordion>

  <Accordion title="Comment mettre à jour le skill ?">
    Envoyez à nouveau le lien GitHub à Claude Code :
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
  </Accordion>
</AccordionGroup>

## Ressources

<CardGroup cols={2}>
  <Card title="Site web LemonData" icon="globe" href="https://lemondata.cc">
    Site principal et dashboard
  </Card>
  <Card title="Documentation API" icon="book" href="https://docs.lemondata.cc">
    Référence API complète
  </Card>
  <Card title="Modèles" icon="robot" href="https://lemondata.cc/en/models">
    Parcourir tous les modèles disponibles
  </Card>
  <Card title="llms.txt" icon="file-lines" href="https://lemondata.cc/llms.txt">
    Aperçu API lisible par machine pour les agents IA
  </Card>
</CardGroup>

## C'est parti !

Vous savez maintenant comment utiliser ce skill. Essayez de demander à Claude Code :

```
I want to use GPT-4 in my project
```

ou

```
Help me integrate image generation
```

Claude Code utilisera automatiquement ce skill pour accomplir tout le travail à votre place — et si quelque chose ne va pas, les indications d'erreur structurées de l'API guideront Claude vers la bonne réponse !

<Info>
**Des questions ?** Consultez nos [GitHub Issues](https://github.com/hedging8563/lemondata-api-skill/issues) ou contactez [support@lemondata.cc](mailto:support@lemondata.cc)
</Info>
