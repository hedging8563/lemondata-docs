---
title: "✨ API orientée agent"
description: "Indices d’erreur structurés pour que les agents IA s’auto-corrigent dès la première nouvelle tentative"
---

## Vue d’ensemble

L’API orientée agent de LemonData enrichit les réponses d’erreur avec des indices structurés que les agents IA peuvent analyser et exploiter immédiatement — sans recherches web, sans consultation de la documentation, sans approximations.

Chaque réponse d’erreur inclut des champs optionnels comme `did_you_mean`, `suggestions`, `hint`, `retryable` et `retry_after` dans l’objet standard `error`. Ces champs sont rétrocompatibles — les clients qui ne les utilisent pas ne verront aucune différence.

## Champs des indices d’erreur

Tous les champs d’indice sont des extensions optionnelles à l’intérieur de l’objet `error` :

| Field | Type | Description |
|-------|------|-------------|
| `did_you_mean` | `string` | Nom du modèle correspondant le plus proche |
| `suggestions` | `array` | Modèles recommandés avec métadonnées |
| `alternatives` | `array` | Modèles alternatifs actuellement disponibles |
| `hint` | `string` | Guide de prochaine étape lisible par un humain/un agent |
| `retryable` | `boolean` | Indique si relancer la même requête peut réussir |
| `retry_after` | `number` | Nombre de secondes à attendre avant de réessayer |
| `balance_usd` | `number` | Solde actuel du compte en USD |
| `estimated_cost_usd` | `number` | Coût estimé de la requête échouée |

## Exemples de codes d’erreur

### model_not_found (400)

Lorsqu’un nom de modèle ne correspond à aucun modèle actif :

```json
{
  "error": {
    "message": "Model 'gpt5' not found",
    "type": "invalid_request_error",
    "param": "model",
    "code": "model_not_found",
    "did_you_mean": "gpt-5.4",
    "suggestions": [
      {"id": "gpt-5.4"},
      {"id": "gpt-5-mini"},
      {"id": "claude-sonnet-4-6"}
    ],
    "hint": "Did you mean 'gpt-5.4'? Use GET https://api.lemondata.cc/v1/models to list all available models."
  }
}
```

La résolution `did_you_mean` utilise :
1. Un mappage d’alias statique (issu des données d’erreurs de production)
2. Une correspondance de chaînes normalisées (suppression des tirets, insensible à la casse)
3. Une correspondance par distance d’édition (seuil ≤ 3)


### model_price_not_configured (400)

Lorsqu'un modèle existe en interne mais n'est pas actuellement exposé aux requêtes publiques :

```json
{
  "error": {
    "message": "Model 'example-model' is not currently available for public requests.",
    "type": "invalid_request_error",
    "param": "model",
    "code": "model_price_not_configured",
    "retryable": false,
    "hint": "This model is not currently available for public requests. Use GET /v1/models to list publicly available model IDs."
  }
}
```

Les agents doivent considérer cela comme un signal de disponibilité de la plateforme, et non comme une faute de frappe. Ne réessayez le même modèle qu'après son apparition dans `GET /v1/models`.

### insufficient_balance (402)

Lorsque le solde du compte est trop faible par rapport au coût estimé :

```json
{
  "error": {
    "message": "Insufficient balance: need ~$0.3500 for claude-sonnet-4-6, but balance is $0.1200.",
    "type": "insufficient_balance",
    "code": "insufficient_balance",
    "balance_usd": 0.12,
    "estimated_cost_usd": 0.35,
    "suggestions": [
      {"id": "gpt-5-mini"},
      {"id": "deepseek-v3-2"}
    ],
    "hint": "Insufficient balance: need ~$0.3500 for claude-sonnet-4-6, but balance is $0.1200. Try a cheaper model, or top up at https://lemondata.cc/dashboard/billing."
  }
}
```

`suggestions` contient des modèles moins chers que le coût estimé vers lesquels l’agent peut basculer.

### all_channels_failed (503)

Lorsque tous les canaux amont d’un modèle sont indisponibles :

```json
{
  "error": {
    "message": "Model claude-opus-4-6 temporarily unavailable",
    "code": "all_channels_failed",
    "retryable": true,
    "retry_after": 30,
    "alternatives": [
      {"id": "claude-sonnet-4-6", "status": "available", "tags": []},
      {"id": "gpt-5-mini", "status": "available", "tags": []}
    ],
    "hint": "All channels for 'claude-opus-4-6' are temporarily unavailable. Retry in 30s or try an alternative model."
  }
}
```

<Note>
`retryable` vaut `false` lorsque la raison est `no_channels` (aucun canal configuré pour ce modèle). Il vaut `true` uniquement pour les échecs transitoires comme les déclenchements de circuit breaker ou l’épuisement du quota.
</Note>

### rate_limit_exceeded (429)

```json
{
  "error": {
    "message": "Rate limit: 60 rpm exceeded",
    "type": "rate_limit_exceeded",
    "code": "rate_limit_exceeded",
    "retryable": true,
    "retry_after": 8,
    "hint": "Rate limited. Retry after 8s. Current limit: 60/min for user role."
  }
}
```

La valeur `retry_after` est calculée à partir de l’heure réelle de réinitialisation de la fenêtre de rate limit.

<Note>
Les endpoints compatibles OpenAI utilisent les types d’erreur publics stables de LemonData tels que `rate_limit_exceeded`, `upstream_error` et `all_channels_failed`. Les endpoints compatibles Anthropic et Gemini utilisent leurs propres formats de réponse natifs.
</Note>

### context_length_exceeded (400)

Lorsque l’entrée dépasse la fenêtre de contexte du modèle (erreur amont, enrichie avec des indices) :

```json
{
  "error": {
    "message": "This model's maximum context length is 128000 tokens...",
    "type": "invalid_request_error",
    "code": "context_length_exceeded",
    "retryable": false,
    "suggestions": [
      {"id": "gemini-2.5-pro"},
      {"id": "claude-sonnet-4-6"}
    ],
    "hint": "Reduce your input or switch to a model with a larger context window."
  }
}
```

## En-têtes des endpoints natifs

Lorsque vous appelez `/v1/chat/completions` avec un modèle qui possède un endpoint natif (Anthropic ou Gemini), la **réponse de succès** inclut des en-têtes d’optimisation :

```
X-LemonData-Hint: This model supports native Anthropic format. Use POST /v1/messages for better performance (no format conversion).
X-LemonData-Native-Endpoint: /v1/messages
```

| Model Provider | Suggested Endpoint | Benefit |
|---------------|-------------------|---------|
| Anthropic (Claude) | `/v1/messages` | Pas de conversion de format, extended thinking, mise en cache des prompts |
| Google (Gemini) | `/v1beta/gemini` | Pas de conversion de format, grounding, paramètres de sécurité |
| OpenAI | — | Chat completions est déjà le format natif |

Ces en-têtes apparaissent à la fois sur les réponses en streaming et non-streaming.

## Améliorations de /v1/models

Trois nouveaux champs dans l’extension `lemondata` de chaque objet modèle :

```json
{
  "id": "gpt-5.4",
  "lemondata": {
    "category": "chat",
    "pricing_unit": "per_token",
    "cache_pricing": {
      "cache_read_per_1m": "1.25",
      "cache_write_per_1m": "2.50",
      "platform_cache_discount": 0.9
    }
  }
}
```

| Field | Values | Description |
|-------|--------|-------------|
| `category` | `chat`, `image`, `video`, `audio`, `tts`, `stt`, `3d`, `embedding`, `rerank` | Type de modèle |
| `pricing_unit` | `per_token`, `per_image`, `per_second`, `per_request` | Mode de facturation du modèle |
| `cache_pricing` | object or `null` | Prix du cache de prompt amont + remise de cache sémantique de la plateforme |

### Filtrage par catégorie

```bash
GET https://api.lemondata.cc/v1/models?category=chat          # Chat models only
GET https://api.lemondata.cc/v1/models?category=image         # Image generation models
GET https://api.lemondata.cc/v1/models?tag=coding&category=chat  # Coding-optimized chat models
```

## llms.txt

Une vue d’ensemble de l’API lisible par machine est disponible à l’adresse :

```
GET https://api.lemondata.cc/llms.txt
```

Elle inclut :
- Un modèle de premier appel avec un exemple fonctionnel
- Les noms de modèles courants (générés dynamiquement à partir des données d’usage)
- Les 12 endpoints de l’API
- Les paramètres de filtre pour la découverte des modèles
- Des recommandations pour la gestion des erreurs

Les agents IA qui lisent `llms.txt` avant leur premier appel API peuvent généralement réussir dès la première tentative.

## Utilisation dans le code d’agent

### Python (OpenAI SDK)

```python
from openai import OpenAI, BadRequestError

client = OpenAI(
    api_key="sk-your-key",
    base_url="https://api.lemondata.cc/v1"
)

def smart_chat(messages, model="gpt-4o"):
    try:
        return client.chat.completions.create(
            model=model, messages=messages
        )
    except BadRequestError as e:
        error = e.body.get("error", {}) if isinstance(e.body, dict) else {}
        # Use did_you_mean for auto-correction
        if error.get("code") == "model_not_found" and error.get("did_you_mean"):
            return client.chat.completions.create(
                model=error["did_you_mean"], messages=messages
            )
        raise
```

### JavaScript (OpenAI SDK)

```javascript
import OpenAI from 'openai';

const client = new OpenAI({
  apiKey: 'sk-your-key',
  baseURL: 'https://api.lemondata.cc/v1'
});

async function smartChat(messages, model = 'gpt-4o') {
  try {
    return await client.chat.completions.create({ model, messages });
  } catch (error) {
    const err = error?.error;
    if (err?.code === 'model_not_found' && err?.did_you_mean) {
      return client.chat.completions.create({
        model: err.did_you_mean, messages
      });
    }
    throw error;
  }
}
```

## Principes de conception

<CardGroup cols={2}>
  <Card title="Échouer rapidement, échouer de manière informative" icon="bolt">
    Les erreurs sont renvoyées immédiatement avec toutes les données dont un agent a besoin pour s’auto-corriger.
  </Card>
  <Card title="Pas d’auto-routage" icon="route">
    L’API ne substitue jamais silencieusement un modèle différent. C’est l’agent qui décide.
  </Card>
  <Card title="Suggestions fondées sur les données" icon="database">
    Toutes les recommandations proviennent des données de production, et non de listes codées en dur.
  </Card>
  <Card title="Rétrocompatible" icon="plug">
    Tous les champs d’indice sont optionnels. Les clients existants ne voient aucune différence.
  </Card>
</CardGroup>