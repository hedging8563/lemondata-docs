---
title: "Zhipu AI (GLM)"
description: "GLM-4, GLM-4.5, GLM-4.6 series"
---

## Overview

TokenLab provides access to Zhipu AI (GLM) models. Total: **6 models** available.

## Chat Models

| Model ID | Pricing |
|----------|--------|
| `glm-4.7` | $1/1M in |
| `glm-5` | $0.58823529/1M in |
| `glm-5-turbo` | $1.2/1M in |
| `glm-5.1` | $0.88235294/1M in |
| `glm-5.2` | $1.17647059/1M in |
| `glm-5v-turbo` | $1.2/1M in |


## Usage

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-tokenlab-key",
    base_url="https://api.tokenlab.sh/v1"
)

response = client.chat.completions.create(
    model="glm-4.7",
    messages=[{"role": "user", "content": "Hello!"}]
)
```

<Note>
The table shows the public catalog's current input price when available. See your dashboard for complete input, output, and unit-specific pricing.
</Note>
