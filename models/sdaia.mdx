---
title: "SDAIA (ALLaM)"
description: "ALLaM Arabic language models"
---

## Overview

TokenLab provides access to SDAIA (ALLaM) models. Total: **1 model** available.

## Chat Models

| Model ID | Pricing |
|----------|--------|
| `allam-2-7b` | Input $0.05 / Output $0.08 per 1M tokens |


## Usage

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-tokenlab-key",
    base_url="https://api.tokenlab.sh/v1"
)

response = client.chat.completions.create(
    model="allam-2-7b",
    messages=[{"role": "user", "content": "Hello!"}]
)
```

<Note>
The table shows current public pricing as input/output rates, a unit price, or a price range. See your dashboard for complete option-specific pricing.
</Note>
