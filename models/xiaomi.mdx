---
title: "Xiaomi (MiMo)"
description: "MiMo reasoning and agentic models"
---

## Overview

TokenLab provides access to Xiaomi (MiMo) models. Total: **2 models** available.

## Chat Models

| Model ID | Pricing |
|----------|--------|
| `mimo-v2.5` | Input $0.14 / Output $0.28 per 1M tokens |
| `mimo-v2.5-pro` | Input $0.435 / Output $0.87 per 1M tokens |


## Usage

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-tokenlab-key",
    base_url="https://api.tokenlab.sh/v1"
)

response = client.chat.completions.create(
    model="mimo-v2.5",
    messages=[{"role": "user", "content": "Hello!"}]
)
```

<Note>
The table shows current public pricing as input/output rates, a unit price, or a price range. See your dashboard for complete option-specific pricing.
</Note>
