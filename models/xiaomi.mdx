---
title: "Xiaomi (MiMo)"
description: "MiMo reasoning and agentic models"
---

## Overview

TokenLab provides access to Xiaomi (MiMo) models. Total: **5 models** available.

## Chat Models

| Model ID | Pricing |
|----------|--------|
| `mimo-v2-flash` | Input $0.4 / Output $2 per 1M tokens |
| `mimo-v2-omni` | Input $0.4 / Output $2 per 1M tokens |
| `mimo-v2-pro` | Input $1 / Output $3 per 1M tokens |
| `mimo-v2.5` | Input $0.4 / Output $2 per 1M tokens |
| `mimo-v2.5-pro` | $0.205882352941–$3.088235294118/1M tokens |


## Usage

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-tokenlab-key",
    base_url="https://api.tokenlab.sh/v1"
)

response = client.chat.completions.create(
    model="mimo-v2.5",
    messages=[{"role": "user", "content": "Hello!"}]
)
```

<Note>
The table shows current public pricing as input/output rates, a unit price, or a price range. See your dashboard for complete option-specific pricing.
</Note>
