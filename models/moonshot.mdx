---
title: "Moonshot (Kimi)"
description: "Kimi K2, Moonshot models"
---

## Overview

TokenLab provides access to Moonshot (Kimi) models. Total: **5 models** available.

## Chat Models

| Model ID | Pricing |
|----------|--------|
| `kimi-k2.5` | Input $0.6 / Output $3 per 1M tokens |
| `kimi-k2.6` | Input $0.95 / Output $4 per 1M tokens |
| `kimi-k2.7-code` | Input $0.95 / Output $4 per 1M tokens |
| `kimi-k2.7-code-highspeed` | Input $1.9 / Output $8 per 1M tokens |
| `kimi-k3` | Input $3 / Output $15 per 1M tokens |


## Usage

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-tokenlab-key",
    base_url="https://api.tokenlab.sh/v1"
)

response = client.chat.completions.create(
    model="kimi-k2.5",
    messages=[{"role": "user", "content": "Hello!"}]
)
```

<Note>
The table shows current public pricing as input/output rates, a unit price, or a price range. See your dashboard for complete option-specific pricing.
</Note>
