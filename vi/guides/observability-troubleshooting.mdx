---
title: "Quan sát và xử lý sự cố"
description: "Hướng dẫn production cho Quan sát và xử lý sự cố."
---

## Tổng quan

Hướng dẫn này giải thích cách dùng Quan sát và xử lý sự cố trong production mà không gắn ứng dụng vào chi tiết triển khai của provider.

Dùng API công khai của TokenLab, metadata mô hình và field phản hồi đã tài liệu hóa làm hợp đồng.

## Hành vi hiện tại

- Kiểm tra các giá trị này trước khi phát hành: `request_id`, `task_id`, `billing_transaction_id`.
- Không phụ thuộc URL trạng thái riêng của provider, định danh routing hoặc metadata thực thi riêng tư.
- Làm retry có tính idempotent và tránh tạo công việc bị tính phí trùng lặp.

## Checklist production

- Log cần hữu ích cho support nhưng không lưu secret, file riêng tư, prompt hoặc media người dùng.
- Ticket support nên có request shape đã làm sạch, endpoint, model, timestamp và định danh công khai.
- Xác minh flow ở staging trước khi chuyển traffic production.

## Trang liên quan

- [Xử lý lỗi](/vi/guides/error-handling)
- [Thanh toán & Giá cả](/vi/guides/billing)
- [Tác vụ bất đồng bộ và polling](/vi/guides/async-jobs-polling)
- [Lấy mức sử dụng API key](/vi/api-reference/management/get-api-key-usage)
