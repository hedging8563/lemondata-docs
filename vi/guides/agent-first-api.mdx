---
title: "✨ API ưu tiên Agent"
description: "Gợi ý lỗi có cấu trúc để AI agent tự sửa ngay ở lần thử lại đầu tiên"
---

## Tổng quan

API ưu tiên Agent của LemonData làm phong phú phản hồi lỗi bằng các gợi ý có cấu trúc mà AI agent có thể phân tích và hành động ngay lập tức — không cần tìm kiếm trên web, không cần tra cứu tài liệu, không cần phỏng đoán.

Mọi phản hồi lỗi đều bao gồm các trường tùy chọn như `did_you_mean`, `suggestions`, `hint`, `retryable`, và `retry_after` bên trong đối tượng `error` tiêu chuẩn. Các trường này tương thích ngược — client không sử dụng chúng sẽ không thấy bất kỳ khác biệt nào.

## Các trường gợi ý lỗi

Tất cả các trường gợi ý đều là phần mở rộng tùy chọn bên trong đối tượng `error`:

| Trường | Kiểu | Mô tả |
|-------|------|-------------|
| `did_you_mean` | `string` | Tên model khớp gần nhất |
| `suggestions` | `array` | Các model được đề xuất kèm metadata |
| `alternatives` | `array` | Các model thay thế hiện đang khả dụng |
| `hint` | `string` | Hướng dẫn bước tiếp theo dành cho người/agent có thể đọc được |
| `retryable` | `boolean` | Việc thử lại cùng một request có thể thành công hay không |
| `retry_after` | `number` | Số giây cần chờ trước khi thử lại |
| `balance_usd` | `number` | Số dư tài khoản hiện tại tính bằng USD |
| `estimated_cost_usd` | `number` | Chi phí ước tính của request thất bại |

## Ví dụ về mã lỗi

### model_not_found (400)

Khi tên model không khớp với bất kỳ model đang hoạt động nào:

```json
{
  "error": {
    "message": "Model 'gpt5' not found",
    "type": "invalid_request_error",
    "param": "model",
    "code": "model_not_found",
    "did_you_mean": "gpt-5.4",
    "suggestions": [
      {"id": "gpt-5.4"},
      {"id": "gpt-5-mini"},
      {"id": "claude-sonnet-4-6"}
    ],
    "hint": "Did you mean 'gpt-5.4'? Use GET https://api.lemondata.cc/v1/models to list all available models."
  }
}
```

Cách phân giải `did_you_mean` sử dụng:
1. Ánh xạ alias tĩnh (từ dữ liệu lỗi production)
2. So khớp chuỗi đã chuẩn hóa (loại bỏ dấu gạch nối, không phân biệt chữ hoa chữ thường)
3. So khớp khoảng cách chỉnh sửa (ngưỡng ≤ 3)

### insufficient_balance (402)

Khi số dư tài khoản quá thấp so với chi phí ước tính:

```json
{
  "error": {
    "message": "Insufficient balance: need ~$0.3500 for claude-sonnet-4-6, but balance is $0.1200.",
    "type": "insufficient_balance",
    "code": "insufficient_balance",
    "balance_usd": 0.12,
    "estimated_cost_usd": 0.35,
    "suggestions": [
      {"id": "gpt-5-mini"},
      {"id": "deepseek-v3-2"}
    ],
    "hint": "Insufficient balance: need ~$0.3500 for claude-sonnet-4-6, but balance is $0.1200. Try a cheaper model, or top up at https://lemondata.cc/dashboard/billing."
  }
}
```

`suggestions` chứa các model có chi phí thấp hơn chi phí ước tính mà agent có thể chuyển sang.

### all_channels_failed (503)

Khi mọi channel upstream cho một model đều không khả dụng:

```json
{
  "error": {
    "message": "Model claude-opus-4-6 temporarily unavailable",
    "code": "all_channels_failed",
    "retryable": true,
    "retry_after": 30,
    "alternatives": [
      {"id": "claude-sonnet-4-6", "status": "available", "tags": []},
      {"id": "gpt-5-mini", "status": "available", "tags": []}
    ],
    "hint": "All channels for 'claude-opus-4-6' are temporarily unavailable. Retry in 30s or try an alternative model."
  }
}
```

<Note>
`retryable` là `false` khi lý do là `no_channels` (không có channel nào được cấu hình cho model này). Nó chỉ là `true` đối với các lỗi tạm thời như circuit breaker bị kích hoạt hoặc quota cạn kiệt.
</Note>

### rate_limit_exceeded (429)

```json
{
  "error": {
    "message": "Rate limit: 60 rpm exceeded",
    "type": "rate_limit_exceeded",
    "code": "rate_limit_exceeded",
    "retryable": true,
    "retry_after": 8,
    "hint": "Rate limited. Retry after 8s. Current limit: 60/min for user role."
  }
}
```

Giá trị `retry_after` được tính từ thời điểm reset thực tế của cửa sổ rate limit.

<Note>
Các endpoint tương thích OpenAI sử dụng các loại lỗi public ổn định của LemonData như `rate_limit_exceeded`, `upstream_error`, và `all_channels_failed`. Các endpoint tương thích Anthropic và tương thích Gemini sử dụng các dạng phản hồi native riêng của chúng.
</Note>

### context_length_exceeded (400)

Khi input vượt quá cửa sổ ngữ cảnh của model (lỗi upstream, được bổ sung gợi ý):

```json
{
  "error": {
    "message": "This model's maximum context length is 128000 tokens...",
    "type": "invalid_request_error",
    "code": "context_length_exceeded",
    "retryable": false,
    "suggestions": [
      {"id": "gemini-2.5-pro"},
      {"id": "claude-sonnet-4-6"}
    ],
    "hint": "Reduce your input or switch to a model with a larger context window."
  }
}
```

## Header của endpoint native

Khi bạn gọi `/v1/chat/completions` với một model có endpoint native (Anthropic hoặc Gemini), **phản hồi thành công** sẽ bao gồm các header tối ưu hóa:

```
X-LemonData-Hint: This model supports native Anthropic format. Use POST /v1/messages for better performance (no format conversion).
X-LemonData-Native-Endpoint: /v1/messages
```

| Nhà cung cấp model | Endpoint được đề xuất | Lợi ích |
|---------------|-------------------|---------|
| Anthropic (Claude) | `/v1/messages` | Không cần chuyển đổi định dạng, extended thinking, prompt caching |
| Google (Gemini) | `/v1beta/gemini` | Không cần chuyển đổi định dạng, grounding, thiết lập an toàn |
| OpenAI | — | Chat completions đã là định dạng native |

Các header này xuất hiện trên cả phản hồi streaming và non-streaming.

## Cải tiến của /v1/models

Ba trường mới trong phần mở rộng `lemondata` của mỗi đối tượng model:

```json
{
  "id": "gpt-5.4",
  "lemondata": {
    "category": "chat",
    "pricing_unit": "per_token",
    "cache_pricing": {
      "cache_read_per_1m": "1.25",
      "cache_write_per_1m": "2.50",
      "platform_cache_discount": 0.9
    }
  }
}
```

| Trường | Giá trị | Mô tả |
|-------|--------|-------------|
| `category` | `chat`, `image`, `video`, `audio`, `tts`, `stt`, `3d`, `embedding`, `rerank` | Loại model |
| `pricing_unit` | `per_token`, `per_image`, `per_second`, `per_request` | Cách model được tính phí |
| `cache_pricing` | object hoặc `null` | Giá prompt cache upstream + mức giảm giá semantic cache của nền tảng |

### Lọc theo category

```bash
GET https://api.lemondata.cc/v1/models?category=chat          # Chat models only
GET https://api.lemondata.cc/v1/models?category=image         # Image generation models
GET https://api.lemondata.cc/v1/models?tag=coding&category=chat  # Coding-optimized chat models
```

## llms.txt

Tổng quan API ở định dạng máy có thể đọc được hiện có tại:

```
GET https://api.lemondata.cc/llms.txt
```

Nó bao gồm:
- Mẫu gọi đầu tiên với ví dụ hoạt động thực tế
- Tên model phổ biến (được tạo động từ dữ liệu sử dụng)
- Toàn bộ 12 endpoint API
- Các tham số lọc để khám phá model
- Hướng dẫn xử lý lỗi

AI agent đọc `llms.txt` trước lần gọi API đầu tiên thường có thể thành công ngay từ lần thử đầu tiên.

## Cách sử dụng trong mã agent

### Python (OpenAI SDK)

```python
from openai import OpenAI, BadRequestError

client = OpenAI(
    api_key="sk-your-key",
    base_url="https://api.lemondata.cc/v1"
)

def smart_chat(messages, model="gpt-4o"):
    try:
        return client.chat.completions.create(
            model=model, messages=messages
        )
    except BadRequestError as e:
        error = e.body.get("error", {}) if isinstance(e.body, dict) else {}
        # Use did_you_mean for auto-correction
        if error.get("code") == "model_not_found" and error.get("did_you_mean"):
            return client.chat.completions.create(
                model=error["did_you_mean"], messages=messages
            )
        raise
```

### JavaScript (OpenAI SDK)

```javascript
import OpenAI from 'openai';

const client = new OpenAI({
  apiKey: 'sk-your-key',
  baseURL: 'https://api.lemondata.cc/v1'
});

async function smartChat(messages, model = 'gpt-4o') {
  try {
    return await client.chat.completions.create({ model, messages });
  } catch (error) {
    const err = error?.error;
    if (err?.code === 'model_not_found' && err?.did_you_mean) {
      return client.chat.completions.create({
        model: err.did_you_mean, messages
      });
    }
    throw error;
  }
}
```

## Nguyên tắc thiết kế

<CardGroup cols={2}>
  <Card title="Thất bại nhanh, thất bại với thông tin đầy đủ" icon="bolt">
    Lỗi được trả về ngay lập tức cùng với toàn bộ dữ liệu mà agent cần để tự sửa.
  </Card>
  <Card title="Không tự động định tuyến" icon="route">
    API không bao giờ âm thầm thay thế bằng một model khác. Agent là bên quyết định.
  </Card>
  <Card title="Đề xuất dựa trên dữ liệu" icon="database">
    Mọi đề xuất đều đến từ dữ liệu production, không phải từ các danh sách hardcode.
  </Card>
  <Card title="Tương thích ngược" icon="plug">
    Tất cả các trường gợi ý đều là tùy chọn. Các client hiện có sẽ không thấy khác biệt.
  </Card>
</CardGroup>