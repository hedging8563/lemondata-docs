---
title: "Tạo API Key"
openapi: "POST /v1/management/api-keys"
description: "Tạo API key người dùng mới cho tổ chức hiện tại."
---
## Tổng quan

Endpoint này tạo một API Key mới loại `type=user` trong tổ chức gắn với management token hiện tại.

## Request body

| Trường | Kiểu | Mặc định / giới hạn | Ghi chú |
|-------|------|------------------|-------|
| name | string | bắt buộc, mặc định Default Key, độ dài 1-50 | Tên hiển thị, được làm sạch ở phía server |
| limitAmount | number \| null | tối thiểu 0, tối đa 100000 | null hoặc bỏ qua = không giới hạn, 0 = không có quota khả dụng, số dương = trần USD |
| models | string[] | mặc định [] | Danh sách cho phép logical model tùy chọn |
| expiresAt | string \| null | datetime RFC3339 | null nghĩa là không hết hạn |

## Lưu ý

- Giá trị đầy đủ của API Key chỉ được hiển thị một lần trong phản hồi.
- `models: []` nghĩa là key không có ràng buộc model bổ sung ngoài chính sách của tổ chức.
- `limitAmount: 0` tạo ra một key hợp lệ nhưng chưa thể tiêu quota cho đến khi được cập nhật sau đó.

## Ví dụ

<RequestExample>
```bash cURL
curl -X POST "https://api.lemondata.cc/v1/management/api-keys" \
  -H "Authorization: Bearer mt-your-management-token" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Backend Worker",
    "limitAmount": 500,
    "models": ["veo3.1", "sora-2"],
    "expiresAt": "2026-12-31T23:59:59Z"
  }'
```
</RequestExample>

## Request / Response

Hãy dùng bảng OpenAPI tương tác phía trên để xem schema chính xác và các trường phản hồi.
