---
title: "Liệt kê các nhóm tài nguyên vật liệu"
openapi: "GET /v1/videos/assets/groups"
description: "Liệt kê các nhóm vật liệu Seedance thuộc sở hữu của tổ chức hiện tại."
---

Liệt kê các nhóm vật liệu thuộc sở hữu của tổ chức đã xác thực. Lọc theo `library_type` và `status` để phân tách các nhóm avatar ảo khỏi các nhóm người thật.

Sử dụng `group_id` được trả về khi tải lên các tài liệu. Không truyền `group_id` dưới dạng `material_asset_id` trong quá trình tạo video; việc tạo video yêu cầu `id` của tài nguyên vật liệu.

## Ví dụ yêu cầu

```bash
curl "https://api.tokenlab.sh/v1/videos/assets/groups?library_type=aigc_avatar&page_size=20" \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```

## Ví dụ phản hồi

```json
{
  "groups": [
    {
      "id": "group-20260720123456-vrt01",
      "object": "seedance.material_asset_group",
      "library_type": "aigc_avatar",
      "group_name": "Product references",
      "status": "ACTIVE",
      "created_at": 1782998400000
    }
  ],
  "next_page_token": null
}
```