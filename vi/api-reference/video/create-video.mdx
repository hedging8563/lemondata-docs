---
title: "Tạo Video"
openapi: "POST /v1/videos/generations"
description: "Tạo một tác vụ sinh video"
---

## Tổng quan

Việc sinh video là bất đồng bộ. Bạn gửi một request, nhận được task ID và `poll_url`, sau đó polling để lấy kết quả.

<Info>
Để có hành vi polling đáng tin cậy nhất, hãy làm theo chính xác `poll_url` được trả về từ request tạo.
</Info>

<Warning>
Đầu ra âm thanh phụ thuộc vào từng model. Trong LemonData, họ Veo 3 sẽ mặc định bật âm thanh nếu bỏ qua `output_audio`. Khi model hỗ trợ điều khiển âm thanh, hãy dùng `output_audio` để bật hoặc tắt một cách tường minh. Alias camelCase `outputAudio` cũng được chấp nhận để tương thích.
</Warning>


## Request Body

<ParamField body="model" type="string" default="sora-2">
  ID model video. Mặc định của API là `sora-2`. Xem [Hướng dẫn Sinh Video](/guides/video-generation) để biết ma trận model hiện tại và các khả năng được hỗ trợ.
</ParamField>

<ParamField body="prompt" type="string" required>
  Mô tả văn bản của video cần tạo.
</ParamField>

<ParamField body="image_url" type="string">
  URL có thể truy cập công khai của ảnh khởi đầu cho việc sinh image-to-video (I2V). Để có khả năng tương thích tốt nhất giữa các model, nên ưu tiên `image_url`.
</ParamField>

<ParamField body="image" type="string">
  Ảnh inline dưới dạng data URL (ví dụ: `data:image/jpeg;base64,...`). Được hỗ trợ bởi các model tương thích, nhưng `image_url` mang lại khả năng tương thích rộng nhất trên các model video.
</ParamField>

<ParamField body="duration" type="integer">
  Thời lượng video tính bằng giây (phụ thuộc vào model).
</ParamField>

<ParamField body="aspect_ratio" type="string">
  Tỷ lệ khung hình (ví dụ: `16:9`, `9:16`, `1:1`).
</ParamField>

<ParamField body="resolution" type="string">
  Độ phân giải video (ví dụ: `1080p`, `720p`).
</ParamField>

<ParamField body="output_audio" type="boolean">
  Công tắc đầu ra âm thanh phụ thuộc vào model. Trong LemonData, họ Veo 3 mặc định dùng `true` khi bỏ qua trường này. Các model video khác sẽ tuân theo hành vi mặc định đã được quy định của riêng chúng. Alias camelCase `outputAudio` được chấp nhận để tương thích.
</ParamField>

<ParamField body="fps" type="integer">
  Số khung hình trên giây (1-120).
</ParamField>

<ParamField body="negative_prompt" type="string">
  Những gì cần tránh trong quá trình sinh video.
</ParamField>

<ParamField body="seed" type="integer">
  Seed ngẫu nhiên để tái tạo kết quả sinh.
</ParamField>

<ParamField body="cfg_scale" type="number">
  Thang hướng dẫn classifier-free (0-20).
</ParamField>

<ParamField body="motion_strength" type="number">
  Cường độ chuyển động (0-1).
</ParamField>

<ParamField body="start_image" type="string">
  URL của ảnh khung hình bắt đầu.
</ParamField>

<ParamField body="end_image" type="string">
  URL của ảnh khung hình kết thúc.
</ParamField>

<ParamField body="user" type="string">
  Một định danh duy nhất cho người dùng cuối.
</ParamField>

## Response

<ResponseField name="task_id" type="string">
  Định danh tác vụ duy nhất để polling.
</ResponseField>

<ResponseField name="poll_url" type="string">
  URL polling được khuyến nghị cho tác vụ này. Sử dụng chính xác đường dẫn này khi kiểm tra trạng thái.
</ResponseField>

<ResponseField name="status" type="string">
  Trạng thái ban đầu: `pending`.
</ResponseField>

<ResponseField name="model" type="string">
  Model được sử dụng.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST "https://api.lemondata.cc/v1/videos/generations" \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sora-2",
    "prompt": "A cat walking through a garden, cinematic lighting",
    "operation": "text-to-video",
    "duration": 4,
    "aspect_ratio": "16:9"
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.lemondata.cc/v1/videos/generations",
    headers={
        "Authorization": "Bearer sk-your-api-key",
        "Content-Type": "application/json"
    },
    json={
        "model": "sora-2",
        "prompt": "A cat walking through a garden, cinematic lighting",
        "operation": "text-to-video",
        "duration": 4,
        "aspect_ratio": "16:9"
    }
)

data = response.json()
task_id = data["task_id"]
print(f"Poll URL: {data['poll_url']}")
print(f"Task ID: {task_id}")
```

```javascript JavaScript
const response = await fetch('https://api.lemondata.cc/v1/videos/generations', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer sk-your-api-key',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'sora-2',
    prompt: 'A cat walking through a garden, cinematic lighting',
    operation: 'text-to-video',
    duration: 4,
    aspect_ratio: '16:9'
  })
});

const data = await response.json();
console.log(`Poll URL: ${data.poll_url}`);
console.log(`Task ID: ${data.task_id}`);
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "net/http"
)

func main() {
    payload := map[string]interface{}{
        "model":    "sora-2",
        "prompt":   "A cat walking through a garden, cinematic lighting",
        "operation": "text-to-video",
        "duration": 4,
        "aspect_ratio": "16:9",
    }
    body, _ := json.Marshal(payload)

    req, _ := http.NewRequest("POST", "https://api.lemondata.cc/v1/videos/generations", bytes.NewBuffer(body))
    req.Header.Set("Authorization", "Bearer sk-your-api-key")
    req.Header.Set("Content-Type", "application/json")

    client := &http.Client{}
    resp, _ := client.Do(req)
    defer resp.Body.Close()

    var result map[string]interface{}
    json.NewDecoder(resp.Body).Decode(&result)
    fmt.Printf("Poll URL: %s\n", result["poll_url"])
    fmt.Printf("Task ID: %s\n", result["task_id"])
}
```

```php PHP
<?php
$ch = curl_init('https://api.lemondata.cc/v1/videos/generations');

curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_HTTPHEADER => [
        'Content-Type: application/json',
        'Authorization: Bearer sk-your-api-key'
    ],
    CURLOPT_POSTFIELDS => json_encode([
        'model' => 'sora-2',
        'prompt' => 'A cat walking through a garden, cinematic lighting',
        'operation' => 'text-to-video',
        'duration' => 4,
        'aspect_ratio' => '16:9'
    ])
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
echo "Poll URL: " . $data['poll_url'] . "\n";
echo "Task ID: " . $data['task_id'];
```
</RequestExample>

<ResponseExample>
```json Response
{
  "task_id": "video_abc123",
  "poll_url": "/v1/tasks/video_abc123",
  "status": "pending",
  "model": "sora-2",
  "created_at": 1706000000
}
```
</ResponseExample>

## Image sang Video

```python
response = requests.post(
    "https://api.lemondata.cc/v1/videos/generations",
    headers={"Authorization": "Bearer sk-your-api-key"},
    json={
        "model": "hailuo-2.3-fast",
        "prompt": "The scene begins from the provided image and adds gentle natural motion.",
        "operation": "image-to-video",
        "image_url": "https://example.com/image.jpg",
        "duration": 6,
        "aspect_ratio": "16:9"
    }
)
```

## Điều khiển Keyframe

Sử dụng `start_image` và `end_image` để điều khiển khung hình đầu tiên và cuối cùng (được hỗ trợ bởi Vidu Q2 Pro):

```python
response = requests.post(
    "https://api.lemondata.cc/v1/videos/generations",
    headers={"Authorization": "Bearer sk-your-api-key"},
    json={
        "model": "viduq2-pro",
        "prompt": "Smooth transition from day to night",
        "operation": "Đầu-cuối sang video",
        "start_image": "https://example.com/day.jpg",
        "end_image": "https://example.com/night.jpg",
        "duration": 5,
        "resolution": "720p",
        "aspect_ratio": "16:9"
    }
)
```

## Các Model Khả Dụng

<Info>
Danh sách model được cập nhật thường xuyên. Để xem các model và mức giá mới nhất hiện có, hãy truy cập [trang Models](https://lemondata.cc/vi/models).
</Info>

Để xem hướng dẫn đầy đủ với so sánh khả năng và ví dụ sử dụng, hãy xem [Hướng dẫn Sinh Video](/guides/video-generation).

## Các model video công khai đang được bật

<Info>
Danh sách này được đồng bộ với inventory model video công khai đang bật trong bản build tài liệu này. Để có trạng thái mới nhất, hãy dùng [Models API](/api-reference/models/list-models).
</Info>

### OpenAI

| Model | Thao tác công khai |
|-------|-------------------|
| `sora-2` | Văn bản sang video, image-to-video |
| `sora-2-pro` | Văn bản sang video, image-to-video |
| `sora-2-pro-storyboard` | Hình ảnh sang video |

### Kuaishou

| Model | Thao tác công khai |
|-------|-------------------|
| `kling-3.0-motion-control` | Điều khiển chuyển động |
| `kling-3.0-video` | Văn bản sang video, image-to-video, Đầu-cuối sang video |
| `kling-v2.5-turbo-pro` | Văn bản sang video, image-to-video, Đầu-cuối sang video |
| `kling-v2.5-turbo-std` | Văn bản sang video, image-to-video |
| `kling-v2.6-pro` | Văn bản sang video, image-to-video, Đầu-cuối sang video |
| `kling-v2.6-std` | Văn bản sang video, image-to-video |
| `kling-v3.0-pro` | Văn bản sang video, image-to-video, Đầu-cuối sang video |
| `kling-v3.0-std` | Văn bản sang video, image-to-video, Đầu-cuối sang video |
| `kling-video-o1-pro` | Văn bản sang video, image-to-video, reference-to-video, Đầu-cuối sang video, video-to-video |
| `kling-video-o1-std` | Văn bản sang video, image-to-video, reference-to-video, Đầu-cuối sang video, video-to-video |

### Google

| Model | Thao tác công khai |
|-------|-------------------|
| `veo3` | Văn bản sang video, image-to-video |
| `veo3-fast` | Văn bản sang video, image-to-video |
| `veo3-pro` | Văn bản sang video, image-to-video |
| `veo3.1` | Văn bản sang video, image-to-video, reference-to-video, Đầu-cuối sang video |
| `veo3.1-fast` | Văn bản sang video, image-to-video, reference-to-video, Đầu-cuối sang video |
| `veo3.1-pro` | Văn bản sang video, image-to-video, Đầu-cuối sang video |

### ByteDance

| Model | Thao tác công khai |
|-------|-------------------|
| `seedance-1.5-pro` | Văn bản sang video, image-to-video |

### MiniMax

| Model | Thao tác công khai |
|-------|-------------------|
| `hailuo-2.3-fast` | Hình ảnh sang video |
| `hailuo-2.3-pro` | Văn bản sang video, image-to-video |
| `hailuo-2.3-standard` | Văn bản sang video, image-to-video |

### Alibaba

| Model | Thao tác công khai |
|-------|-------------------|
| `wan-2.2-plus` | Văn bản sang video, image-to-video |
| `wan-2.5` | Văn bản sang video, image-to-video |
| `wan-2.6` | Văn bản sang video, image-to-video, reference-to-video |

### Shengshu

| Model | Thao tác công khai |
|-------|-------------------|
| `viduq2` | Văn bản sang video, reference-to-video |
| `viduq2-pro` | Hình ảnh sang video, reference-to-video, Đầu-cuối sang video |
| `viduq2-pro-fast` | Hình ảnh sang video, Đầu-cuối sang video |
| `viduq2-turbo` | Hình ảnh sang video, Đầu-cuối sang video |
| `viduq3-pro` | Văn bản sang video, image-to-video, Đầu-cuối sang video |
| `viduq3-turbo` | Văn bản sang video, image-to-video, Đầu-cuối sang video |

### xAI

| Model | Thao tác công khai |
|-------|-------------------|
| `grok-imagine-image-to-video` | Hình ảnh sang video |
| `grok-imagine-text-to-video` | Văn bản sang video |
| `grok-imagine-upscale` | Video sang video |


### Other

| Model | Thao tác công khai |
|-------|-------------------|
| `topaz-video-upscale` | Video sang video |
