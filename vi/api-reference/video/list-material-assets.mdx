---
title: "Liệt kê tài nguyên vật liệu"
openapi: "GET /v1/videos/assets"
description: "Liệt kê các tài nguyên vật liệu Seedance thuộc sở hữu của tổ chức hiện tại."
---

Liệt kê các tài nguyên vật liệu thuộc sở hữu của tổ chức đã xác thực. Lọc theo `library_type`, `group_id`, hoặc `status` khi bạn cần tìm tài nguyên cho một quy trình tạo cụ thể.

Chỉ sử dụng các tài nguyên có `status` là `ACTIVE`. Hãy coi `asset_url` là thông tin nguồn tải lên hoặc thông tin kiểm tra, không phải là địa chỉ vật liệu dài hạn.

## Ví dụ yêu cầu

```bash
curl "https://api.tokenlab.sh/v1/videos/assets?library_type=aigc_avatar&group_id=seedance_group_virtual_123&page_size=20" \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```

## Ví dụ phản hồi

```json
{
  "assets": [
    {
      "id": "seedance_asset_123",
      "object": "seedance.material_asset",
      "group_id": "seedance_group_virtual_123",
      "library_type": "aigc_avatar",
      "asset_name": "Front view",
      "asset_type": "Image",
      "status": "ACTIVE",
      "ready": true,
      "created_at": 1782998400000
    }
  ],
  "next_page_token": null
}
```