---
title: "Tạo phiên xác thực người thật"
openapi: "POST /v1/videos/assets/visual-validation-sessions"
description: "Tạo phiên H5 xác thực tư liệu người thật của TokenLab."
---

Tạo một phiên xác thực có thời hạn ngắn và chuyển hướng người dùng của bạn đến liên kết H5 được trả về. TokenLab chỉ trả về các trường trung lập: `session_id`, `h5_link`, `callback_url`, `expires_in` và `language`.

Phiên làm việc và liên kết H5 sẽ hết hạn sau 120 giây. Token liên kết chỉ được cung cấp qua chuyển hướng callback của TokenLab. Khi quy trình H5 hoàn tất, TokenLab sẽ chuyển hướng đến `callback_url` ban đầu của bạn cùng với các tham số truy vấn sau:

| Tham số | Ý nghĩa |
|-----------|---------|
| `session_id` | ID phiên xác thực của TokenLab. |
| `verification_token` | Token ẩn dùng để liên kết kết quả. |
| `verification_status` | `succeeded` hoặc `failed`. |
| `result_code` | Mã kết quả cho logic ứng dụng của bạn. |

Nếu `verification_status` là `succeeded`, hãy gọi [Liên kết kết quả xác thực người thật](/vi/api-reference/video/bind-real-person-verification-result). Bạn có thể sử dụng [bảng điều khiển kiểm thử](https://tokenlab.sh/dashboard/seedance-assets) để xác minh việc xử lý callback trước khi tích hợp quy trình này vào sản phẩm của mình.

## Ví dụ yêu cầu

```bash
curl https://api.tokenlab.sh/v1/videos/assets/visual-validation-sessions \
  -H "Authorization: Bearer $TOKENLAB_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"callback_url":"https://yourapp.example.com/seedance/callback","language":"en"}'
```

## Ví dụ phản hồi

```json
{
  "object": "seedance.visual_validation_session",
  "session_id": "svv_abc123",
  "h5_link": "https://verify.example.com/h5?lng=en",
  "callback_url": "https://yourapp.example.com/seedance/callback",
  "expires_in": 120,
  "language": "en"
}
```