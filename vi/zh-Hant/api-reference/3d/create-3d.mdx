---
title: "Tạo mô hình 3D"
openapi: "POST /v1/3d/generations"
description: "Tạo tác vụ tạo mô hình 3D"
---

Sử dụng Tripo3D và các nhà cung cấp khác để tạo mô hình 3D từ văn bản hoặc hình ảnh. Đây là một API bất đồng bộ.

## Request Body

<ParamField body="model" type="string" default="tripo3d-v2.5">
  Mô hình cần sử dụng (ví dụ: `tripo3d-v2.5`, `tripo3d-v2`).
</ParamField>

<ParamField body="prompt" type="string" required>
  Mô tả văn bản cho mô hình 3D cần tạo.
</ParamField>

<ParamField body="image" type="string">
  Hình ảnh mã hóa Base64 được sử dụng để tạo 3D từ hình ảnh.
</ParamField>

<ParamField body="image_url" type="string">
  URL hình ảnh được sử dụng để tạo 3D từ hình ảnh.
</ParamField>

<ParamField body="format" type="string" default="glb">
  Định dạng đầu ra: `glb`, `fbx`, `obj` hoặc `usdz`.
</ParamField>

<ParamField body="quality" type="string" default="standard">
  Mức chất lượng: `draft`, `standard` hoặc `high`.
</ParamField>

<ParamField body="style" type="string">
  Cài đặt trước phong cách của mô hình.
</ParamField>

<ParamField body="seed" type="integer">
  Giá trị seed được sử dụng để tạo kết quả có thể tái lập.
</ParamField>

<ParamField body="user" type="string">
  Mã định danh duy nhất của người dùng cuối.
</ParamField>

## Phản hồi (Response)

<ResponseField name="id" type="string">
  ID tác vụ được sử dụng để kiểm tra trạng thái (polling).
</ResponseField>

<ResponseField name="status" type="string">
  Trạng thái tác vụ: `pending`, `processing`, `completed` hoặc `failed`.
</ResponseField>

<ResponseField name="created" type="integer">
  Dấu thời gian Unix khi tác vụ được tạo.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST "https://api.lemondata.cc/v1/3d/generations" \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "tripo3d-v2.5",
    "prompt": "A detailed medieval castle with towers",
    "format": "glb",
    "quality": "high"
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.lemondata.cc/v1/3d/generations",
    headers={"Authorization": "Bearer sk-your-api-key"},
    json={
        "model": "tripo3d-v2.5",
        "prompt": "A detailed medieval castle with towers",
        "format": "glb",
        "quality": "high"
    }
)

task_id = response.json()["id"]
print(f"Task ID: {task_id}")
```

```javascript JavaScript
const response = await fetch('https://api.lemondata.cc/v1/3d/generations', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer sk-your-api-key',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'tripo3d-v2.5',
    prompt: 'A detailed medieval castle with towers',
    format: 'glb',
    quality: 'high'
  })
});

const data = await response.json();
console.log(`Task ID: ${data.id}`);
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "net/http"
)

func main() {
    payload := map[string]interface{}{
        "model":   "tripo3d-v2.5",
        "prompt":  "A detailed medieval castle with towers",
        "format":  "glb",
        "quality": "high",
    }
    body, _ := json.Marshal(payload)

    req, _ := http.NewRequest("POST", "https://api.lemondata.cc/v1/3d/generations", bytes.NewBuffer(body))
    req.Header.Set("Authorization", "Bearer sk-your-api-key")
    req.Header.Set("Content-Type", "application/json")

    client := &http.Client{}
    resp, _ := client.Do(req)
    defer resp.Body.Close()

    var result map[string]interface{}
    json.NewDecoder(resp.Body).Decode(&result)
    fmt.Printf("Task ID: %s\n", result["id"])
}
```

```php PHP
<?php
$ch = curl_init('https://api.lemondata.cc/v1/3d/generations');

curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_HTTPHEADER => [
        'Content-Type: application/json',
        'Authorization: Bearer sk-your-api-key'
    ],
    CURLOPT_POSTFIELDS => json_encode([
        'model' => 'tripo3d-v2.5',
        'prompt' => 'A detailed medieval castle with towers',
        'format' => 'glb',
        'quality' => 'high'
    ])
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
echo "Task ID: " . $data['id'];
```
</RequestExample>

<ResponseExample>
```json Response
{
  "id": "3d_abc123",
  "status": "pending",
  "created": 1706000000,
  "model": "tripo3d-v2.5"
}
```
</ResponseExample>