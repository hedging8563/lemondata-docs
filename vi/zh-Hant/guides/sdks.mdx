---
title: "SDK và Thư viện"
description: "SDK chính thức và cộng đồng của LemonData"
---

## Tổng quan

LemonData **tương thích với OpenAI**, điều này có nghĩa là bạn chỉ cần thay đổi base URL để sử dụng bất kỳ OpenAI SDK nào. Chúng tôi cũng hỗ trợ các SDK Anthropic và Google Gemini gốc.

## SDK chính thức

Vì LemonData tương thích với OpenAI, vui lòng sử dụng OpenAI SDK chính thức:

<CardGroup cols={2}>
  <Card title="Python" icon="python" href="https://github.com/openai/openai-python">
    `pip install openai`
  </Card>
  <Card title="Node.js" icon="node-js" href="https://github.com/openai/openai-node">
    `npm install openai`
  </Card>
  <Card title="Go" icon="golang" href="https://github.com/sashabaranov/go-openai">
    `go get github.com/sashabaranov/go-openai`
  </Card>
  <Card title=".NET" icon="microsoft" href="https://github.com/openai/openai-dotnet">
    `dotnet add package OpenAI`
  </Card>
</CardGroup>

## Cấu hình

### Python

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.lemondata.cc/v1"
)

# Sử dụng bất kỳ mô hình nào
response = client.chat.completions.create(
    model="gpt-4o",  # Hoặc claude-sonnet-4-5, gemini-2.5-flash, v.v.
    messages=[{"role": "user", "content": "Hello!"}]
)
```

### JavaScript / TypeScript

```javascript
import OpenAI from 'openai';

const client = new OpenAI({
  apiKey: 'sk-your-api-key',
  baseURL: 'https://api.lemondata.cc/v1'
});

const response = await client.chat.completions.create({
  model: 'gpt-4o',
  messages: [{ role: 'user', content: 'Hello!' }]
});
```

### Go

```go
package main

import (
    "context"
    "fmt"
    openai "github.com/sashabaranov/go-openai"
)

func main() {
    config := openai.DefaultConfig("sk-your-api-key")
    config.BaseURL = "https://api.lemondata.cc/v1"

    client := openai.NewClientWithConfig(config)

    resp, err := client.CreateChatCompletion(
        context.Background(),
        openai.ChatCompletionRequest{
            Model: "gpt-4o",
            Messages: []openai.ChatCompletionMessage{
                {Role: "user", Content: "Hello!"},
            },
        },
    )
    if err != nil {
        panic(err)
    }
    fmt.Println(resp.Choices[0].Message.Content)
}
```

### .NET / C#

```csharp
using OpenAI;

var options = new OpenAIClientOptions
{
    Endpoint = new Uri("https://api.lemondata.cc/v1")
};

var client = new OpenAIClient("sk-your-api-key", options);

var chat = client.GetChatClient("gpt-4o");
var response = await chat.CompleteChatAsync("Hello!");

Console.WriteLine(response.Value.Content[0].Text);
```

### cURL

```bash
curl https://api.lemondata.cc/v1/chat/completions \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o",
    "messages": [{"role": "user", "content": "Hello!"}]
  }'
```

## Biến môi trường

Chúng tôi khuyên bạn nên sử dụng các biến môi trường để quản lý API key:

```bash
# Tệp cấu hình .env hoặc shell
export LEMONDATA_API_KEY="sk-your-api-key"
export LEMONDATA_BASE_URL="https://api.lemondata.cc/v1"
```

```python
import os
from openai import OpenAI

client = OpenAI(
    api_key=os.environ.get("LEMONDATA_API_KEY"),
    base_url=os.environ.get("LEMONDATA_BASE_URL")
)
```

## Anthropic SDK

Đối với các yêu cầu Anthropic gốc, vui lòng sử dụng Anthropic SDK:

```python
from anthropic import Anthropic

client = Anthropic(
    api_key="sk-your-api-key",
    base_url="https://api.lemondata.cc"
)

message = client.messages.create(
    model="claude-sonnet-4-5",
    max_tokens=1024,
    messages=[{"role": "user", "content": "Hello!"}]
)
```

## Tích hợp LangChain

Vui lòng tham khảo hướng dẫn [Tích hợp LangChain](/integrations/langchain) để biết thêm về việc tích hợp framework.