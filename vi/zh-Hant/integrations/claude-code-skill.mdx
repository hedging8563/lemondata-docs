---
title: "✨ Kỹ năng Claude Code"
description: "Hướng dẫn bắt đầu nhanh trong 5 phút - Để Claude Code tự động tích hợp bất kỳ LemonData API nào cho bạn"
---

<Note>
Kỹ năng này cho phép bạn tích hợp bất kỳ API nào trong số **hàng trăm AI API** của LemonData chỉ bằng cách mô tả nhu cầu của mình bằng ngôn ngữ tự nhiên đơn giản!
</Note>

## Kỹ năng này có thể làm gì?

Khi bạn muốn sử dụng các tính năng AI trong mã nguồn của mình (như GPT-4, tạo hình ảnh, nhận dạng giọng nói, v.v.), kỹ năng này sẽ:

1. ✅ **Tự động tìm kiếm** hàng trăm API của LemonData
2. ✅ **Tìm thấy API phù hợp nhất** với nhu cầu của bạn
3. ✅ **Tạo mã nguồn hoàn chỉnh, có thể chạy được**
4. ✅ **Cấu hình API Key của bạn**
5. ✅ **Cung cấp ví dụ sử dụng** và các thực hành tốt nhất (best practices)

**Nói tóm lại: Chỉ cần mô tả nhu cầu của bạn bằng ngôn ngữ tự nhiên để nhận mã tích hợp API hoàn chỉnh!**

## Bước 1: Cài đặt kỹ năng

<Tabs>
  <Tab title="npx (Khuyên dùng)">
    Cách cài đặt đơn giản nhất:

    ```bash
    npx add-skill hedging8563/lemondata-api-skill -y
    ```

    Điều này sẽ tự động cài đặt kỹ năng này cho tất cả các trợ lý lập trình được phát hiện (Claude Code, Cursor, Copilot, v.v.).
  </Tab>
  <Tab title="Chia sẻ URL GitHub">
    1. Mở **Claude Code** trong terminal hoặc IDE của bạn
    2. Dán liên kết này vào cuộc hội thoại:
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
    3. Claude Code sẽ nhận diện đây là một kho lưu trữ kỹ năng và hỗ trợ bạn cài đặt
  </Tab>
  <Tab title="Git Clone">
    **Cài đặt cá nhân** (áp dụng cho tất cả dự án):
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git ~/.claude/skills/lemondata-api-integration
    ```

    **Cài đặt cho dự án cụ thể** (chia sẻ với nhóm qua git):
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git .claude/skills/lemondata-api-integration
    ```
  </Tab>
</Tabs>

### Xác minh cài đặt

Hỏi Claude Code:
```
What skills are available?
```

Nếu bạn thấy `lemondata-api-integration`, nghĩa là cài đặt đã thành công!

## Bước 2: Lấy API Key của bạn

Trước khi sử dụng kỹ năng này, bạn cần một LemonData API Key.

<Tip>
**Bạn không cần cấu hình API Key trước!** Khi bạn sử dụng kỹ năng này, Claude Code sẽ tự động hỏi API Key của bạn và đưa nó vào mã nguồn được tạo ra.
</Tip>

<Steps>
  <Step title="Truy cập LemonData">
    Truy cập [lemondata.cc](https://lemondata.cc)
  </Step>
  <Step title="Đăng nhập">
    Tạo tài khoản hoặc đăng nhập
  </Step>
  <Step title="Lấy API Key">
    Điều hướng đến [Dashboard → API Keys](https://lemondata.cc/dashboard/api) và tạo một khóa mới
  </Step>
  <Step title="Sao chép khóa">
    Khóa của bạn bắt đầu bằng `sk-...` - vui lòng lưu trữ nó an toàn
  </Step>
</Steps>

<Warning>
**Lưu ý quan trọng:**
- Tất cả LemonData API đều sử dụng cùng một API Key
- Không chia sẻ khóa của bạn với người khác
- Không commit khóa của bạn lên GitHub
</Warning>

## Bước 3: Bắt đầu sử dụng

Sử dụng kỹ năng này đơn giản như đang trò chuyện!

### Ví dụ 1: Sử dụng GPT-4

**Bạn nói:**
```
I want to use GPT-4 in my Python project
```

**Claude Code sẽ:**
1. Hỏi API Key của bạn
2. Tìm kiếm các API liên quan đến GPT-4
3. Hiển thị các tùy chọn API có sẵn
4. Tạo mã Python hoàn chỉnh
5. Giải thích cách sử dụng nó

**Bạn nhận được:**
```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.lemondata.cc/v1"
)

response = client.chat.completions.create(
    model="gpt-4o",
    messages=[{"role": "user", "content": "Hello!"}]
)

print(response.choices[0].message.content)
```

### Ví dụ 2: Tạo hình ảnh

**Bạn nói:**
```
How do I generate images with Flux in Node.js?
```

**Claude Code sẽ:**
- Tìm kiếm API tạo hình ảnh
- Tạo mã JavaScript
- Bao gồm mã tải xuống và lưu hình ảnh

### Ví dụ 3: Nhận dạng giọng nói

**Bạn nói:**
```
Integrate a speech-to-text API
```

**Claude Code sẽ:**
- Hỏi ngôn ngữ lập trình bạn đang sử dụng
- Tìm kiếm API nhận dạng giọng nói
- Tạo mã bằng ngôn ngữ bạn đã chọn
- Cung cấp ví dụ xử lý tệp âm thanh

## Các tính năng được hỗ trợ

Kỹ năng này có thể giúp bạn tích hợp tất cả các khả năng AI sau:

| Loại tính năng | Ví dụ |
|-------------|----------|
| 💬 Trò chuyện | GPT-4o, Claude, Gemini, DeepSeek |
| 🎨 Tạo hình ảnh | Midjourney, Flux, Stable Diffusion |
| 🎬 Tạo video | Sora, Runway, Kling, Luma AI |
| 🎵 Tạo nhạc | Suno |
| 🗿 Mô hình 3D | Tripo3D |
| 🎤 Âm thanh | Văn bản thành giọng nói (TTS), Giọng nói thành văn bản (STT) |
| 📊 Vector embedding | text-embedding-3 |
| 🔄 Reranking | bce-reranker, qwen3-rerank |

## Mẹo sử dụng

### 1. Nêu trực tiếp nhu cầu của bạn

<Tabs>
  <Tab title="❌ Không nên làm thế này">
    ```
    Help me check if LemonData has an image generation API
    ```
  </Tab>
  <Tab title="✅ Nên làm thế này">
    ```
    I want to generate images in Python
    ```
  </Tab>
</Tabs>

### 2. Chỉ định ngôn ngữ của bạn

Nếu bạn có yêu cầu về ngôn ngữ cụ thể, hãy nêu trực tiếp:

```
Use JavaScript to call GPT-4
Use Python for speech recognition
Use Go to generate images
```

### 3. Mô tả ngữ cảnh của bạn

Nếu bạn có nhu cầu cụ thể, vui lòng mô tả chi tiết:

```
I'm building a chatbot, need to use GPT-4
My website needs to let users upload and transform images
I'm making a voice notes app, need speech-to-text
```

## Lưu ý quan trọng

### Bảo mật API Key

<CardGroup cols={2}>
  <Card title="❌ Tuyệt đối không làm thế này" icon="xmark" color="#ef4444">
    - Đặt API Key trực tiếp trong mã nguồn front-end
    - Commit API Key lên GitHub
    - Chia sẻ API Key với người khác
  </Card>
  <Card title="✅ Thực hành tốt nhất" icon="check" color="#22c55e">
    - Sử dụng biến môi trường
    - Đối với trang web, gọi API từ back-end
    - Sử dụng các framework như Next.js, Express, v.v.
  </Card>
</CardGroup>

### Quản lý chi phí

- Mỗi lần gọi API sẽ tiêu tốn hạn mức (credit)
- Thiết lập giới hạn sử dụng trong [Dashboard](https://lemondata.cc/dashboard) của bạn
- Sử dụng lượng dữ liệu nhỏ khi thử nghiệm

### Xử lý lỗi

Mã nguồn được tạo ra bao gồm xử lý lỗi cơ bản, nhưng bạn có thể cần thêm:
- Thêm cơ chế thử lại (retry)
- Ghi nhật ký lỗi (log)
- Cung cấp thông báo lỗi thân thiện với người dùng

## Câu hỏi thường gặp (FAQ)

<AccordionGroup>
  <Accordion title="Kỹ năng không tự động kích hoạt?">
    Thử đề cập đến "LemonData" hoặc "LemonData API" trong yêu cầu, hoặc mô tả nhu cầu của bạn cụ thể hơn:
    ```
    Use LemonData to integrate GPT-4 in my project
    ```
  </Accordion>

  <Accordion title="Không tìm thấy API tôi muốn?">
    Thử các từ khóa khác nhau:
    - "chat" → "dialogue" hoặc "conversation"
    - "image" → "picture" hoặc "generation"
    - "voice" → "audio" hoặc "speech"
  </Accordion>

  <Accordion title="Mã được tạo có lỗi?">
    Kiểm tra các điểm sau:
    1. API Key có chính xác không?
    2. Đã cài đặt các thư viện cần thiết chưa? (Ví dụ: `openai` cho Python)
    3. Bạn có thể truy cập máy chủ của LemonData không?
    4. API Key của bạn có đủ hạn mức không?
  </Accordion>

  <Accordion title="Làm thế nào để cập nhật kỹ năng?">
    Gửi lại liên kết GitHub cho Claude Code:
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
  </Accordion>
</AccordionGroup>

## Tài nguyên

<CardGroup cols={2}>
  <Card title="Trang web LemonData" icon="globe" href="https://lemondata.cc">
    Trang web chính và Dashboard
  </Card>
  <Card title="Tài liệu API" icon="book" href="https://docs.lemondata.cc">
    Hướng dẫn tham chiếu API đầy đủ
  </Card>
  <Card title="Mô hình" icon="robot" href="https://lemondata.cc/zh-TW/models">
    Duyệt tất cả các mô hình có sẵn
  </Card>
  <Card title="Giá cả" icon="dollar-sign" href="https://lemondata.cc/#pricing">
    Thấp hơn 30% so với giá chính thức
  </Card>
</CardGroup>

## Bắt đầu ngay!

Bây giờ bạn đã biết cách sử dụng kỹ năng này. Hãy thử hỏi Claude Code:

```
I want to use GPT-4 in my project
```

Hoặc

```
Help me integrate image generation
```

Claude Code sẽ tự động sử dụng kỹ năng này để hoàn thành mọi việc cho bạn!

<Info>
**Có bất kỳ câu hỏi nào không?** Vui lòng kiểm tra [GitHub Issues](https://github.com/hedging8563/lemondata-api-skill/issues) của chúng tôi hoặc liên hệ [support@lemondata.cc](mailto:support@lemondata.cc)
</Info>