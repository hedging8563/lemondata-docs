---
title: "DingTalk"
description: "Kết nối instance Claw của bạn với DingTalk bằng chế độ Stream"
---

Kết nối DingTalk (钉钉) với instance Claw của bạn bằng chế độ Stream — một kết nối dựa trên WebSocket không yêu cầu URL công khai.

## Điều kiện tiên quyết

- Một tài khoản [DingTalk](https://www.dingtalk.com/) với quyền quản trị viên doanh nghiệp
- Một instance Claw ([tạo tại đây](https://lemondata.cc/dashboard/claw))

## Hướng dẫn thiết lập

<Steps>
  <Step title="Tạo ứng dụng DingTalk">
    1. Truy cập [DingTalk Open Platform](https://open-dev.dingtalk.com/)
    2. Nhấp vào **Application Development** → **Enterprise Internal Development**
    3. Nhấp vào **Create Application**
    4. Nhập tên và mô tả ứng dụng
    5. Nhấp vào **Confirm**
  </Step>
  <Step title="Bật tính năng Robot">
    1. Trong ứng dụng của bạn, đi tới **App Features** → **Robot**
    2. Bật cấu hình robot
    3. Chọn **Stream Mode** (khuyên dùng — không cần URL công khai)
    4. Nhấp vào **Publish**

    <Tip>
    Chế độ Stream sử dụng WebSocket, vì vậy instance Claw của bạn không cần URL webhook có thể truy cập công khai. Đây là cách thiết lập đơn giản nhất.
    </Tip>
  </Step>
  <Step title="Lấy thông tin xác thực">
    1. Đi tới **Basic Information** → **Credentials**
    2. Sao chép **AppKey** và **AppSecret**
  </Step>
  <Step title="Xuất bản ứng dụng">
    1. Đi tới **Version Management**
    2. Tạo một phiên bản mới (tối thiểu là phiên bản thử nghiệm)
    3. Thiết lập phạm vi hiển thị (người dùng/phòng ban nào có thể thấy bot)
    4. Gửi và xuất bản
  </Step>
  <Step title="Cấu hình trong LemonData Dashboard">
    1. Truy cập [Claw Dashboard](https://lemondata.cc/dashboard/claw) của bạn
    2. Nhấp vào **biểu tượng cài đặt** trên instance của bạn
    3. Mở tab **IM Integrations**
    4. Mở rộng phần **DingTalk**
    5. Dán **AppKey** và **AppSecret** của bạn
    6. Nhấp vào **Save**
  </Step>
  <Step title="Bắt đầu trò chuyện">
    1. Trong DingTalk, tìm kiếm bot của bạn theo tên
    2. Gửi cho nó một tin nhắn
    3. Hoàn tất quá trình ghép nối
    4. Bắt đầu trò chuyện với AI!
  </Step>
</Steps>

## Khắc phục sự cố

<AccordionGroup>
  <Accordion title="Bot không phản hồi">
    - Xác minh AppKey và AppSecret là chính xác
    - Kiểm tra xem ứng dụng đã được xuất bản chưa (ít nhất là phiên bản thử nghiệm)
    - Đảm bảo chế độ Stream đã được chọn trong cấu hình robot
    - Xác minh bot hiển thị với tài khoản người dùng của bạn
  </Accordion>
  <Accordion title="Kết nối thường xuyên bị ngắt">
    - Chế độ Stream duy trì kết nối WebSocket liên tục
    - Việc ngắt kết nối ngắn là bình thường — bot sẽ tự động kết nối lại
    - Nếu tình trạng tiếp diễn, hãy thử khởi động lại instance từ dashboard
  </Accordion>
</AccordionGroup>