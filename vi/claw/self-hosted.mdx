---
title: "Cài đặt tự lưu trữ"
description: "Chạy LemonClaw trên máy chủ của bạn với LemonData AI API — một lệnh, không cần Docker"
---

<Note>
**Tự lưu trữ** nghĩa là bạn chạy LemonClaw trực tiếp trên VPS hoặc máy tính của mình. Bạn chỉ trả phí sử dụng mô hình AI thông qua số dư LemonData — không có phí phiên bản.
</Note>

## Cài đặt nhanh

<CodeGroup>
```bash Linux / macOS
curl -fsSL https://raw.githubusercontent.com/hedging8563/lemonclaw/main/deploy/self-hosted/install.sh | bash
```

```bash Non-interactive
LEMONDATA_API_KEY=sk-xxx curl -fsSL https://raw.githubusercontent.com/hedging8563/lemonclaw/main/deploy/self-hosted/install.sh | bash
```
</CodeGroup>

Trình cài đặt sẽ yêu cầu bạn nhập **LemonData API Key**. Lấy một key từ [Bảng điều khiển](https://lemondata.cc/dashboard).

## Những gì được cài đặt

<Steps>
  <Step title="Node.js 22+">
    Tự động cài đặt nếu chưa có (qua NodeSource trên Linux, Homebrew trên macOS)
  </Step>
  <Step title="LemonClaw">
    Gói LemonClaw chính thức từ npm, được cấu hình sẵn với LemonData API
  </Step>
  <Step title="15 mô hình AI">
    Claude, GPT-5, Gemini, DeepSeek, và nhiều hơn nữa — sẵn sàng sử dụng
  </Step>
  <Step title="Plugin nhắn tin">
    Plugin DingTalk, WeCom và QQ được cài đặt sẵn
  </Step>
  <Step title="Dịch vụ hệ thống">
    Tự động khởi động cùng hệ thống (systemd trên Linux, launchd trên macOS)
  </Step>
</Steps>

## Yêu cầu

| Yêu cầu | Chi tiết |
|----------|----------|
| **Hệ điều hành** | Linux (Ubuntu/Debian/CentOS/RHEL) hoặc macOS |
| **Kiến trúc** | x64 hoặc arm64 |
| **RAM** | Tối thiểu 1 GB, khuyến nghị 2 GB |
| **API Key** | [Lấy tại đây](https://lemondata.cc) |

## Sau khi cài đặt

Trình cài đặt hiển thị thông tin đăng nhập của bạn:

```
✓ LemonClaw installed successfully!

  WebUI:        http://your-ip:18789
  Token:        your-gateway-token
  Auto-login:   http://your-ip:18789?token=your-gateway-token
  Model:        claude-sonnet-4-6
```

<Warning>
**Lưu lại Token của bạn!** Bạn cần nó để truy cập WebUI và kết nối các nền tảng nhắn tin.
</Warning>

## Kết nối nền tảng nhắn tin

Truy cập WebUI tại `http://your-ip:18789` bằng Token của bạn, sau đó làm theo hướng dẫn cho từng nền tảng:

<CardGroup cols={2}>
  <Card title="Telegram" icon="paper-plane" href="/claw/telegram" />
  <Card title="Discord" icon="discord" href="/claw/discord" />
  <Card title="WhatsApp" icon="whatsapp" href="/claw/whatsapp" />
  <Card title="Slack" icon="slack" href="/claw/slack" />
  <Card title="DingTalk" icon="bell" href="/claw/dingtalk" />
  <Card title="WeCom" icon="building" href="/claw/wecom" />
</CardGroup>

## Lệnh quản lý

<Tabs>
  <Tab title="Linux">
    ```bash
    systemctl --user status lemonclaw    # Kiểm tra trạng thái
    journalctl --user -u lemonclaw -f    # Xem nhật ký
    systemctl --user restart lemonclaw   # Khởi động lại
    systemctl --user stop lemonclaw      # Dừng
    ```
  </Tab>
  <Tab title="macOS">
    ```bash
    tail -f ~/.lemonclaw/lemonclaw.log    # Xem nhật ký
    launchctl stop cc.lemondata.lemonclaw && launchctl start cc.lemondata.lemonclaw  # Khởi động lại
    launchctl stop cc.lemondata.lemonclaw  # Dừng
    ```
  </Tab>
</Tabs>

## Cấu hình

Vị trí tệp cấu hình: `~/.lemonclaw/config.json`

Sau khi chỉnh sửa, khởi động lại dịch vụ để áp dụng thay đổi.

### Thay đổi mô hình mặc định

```json
{
  "agents": {
    "defaults": {
      "model": {
        "primary": "lemondata-claude/claude-opus-4-6"
      }
    }
  }
}
```

Các tiền tố mô hình khả dụng:
- `lemondata-claude/` — Các mô hình Claude (anthropic-messages API)
- `lemondata-minimax/` — Các mô hình MiniMax (anthropic-messages API)
- `lemondata/` — Tất cả các mô hình khác (openai-completions API)

## Nâng cấp

```bash
curl -fsSL https://raw.githubusercontent.com/hedging8563/lemonclaw/main/deploy/self-hosted/install.sh | bash
# Sau đó khởi động lại dịch vụ
systemctl --user restart lemonclaw  # Linux
# hoặc
launchctl stop cc.lemondata.lemonclaw && launchctl start cc.lemondata.lemonclaw  # macOS
```

## Gỡ cài đặt

```bash
# Dừng và vô hiệu hóa dịch vụ
systemctl --user stop lemonclaw && systemctl --user disable lemonclaw  # Linux
launchctl unload ~/Library/LaunchAgents/cc.lemondata.lemonclaw.plist  # macOS

# Xóa LemonClaw
rm -rf ~/.lemonclaw
rm -rf ~/.lemonclaw
```

## Tự lưu trữ so với Quản lý

| | Tự lưu trữ | Quản lý (Claw) |
|---|---|---|
| **Chi phí phiên bản** | Miễn phí | $20 / 30 ngày |
| **Sử dụng AI** | Trả theo dùng | Trả theo dùng |
| **Hạ tầng** | Máy chủ của bạn | Chúng tôi quản lý mọi thứ |
| **Cập nhật** | Thủ công (`npm update`) | Tự động |
| **Hỗ trợ** | Cộng đồng | Ưu tiên |

<Info>
**Mã nguồn**: [github.com/hedging8563/lemonclaw](https://github.com/hedging8563/lemonclaw)
</Info>
