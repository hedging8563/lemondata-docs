---
title: "Vercel AI SDK"
description: "Tích hợp LemonData với Vercel AI SDK bằng provider tương thích OpenAI hoặc provider Responses-native"
---

## Tổng quan

Đối với LemonData, lựa chọn mặc định ổn định nhất trong Vercel AI SDK là **provider tương thích OpenAI**.

Nếu bạn cần cụ thể hành vi **Responses-native**, bạn có thể chuyển sang provider OpenAI và giữ nguyên base URL của LemonData.

## Mặc định được khuyến nghị: Provider tương thích OpenAI

```bash
npm install ai @ai-sdk/openai-compatible
```

```typescript
import { createOpenAICompatible } from '@ai-sdk/openai-compatible';

export const lemondata = createOpenAICompatible({
  name: 'lemondata',
  apiKey: process.env.LEMONDATA_API_KEY,
  baseURL: 'https://api.lemondata.cc/v1',
});
```

## Tạo văn bản

```typescript
import { generateText } from 'ai';
import { lemondata } from './lemondata';

const { text } = await generateText({
  model: lemondata.chatModel('gpt-5.4'),
  prompt: 'Explain LemonData in one sentence.',
});

console.log(text);
```

## Stream văn bản

```typescript
import { streamText } from 'ai';
import { lemondata } from './lemondata';

const result = await streamText({
  model: lemondata.chatModel('gpt-5.4'),
  prompt: 'Write a short poem about coding.',
});

for await (const textPart of result.textStream) {
  process.stdout.write(textPart);
}
```

## Gọi công cụ

```typescript
import { generateText, tool } from 'ai';
import { z } from 'zod';
import { lemondata } from './lemondata';

const result = await generateText({
  model: lemondata.chatModel('gpt-5.4'),
  prompt: 'What is the weather in San Francisco?',
  tools: {
    weather: tool({
      description: 'Get weather in a location',
      parameters: z.object({
        location: z.string(),
      }),
      execute: async ({ location }) => ({
        location,
        temperature: 72,
        condition: 'sunny',
      }),
    }),
  },
});

console.log(result.text);
```

## Đầu ra có cấu trúc

```typescript
import { generateObject } from 'ai';
import { z } from 'zod';
import { lemondata } from './lemondata';

const { object } = await generateObject({
  model: lemondata.chatModel('gpt-5.4'),
  schema: z.object({
    name: z.string(),
    role: z.string(),
  }),
  prompt: 'Generate a fake developer profile.',
});

console.log(object);
```

## Nếu bạn thực sự cần hành vi Responses-native

```bash
npm install ai @ai-sdk/openai
```

```typescript
import { createOpenAI } from '@ai-sdk/openai';

export const lemondataResponses = createOpenAI({
  apiKey: process.env.LEMONDATA_API_KEY,
  baseURL: 'https://api.lemondata.cc/v1',
});
```

```typescript
import { generateText } from 'ai';
import { lemondataResponses } from './lemondata-responses';

const { text } = await generateText({
  model: lemondataResponses('gpt-5.4'),
  prompt: 'Explain LemonData in one sentence.',
});
```

<Note>
Sử dụng `@ai-sdk/openai-compatible` làm lựa chọn mặc định an toàn cho các tích hợp kiểu proxy. Chỉ chuyển sang `@ai-sdk/openai` khi bạn thực sự muốn đường dẫn provider được xây dựng trên `/v1/responses`.
</Note>

## Biến môi trường

```bash
# .env.local
LEMONDATA_API_KEY=sk-your-lemondata-key
```

## Thực tiễn tốt nhất

<AccordionGroup>
  <Accordion title="Ưu tiên openai-compatible trước">
    Đối với các gateway bên thứ ba và backend proxy, `@ai-sdk/openai-compatible` thường là điểm khởi đầu ít gây nhầm lẫn nhất.
  </Accordion>

  <Accordion title="Chỉ chuyển sang Responses khi cần">
    Nếu bạn cần hành vi provider gắn với `/v1/responses`, hãy chủ động chuyển package provider thay vì trộn cả hai mẫu trong cùng một client.
  </Accordion>

  <Accordion title="Giữ bí mật ở phía server">
    Không bao giờ để lộ API key LemonData trong code phía client. Hãy đặt phần thiết lập provider trong các file server hoặc API routes.
  </Accordion>
</AccordionGroup>