---
title: "Seedance Görevini İptal Et (Volc Uyumlu)"
openapi: "DELETE /api/v3/contents/generations/tasks/{id}"
description: "Hala iptal edilebilir durumdaki kuyruklanmış Seedance uyumlu görevi iptal eder."
---

## Genel bakış

Bu endpoint’ler, Volc tarzı Seedance görev şeklini zaten kullanan ekipler içindir. Kimlik doğrulama TokenLab Bearer API key ile yapılır. Dönen görev ID’si `cgt-` ile başlar; `status` `succeeded`, `failed`, `cancelled` veya `expired` olana kadar durumu sorgulayın.

Ayrıca bkz. [Seedance 2.0 Video Modelleri](/tr/guides/seedance-2-video) ve [Video Oluşturma](/tr/guides/video-generation).

## Yanıt şekli

Başarılı iptal isteği HTTP 200 ve boş body döndürür. Son durumu doğrulamak istiyorsanız ardından görevi sorgulayın.

## Örnek

```bash
curl -X DELETE https://api.tokenlab.sh/api/v3/contents/generations/tasks/cgt-0123456789abcdef0123456789abcdef \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```
