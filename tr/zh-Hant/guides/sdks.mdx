---
title: "SDK'lar ve Kütüphaneler"
description: "LemonData'nın resmi ve topluluk SDK'ları"
---

## Genel Bakış

LemonData **OpenAI ile uyumludur**, bu da herhangi bir OpenAI SDK'sını yalnızca base URL'yi değiştirerek kullanabileceğiniz anlamına gelir. Ayrıca yerel Anthropic ve Google Gemini SDK'larını da destekliyoruz.

## Resmi SDK'lar

LemonData OpenAI ile uyumlu olduğu için lütfen resmi OpenAI SDK'larını kullanın:

<CardGroup cols={2}>
  <Card title="Python" icon="python" href="https://github.com/openai/openai-python">
    `pip install openai`
  </Card>
  <Card title="Node.js" icon="node-js" href="https://github.com/openai/openai-node">
    `npm install openai`
  </Card>
  <Card title="Go" icon="golang" href="https://github.com/sashabaranov/go-openai">
    `go get github.com/sashabaranov/go-openai`
  </Card>
  <Card title=".NET" icon="microsoft" href="https://github.com/openai/openai-dotnet">
    `dotnet add package OpenAI`
  </Card>
</CardGroup>

## Yapılandırma

### Python

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.lemondata.cc/v1"
)

# 使用任何模型
response = client.chat.completions.create(
    model="gpt-4o",  # 或 claude-sonnet-4-5, gemini-2.5-flash 等
    messages=[{"role": "user", "content": "Hello!"}]
)
```

### JavaScript / TypeScript

```javascript
import OpenAI from 'openai';

const client = new OpenAI({
  apiKey: 'sk-your-api-key',
  baseURL: 'https://api.lemondata.cc/v1'
});

const response = await client.chat.completions.create({
  model: 'gpt-4o',
  messages: [{ role: 'user', content: 'Hello!' }]
});
```

### Go

```go
package main

import (
    "context"
    "fmt"
    openai "github.com/sashabaranov/go-openai"
)

func main() {
    config := openai.DefaultConfig("sk-your-api-key")
    config.BaseURL = "https://api.lemondata.cc/v1"

    client := openai.NewClientWithConfig(config)

    resp, err := client.CreateChatCompletion(
        context.Background(),
        openai.ChatCompletionRequest{
            Model: "gpt-4o",
            Messages: []openai.ChatCompletionMessage{
                {Role: "user", Content: "Hello!"},
            },
        },
    )
    if err != nil {
        panic(err)
    }
    fmt.Println(resp.Choices[0].Message.Content)
}
```

### .NET / C#

```csharp
using OpenAI;

var options = new OpenAIClientOptions
{
    Endpoint = new Uri("https://api.lemondata.cc/v1")
};

var client = new OpenAIClient("sk-your-api-key", options);

var chat = client.GetChatClient("gpt-4o");
var response = await chat.CompleteChatAsync("Hello!");

Console.WriteLine(response.Value.Content[0].Text);
```

### cURL

```bash
curl https://api.lemondata.cc/v1/chat/completions \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o",
    "messages": [{"role": "user", "content": "Hello!"}]
  }'
```

## Ortam Değişkenleri

API anahtarlarını yönetmek için ortam değişkenlerini kullanmanızı öneririz:

```bash
# .env 或 shell 設定檔
export LEMONDATA_API_KEY="sk-your-api-key"
export LEMONDATA_BASE_URL="https://api.lemondata.cc/v1"
```

```python
import os
from openai import OpenAI

client = OpenAI(
    api_key=os.environ.get("LEMONDATA_API_KEY"),
    base_url=os.environ.get("LEMONDATA_BASE_URL")
)
```

## Anthropic SDK

Yerel Anthropic istekleri için lütfen Anthropic SDK'sını kullanın:

```python
from anthropic import Anthropic

client = Anthropic(
    api_key="sk-your-api-key",
    base_url="https://api.lemondata.cc"
)

message = client.messages.create(
    model="claude-sonnet-4-5",
    max_tokens=1024,
    messages=[{"role": "user", "content": "Hello!"}]
)
```

## LangChain Entegrasyonu

Çerçeve entegrasyonu hakkında bilgi edinmek için lütfen [LangChain Entegrasyonu](/integrations/langchain) kılavuzuna bakın.