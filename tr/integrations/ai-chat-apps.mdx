---
title: "AI Sohbet Uygulamaları"
description: "Popüler AI sohbet uygulamalarını LemonData'nın OpenAI uyumlu API'sini kullanacak şekilde yapılandırın"
---

## Genel Bakış

Çoğu genel AI sohbet uygulaması, standart OpenAI uyumlu sohbet yolu üzerinden LemonData'ya bağlanır.

Pratikte bu şu anlama gelir:
- uygulama açıkça tam bir endpoint URL'si istemediği sürece base URL olarak `https://api.lemondata.cc/v1` kullanın
- OpenAI tarzı API key olarak LemonData API key'inizi kullanın
- uygulamanın standart bir sohbet iş akışında kullanabileceği modelleri seçin

<Note>
Genel sohbet uygulamaları genellikle **chat-completions odaklıdır**, Codex Responses WebSocket istemcileri değildir. Responses'a özgü davranış gerektiren Codex CLI/App, Cursor veya diğer kodlama araçları için bunun yerine özel entegrasyon kılavuzlarını kullanın.
</Note>

## Desteklenen Uygulamalar

| Uygulama | Platform | Notlar |
|-------------|----------|-------|
| LobeChat | Web, Masaüstü, Self-hosted | Güçlü özel sağlayıcı desteği |
| Cherry Studio | Windows, macOS, Linux | İyi OpenAI uyumlu sağlayıcı akışı |
| Chatbox | Windows, macOS, Linux | Basit OpenAI uyumlu kurulum |
| TypingMind | Web, Masaüstü | Özel bir endpoint stili kullanır |
| BotGem | iOS, macOS | Özel sağlayıcı yapılandırması |
| OpenCat | iOS, macOS | OpenAI uyumlu özel endpoint akışı |
| ChatWise | Windows, macOS | Standart özel sağlayıcı deseni |

## Varsayılan Bağlantı Değerleri

| Alan | Değer |
|-------|-------|
| Base URL | `https://api.lemondata.cc/v1` |
| API Key | `sk-your-lemondata-key` |
| API Type | OpenAI Compatible |

Bir uygulama base URL yerine **tam bir endpoint URL'si** isterse, şunu kullanın:
- Chat Completions: `https://api.lemondata.cc/v1/chat/completions`

## Örnek Uygulama Kurulumları

<Tabs>
  <Tab title="LobeChat">
    **Self-hosted LobeChat**

    ```bash
    OPENAI_API_KEY=sk-your-lemondata-key
    OPENAI_PROXY_URL=https://api.lemondata.cc/v1
    ```

    **LobeChat Cloud**
    1. **Settings** → **AI Service Provider** bölümünü açın
    2. **OpenAI** seçin
    3. LemonData API key'inizi girin
    4. Özel endpoint'i `https://api.lemondata.cc/v1` olarak ayarlayın
  </Tab>

  <Tab title="Cherry Studio">
    1. **Settings** → **Model Provider** bölümünü açın
    2. Bir **OpenAI-compatible** sağlayıcı ekleyin
    3. Şunları girin:
       - **Name**: LemonData
       - **API Base URL**: `https://api.lemondata.cc/v1`
       - **API Key**: `sk-your-lemondata-key`
    4. Kullanmak istediğiniz modelleri ekleyin
  </Tab>

  <Tab title="Chatbox">
    1. **Settings** → **AI Model** bölümünü açın
    2. **OpenAI API Compatible** seçin
    3. Şunları girin:
       - **API Host**: `https://api.lemondata.cc/v1`
       - **API Key**: `sk-your-lemondata-key`
       - **Model**: `gpt-5-mini` veya `gpt-4o`
  </Tab>

  <Tab title="TypingMind">
    1. **Settings** bölümünü açın
    2. **Custom Endpoint** seçin
    3. Şunları girin:
       - **Endpoint URL**: `https://api.lemondata.cc/v1/chat/completions`
       - **API Key**: `sk-your-lemondata-key`
    4. Görünür yapmak istediğiniz modelleri yapılandırın
  </Tab>
</Tabs>

## Daha Güvenli Varsayılan Modeller

| Kategori | Önerilen Modeller |
|----------|------------------|
| Genel varsayılan | `gpt-5.4`, `gpt-5-mini`, `gpt-4o` |
| Anthropic | `claude-sonnet-4-6`, `claude-opus-4-6` |
| Google | `gemini-2.5-flash`, `gemini-2.5-pro` |
| DeepSeek | `deepseek-r1`, `deepseek-v3-2` |

## İpuçları

<AccordionGroup>
  <Accordion title="Uygulama destekliyorsa base URL'yi tercih edin">
    Çoğu uygulama, tam bir endpoint URL'si yerine base URL olarak `https://api.lemondata.cc/v1` verdiğinizde yapılandırması daha kolay olur ve daha az kırılgandır.
  </Accordion>

  <Accordion title="Önce daha küçük modeller kullanın">
    Günlük sohbet için `gpt-5-mini` veya `gpt-4o` ile başlayın, ardından daha ağır muhakeme görevlerini `gpt-5.4` veya `claude-opus-4-6` modellerine taşıyın.
  </Accordion>

  <Accordion title="Kodlama araçlarını ayrı tutun">
    Genel sohbet uygulamaları ve kodlama ajanları genellikle farklı endpoint davranışlarına ihtiyaç duyar. Codex/Cursor tarzı araçları kendi özel entegrasyon sayfalarında tutun.
  </Accordion>
</AccordionGroup>

## Sorun Giderme

<AccordionGroup>
  <Accordion title="Bağlantı Hatası">
    - Base URL'nin tam olarak `https://api.lemondata.cc/v1` olduğunu doğrulayın
    - Uygulama yolları otomatik olarak ekliyorsa sondaki eğik çizgileri kaldırın
    - Uygulama tam bir endpoint URL'si istiyorsa `https://api.lemondata.cc/v1/chat/completions` kullanın
  </Accordion>

  <Accordion title="401 Unauthorized">
    - API key'inizi yeniden kontrol edin
    - Anahtarın LemonData dashboard içinde etkin olduğunu doğrulayın
    - Fazladan boşluk veya satır sonu olmadığından emin olun
  </Accordion>

  <Accordion title="Model Kullanılamıyor">
    - Model adını birebir doğrulayın
    - `gpt-5-mini`, `gpt-4o` veya `claude-sonnet-4-6` gibi geniş desteklenen bir varsayılan deneyin
    - LemonData dokümantasyonunda veya dashboard'da güncel kullanılabilirliği kontrol edin
  </Accordion>
</AccordionGroup>