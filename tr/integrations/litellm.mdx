---
title: "LiteLLM"
description: "LiteLLM'yi LemonData ile upstream sağlayıcı veya dahili ağ geçidi katmanı olarak kullanın"
---

## Genel Bakış

LiteLLM ve LemonData genelde iki şekilde birlikte kullanılır.

- LemonData'yı LiteLLM arkasında **OpenAI uyumlu upstream** olarak kullanmak
- sanal anahtarlar, ek yönlendirme veya merkezi gözlemlenebilirlik için LiteLLM'yi LemonData'nın önüne koymak

LemonData için en temiz varsayılan yol, LiteLLM'nin **custom OpenAI / OpenAI uyumlu** modunu kullanıp `api_base = https://api.lemondata.cc/v1` ayarlamaktır.

<Note>
Açıkça Claude-native veya Gemini-native request shape ihtiyacınız varsa, bu akışları LiteLLM'nin OpenAI uyumlu soyutlamasına zorlamak yerine LemonData'nın native entegrasyonlarını tercih edin.
</Note>

<Note>
**Tür**: Framework veya platform

**Birincil yol**: OpenAI uyumlu upstream

**Destek seviyesi**: Desteklenen yol
</Note>

## Kurulum

```bash
pip install 'litellm[proxy]'
```

## Proxy yapılandırması

```yaml
model_list:
  - model_name: lemondata-gpt-5.4
    litellm_params:
      model: custom_openai/gpt-5.4
      api_base: https://api.lemondata.cc/v1
      api_key: os.environ/OPENAI_API_KEY
```

Başlatma:

```bash
export OPENAI_API_KEY="sk-your-lemondata-key"
litellm --config litellm-config.yaml --port 4000
```

## En iyi uygulamalar

- LemonData'yı önce `custom_openai` upstream olarak ele alın
- LiteLLM'yi yalnızca gerçekten ek bir gateway katmanına ihtiyaç duyuyorsanız öne koyun
- OpenAI uyumlu yol üzerinde her sağlayıcıya özgü ayrıntının otomatik olarak çalışacağını varsaymayın
