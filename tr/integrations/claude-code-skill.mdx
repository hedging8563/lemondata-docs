---
title: "✨ Claude Code Becerisi"
description: "5 dakikalık hızlı başlangıç rehberi - Claude Code'un herhangi bir LemonData API'sini sizin için otomatik olarak entegre etmesine izin verin"
---

<Note>
Bu beceri, sadece neye ihtiyacınız olduğunu günlük dilde açıklayarak LemonData'nın **yüzlerce AI API'sinden** herhangi birini entegre etmenize olanak tanır. API **Agent-First** tasarımlıdır — Claude bir model adını yanlış tahmin etse bile, hata yanıtı ona nasıl düzelteceğini tam olarak söyler.
</Note>

## Bu Beceri Neler Yapabilir?

Kodunuzda AI özelliklerini (GPT-4, görsel oluşturma, konuşma tanıma vb.) kullanmak istediğinizde, bu beceri şunları yapacaktır:

1. ✅ **API'yi doğrudan dener** — arama yapmaya gerek yok
2. ✅ **Hatalardan kendi kendine düzelir** — yapılandırılmış hata ipuçları sonraki denemeyi yönlendirir
3. ✅ **Seçtiğiniz dilde eksiksiz, çalıştırılabilir kod üretir**
4. ✅ **Gerçek zamanlı fiyatlandırma ve kullanılabilirliğe göre en iyi modeli seçer**
5. ✅ **Native endpoint önerileriyle entegrasyonunuzu optimize eder**

**Kısacası: İhtiyaçlarınızı tarif edin ve çalışan API entegrasyon kodunu alın — ilk deneme mükemmel olmasa bile, API Claude'u doğru cevaba yönlendirir.**

## Adım 1: Beceriyi Yükleyin

<Tabs>
  <Tab title="npx (Önerilen)">
    Yüklemenin en kolay yolu:

    ```bash
    npx add-skill hedging8563/lemondata-api-skill -y
    ```

    Bu, beceriyi algılanan tüm kodlama ajanlarına (Claude Code, Cursor, Copilot vb.) otomatik olarak yükleyecektir.
  </Tab>
  <Tab title="GitHub URL'sini Paylaşın">
    1. Terminalinizde veya IDE'nizde **Claude Code**'u açın
    2. Bu bağlantıyı sohbete yapıştırın:
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
    3. Claude Code bunu bir beceri deposu olarak tanıyacak ve yüklemenize yardımcı olacaktır
  </Tab>
  <Tab title="Git Clone">
    **Kişisel kurulum** (tüm projelerde kullanılabilir):
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git ~/.claude/skills/lemondata-api-integration
    ```

    **Projeye özel kurulum** (git aracılığıyla ekiple paylaşılır):
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git .claude/skills/lemondata-api-integration
    ```
  </Tab>
</Tabs>

### Kurulumu Doğrulayın

Claude Code'a şunu sorun:
```
What skills are available?
```

Eğer `lemondata-api-integration` görüyorsanız, kurulum başarılı olmuştur!

## Adım 2: API Key'inizi Alın

Beceriyi kullanmadan önce bir LemonData API Key'e ihtiyacınız vardır.

<Tip>
**API Key'i önceden yapılandırmanıza gerek yok!** Beceriyi kullandığınızda, Claude Code otomatik olarak API Key'inizi isteyecek ve üretilen koda dahil edecektir.
</Tip>

<Steps>
  <Step title="LemonData'yı Ziyaret Edin">
    [lemondata.cc](https://lemondata.cc) adresine gidin
  </Step>
  <Step title="Giriş Yapın">
    Bir hesap oluşturun veya giriş yapın
  </Step>
  <Step title="API Key Alın">
    [Dashboard → API Keys](https://lemondata.cc/dashboard/api) bölümüne gidin ve yeni bir anahtar oluşturun
  </Step>
  <Step title="Anahtarı Kopyalayın">
    Anahtarınız `sk-...` ile başlar - güvenli bir şekilde saklayın
  </Step>
</Steps>

<Warning>
**Önemli:**
- Tüm LemonData API'leri aynı API Key'i kullanır
- Anahtarınızı başkalarıyla paylaşmayın
- Anahtarınızı GitHub'a commit etmeyin
</Warning>

## Adım 3: Kullanmaya Başlayın

Beceriyi kullanmak sohbet etmek kadar basittir!

### Örnek 1: GPT-4 Kullanımı

**Siz dersiniz ki:**
```
I want to use GPT-4 in my Python project
```

**Claude Code şunları yapacaktır:**
1. En iyi model tahminiyle API'yi doğrudan çağırmayı dener
2. Model adı yanlışsa, hatadan `did_you_mean` bilgisini okur ve otomatik düzeltir
3. Doğru modelle eksiksiz Python kodu üretir

**Sonuç:**
```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.lemondata.cc/v1"
)

response = client.chat.completions.create(
    model="gpt-4o",
    messages=[{"role": "user", "content": "Hello!"}]
)

print(response.choices[0].message.content)
```

### Örnek 2: Görsel Oluşturma

**Siz dersiniz ki:**
```
How do I generate images with Flux in Node.js?
```

**Claude Code şunları yapacaktır:**
- Görsel oluşturma API'sini çağırır
- Uygun hata yönetimiyle JavaScript kodu üretir
- Görsel indirme ve kaydetme kodunu dahil eder

### Örnek 3: Konuşma Tanıma

**Siz dersiniz ki:**
```
Integrate a speech-to-text API
```

**Claude Code şunları yapacaktır:**
- Hangi programlama dilini kullandığınızı sorar
- Doğru audio endpoint ile kod üretir
- Ses dosyası işleme örnekleri sunar

## Agent-First API Özellikleri

LemonData'nın API'si AI ajanları için tasarlanmıştır. Her hata yanıtı yapılandırılmış ipuçları içerir:

### Akıllı Hata Kurtarma

| Hata | API'nin Döndürdüğü | Claude'un Yaptığı |
|------|--------------------|--------------------|
| Yanlış model adı | `did_you_mean` + `suggestions` | Otomatik düzeltir ve tekrar dener |
| Yetersiz bakiye | `balance_usd` + daha ucuz `suggestions` | Uygun fiyatlı modele geçer |
| Model kullanılamıyor | `alternatives` + `retry_after` | Kullanılabilir modele geçer |
| Rate limited | `retry_after` (tam saniye) | Bekler ve tekrar dener |
| Bağlam çok uzun | Daha büyük modelli `suggestions` | Daha büyük bağlamlı modele geçer |

### Model Keşfi

Dokümantasyon aramaya gerek yok. Şu API endpoint'lerini kullanın:

```bash
# Makine tarafından okunabilir API özetini okuyun
curl https://lemondata.cc/llms.txt

# Kategoriye göre modelleri listeleyin
curl "https://api.lemondata.cc/v1/models?category=chat" -H "Authorization: Bearer sk-KEY"
curl "https://api.lemondata.cc/v1/models?category=image" -H "Authorization: Bearer sk-KEY"

# Yeteneğe göre filtreleyin
curl "https://api.lemondata.cc/v1/models?tag=coding" -H "Authorization: Bearer sk-KEY"
```

### Native Endpoint Optimizasyonu

Claude, bir Claude veya Gemini modeliyle `/v1/chat/completions` kullandığında, API optimizasyon başlıkları döndürür:

```
X-LemonData-Hint: This model supports native Anthropic format. Use POST /v1/messages for better performance.
X-LemonData-Native-Endpoint: /v1/messages
```

Claude Code daha iyi performans için otomatik olarak native endpoint'e geçecektir.

## Desteklenen Özellikler

Bu beceri, tüm bu AI yeteneklerini entegre etmenize yardımcı olabilir:

| Özellik Türü | Örnekler |
|--------------|----------|
| 💬 Sohbet | GPT-4o, Claude, Gemini, DeepSeek |
| 🎨 Görsel Oluşturma | Midjourney, Flux, Stable Diffusion |
| 🎬 Video Oluşturma | Sora, Runway, Kling, Luma AI |
| 🎵 Müzik Oluşturma | Suno |
| 🗿 3D Modeller | Tripo3D |
| 🎤 Ses | Text-to-Speech, Speech-to-Text |
| 📊 Embeddings | text-embedding-3 |
| 🔄 Rerank | bce-reranker, qwen3-rerank |

## Kullanım İpuçları

### 1. Sadece İhtiyaçlarınızı Belirtin

<Tabs>
  <Tab title="❌ Bunu yapmayın">
    ```
    Help me check if LemonData has an image generation API
    ```
  </Tab>
  <Tab title="✅ Bunun yerine şunu yapın">
    ```
    I want to generate images in Python
    ```
  </Tab>
</Tabs>

### 2. Dilinizi Belirtin

Belirli bir dil gereksiniminiz varsa, sadece söyleyin:

```
Use JavaScript to call GPT-4
Use Python for speech recognition
Use Go to generate images
```

### 3. Senaryonuzu Açıklayın

Özel ihtiyaçlarınız varsa, bunları ayrıntılı olarak açıklayın:

```
I'm building a chatbot, need to use GPT-4
My website needs to let users upload and transform images
I'm making a voice notes app, need speech-to-text
```

## Önemli Hususlar

### API Key Güvenliği

<CardGroup cols={2}>
  <Card title="❌ Asla Yapmayın" icon="xmark" color="#ef4444">
    - API Key'i doğrudan frontend koduna koymak
    - API Key'i GitHub'a commit etmek
    - API Key'i başkalarıyla paylaşmak
  </Card>
  <Card title="✅ En İyi Uygulamalar" icon="check" color="#22c55e">
    - Ortam değişkenlerini kullanın
    - Web siteleri için API'leri backend'den çağırın
    - Next.js, Express gibi framework'ler kullanın
  </Card>
</CardGroup>

### Maliyet Yönetimi

- Her API çağrısı kredi tüketir
- [Dashboard](https://lemondata.cc/dashboard) üzerinden kullanım limitleri belirleyin
- Test için küçük veri hacimleri kullanın
- Maliyetleri programatik olarak izlemek için `error.balance_usd` ve `error.estimated_cost_usd` değerlerini kontrol edin

### Hata Yönetimi

Üretilen kod, şunları yapan Agent-First hata yönetimi içerir:
- Yapılandırılmış hata ipuçlarını okur (`did_you_mean`, `suggestions`, `retry_after`)
- Yaygın hatalardan otomatik olarak kurtulur
- Gerektiğinde alternatif modellere geçer

## SSS

<AccordionGroup>
  <Accordion title="Beceri otomatik olarak tetiklenmiyor mu?">
    İsteğinizde "LemonData" veya "LemonData API" ifadesini kullanmayı deneyin veya ihtiyacınızı daha spesifik bir şekilde açıklayın:
    ```
    Use LemonData to integrate GPT-4 in my project
    ```
  </Accordion>

  <Accordion title="API, Claude'un doğru modeli bulmasına nasıl yardımcı oluyor?">
    LemonData'nın Agent-First API'si yapılandırılmış hata ipuçları döndürür. Claude yanlış bir model adı tahmin ederse, hata yanıtı doğru adla birlikte `did_you_mean` ve alternatif modellerle birlikte `suggestions` içerir. Claude bunları okur ve bir sonraki denemede otomatik olarak düzeltir.
  </Accordion>

  <Accordion title="Üretilen kodda hatalar mı var?">
    Şu noktaları kontrol edin:
    1. API Key doğru mu?
    2. Gerekli kütüphaneler yüklü mü? (örneğin Python için `openai`)
    3. LemonData sunucularına erişebiliyor musunuz?
    4. API Key'inizde yeterli kredi var mı?
  </Accordion>

  <Accordion title="Beceri nasıl güncellenir?">
    GitHub bağlantısını Claude Code'a tekrar gönderin:
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
  </Accordion>
</AccordionGroup>

## Kaynaklar

<CardGroup cols={2}>
  <Card title="LemonData Web Sitesi" icon="globe" href="https://lemondata.cc">
    Ana web sitesi ve dashboard
  </Card>
  <Card title="API Dokümantasyonu" icon="book" href="https://docs.lemondata.cc">
    Eksiksiz API referansı
  </Card>
  <Card title="Modeller" icon="robot" href="https://lemondata.cc/en/models">
    Tüm mevcut modellere göz atın
  </Card>
  <Card title="llms.txt" icon="file-lines" href="https://lemondata.cc/llms.txt">
    AI ajanları için makine tarafından okunabilir API özeti
  </Card>
</CardGroup>

## Başlayın!

Artık bu beceriyi nasıl kullanacağınızı biliyorsunuz. Claude Code'a şunları sormayı deneyin:

```
I want to use GPT-4 in my project
```

veya

```
Help me integrate image generation
```

Claude Code, tüm işi sizin yerinize tamamlamak için bu beceriyi otomatik olarak kullanacaktır — ve bir şeyler ters giderse, API'nin yapılandırılmış hata ipuçları onu doğru cevaba yönlendirecektir!

<Info>
**Sorularınız mı var?** [GitHub Issues](https://github.com/hedging8563/lemondata-api-skill/issues) sayfamızı kontrol edin veya [support@lemondata.cc](mailto:support@lemondata.cc) adresiyle iletişime geçin
</Info>
