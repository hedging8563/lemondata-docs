---
title: "✨ مهارة Claude Code"
description: "دليل البدء السريع في 5 دقائق - دع Claude Code يدمج أي واجهة برمجة تطبيقات LemonData تلقائيًا"
---

<Note>
تتيح لك هذه المهارة دمج أي من **مئات واجهات برمجة تطبيقات الذكاء الاصطناعي** الخاصة بـ LemonData بمجرد وصف ما تحتاجه بلغة بسيطة. واجهة البرمجة مصممة بنهج **Agent-First** — حتى لو خمّن Claude اسم النموذج بشكل خاطئ، فإن استجابة الخطأ تخبره بالضبط كيف يصحح ذلك.
</Note>

## ماذا يمكن لهذه المهارة أن تفعل؟

عندما تريد استخدام ميزات الذكاء الاصطناعي في الكود الخاص بك (مثل GPT-4، توليد الصور، التعرف على الكلام، إلخ)، ستقوم هذه المهارة بـ:

1. ✅ **تجربة الـ API مباشرة** — بدون الحاجة للبحث
2. ✅ **التصحيح الذاتي من الأخطاء** — تلميحات الخطأ المنظمة توجه المحاولة التالية
3. ✅ **إنشاء كود كامل وقابل للتشغيل** بلغة البرمجة التي تختارها
4. ✅ **اختيار أفضل نموذج** بناءً على الأسعار والتوفر في الوقت الفعلي
5. ✅ **تحسين التكامل** مع اقتراحات نقاط النهاية الأصلية

**باختصار: فقط صف احتياجاتك، واحصل على كود تكامل API يعمل — حتى لو لم تكن المحاولة الأولى مثالية، فإن الـ API يوجه Claude إلى الإجابة الصحيحة.**

## الخطوة 1: تثبيت المهارة

<Tabs>
  <Tab title="npx (موصى به)">
    أسهل طريقة للتثبيت:

    ```bash
    npx add-skill hedging8563/lemondata-api-skill -y
    ```

    سيقوم هذا تلقائياً بتثبيت المهارة في جميع وكلاء البرمجة المكتشفين (Claude Code، Cursor، Copilot، إلخ).
  </Tab>
  <Tab title="مشاركة رابط GitHub">
    1. افتح **Claude Code** في الـ terminal أو الـ IDE الخاص بك
    2. الصق هذا الرابط في الدردشة:
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
    3. سيتعرف Claude Code عليه كمستودع مهارات وسيساعدك في تثبيته
  </Tab>
  <Tab title="Git Clone">
    **تثبيت شخصي** (متاح في جميع المشاريع):
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git ~/.claude/skills/lemondata-api-integration
    ```

    **تثبيت خاص بالمشروع** (يتم مشاركته مع الفريق عبر git):
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git .claude/skills/lemondata-api-integration
    ```
  </Tab>
</Tabs>

### التحقق من التثبيت

اسأل Claude Code:
```
What skills are available?
```

إذا رأيت `lemondata-api-integration`، فقد تم التثبيت بنجاح!

## الخطوة 2: الحصول على API Key الخاص بك

قبل استخدام المهارة، تحتاج إلى LemonData API Key.

<Tip>
**لا تحتاج إلى تهيئة API Key مسبقاً!** عند استخدام المهارة، سيطلب Claude Code تلقائياً API Key الخاص بك ويضمنه في الكود الذي تم إنشاؤه.
</Tip>

<Steps>
  <Step title="زيارة LemonData">
    انتقل إلى [lemondata.cc](https://lemondata.cc)
  </Step>
  <Step title="تسجيل الدخول">
    أنشئ حساباً أو سجل دخولك
  </Step>
  <Step title="الحصول على API Key">
    انتقل إلى [Dashboard → API Keys](https://lemondata.cc/dashboard/api) وأنشئ مفتاحاً جديداً
  </Step>
  <Step title="نسخ المفتاح">
    يبدأ مفتاحك بـ `sk-...` - احفظه بشكل آمن
  </Step>
</Steps>

<Warning>
**هام:**
- تستخدم جميع واجهات برمجة تطبيقات LemonData نفس API Key
- لا تشارك مفتاحك مع الآخرين
- لا ترفع مفتاحك إلى GitHub
</Warning>

## الخطوة 3: ابدأ الاستخدام

استخدام المهارة بسيط مثل الدردشة!

### مثال 1: استخدام GPT-4

**أنت تقول:**
```
I want to use GPT-4 in my Python project
```

**سيقوم Claude Code بـ:**
1. تجربة استدعاء الـ API مباشرة مع أفضل تخمين للنموذج
2. إذا كان اسم النموذج خاطئاً، يقرأ `did_you_mean` من الخطأ ويصحح تلقائياً
3. إنشاء كود Python كامل مع النموذج الصحيح

**ستحصل على:**
```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.lemondata.cc/v1"
)

response = client.chat.completions.create(
    model="gpt-4o",
    messages=[{"role": "user", "content": "Hello!"}]
)

print(response.choices[0].message.content)
```

### مثال 2: توليد الصور

**أنت تقول:**
```
How do I generate images with Flux in Node.js?
```

**سيقوم Claude Code بـ:**
- استدعاء API توليد الصور
- إنشاء كود JavaScript مع معالجة أخطاء مناسبة
- تضمين كود تنزيل وحفظ الصور

### مثال 3: التعرف على الكلام

**أنت تقول:**
```
Integrate a speech-to-text API
```

**سيقوم Claude Code بـ:**
- السؤال عن لغة البرمجة التي تستخدمها
- إنشاء كود مع نقطة نهاية الصوت الصحيحة
- توفير أمثلة للتعامل مع ملفات الصوت

## ميزات Agent-First API

واجهة برمجة تطبيقات LemonData مصممة لوكلاء الذكاء الاصطناعي. كل استجابة خطأ تتضمن تلميحات منظمة:

### التعافي الذكي من الأخطاء

| الخطأ | ما يُرجعه الـ API | ما يفعله Claude |
|-------|---------------------|--------------------|
| اسم نموذج خاطئ | `did_you_mean` + `suggestions` | يصحح تلقائياً ويعيد المحاولة |
| رصيد غير كافٍ | `balance_usd` + `suggestions` أرخص | يتحول إلى نموذج بسعر مناسب |
| نموذج غير متاح | `alternatives` + `retry_after` | يتحول إلى نموذج متاح |
| تجاوز حد المعدل | `retry_after` (ثوانٍ محددة) | ينتظر ثم يعيد المحاولة |
| سياق طويل جداً | `suggestions` مع نماذج أكبر | يتحول إلى نموذج بسياق أكبر |

### اكتشاف النماذج

لا حاجة للبحث في التوثيق. استخدم نقاط نهاية الـ API هذه:

```bash
# قراءة نظرة عامة على الـ API القابلة للقراءة آلياً
curl https://lemondata.cc/llms.txt

# عرض النماذج حسب الفئة
curl "https://api.lemondata.cc/v1/models?category=chat" -H "Authorization: Bearer sk-KEY"
curl "https://api.lemondata.cc/v1/models?category=image" -H "Authorization: Bearer sk-KEY"

# التصفية حسب القدرة
curl "https://api.lemondata.cc/v1/models?tag=coding" -H "Authorization: Bearer sk-KEY"
```

### تحسين نقاط النهاية الأصلية

عندما يستخدم Claude `/v1/chat/completions` مع نموذج Claude أو Gemini، يُرجع الـ API رؤوس تحسين:

```
X-LemonData-Hint: This model supports native Anthropic format. Use POST /v1/messages for better performance.
X-LemonData-Native-Endpoint: /v1/messages
```

سيتحول Claude Code تلقائياً إلى نقطة النهاية الأصلية لأداء أفضل.

## الميزات المدعومة

يمكن لهذه المهارة مساعدتك في دمج كل قدرات الذكاء الاصطناعي هذه:

| نوع الميزة | أمثلة |
|-------------|--------|
| 💬 الدردشة | GPT-4o, Claude, Gemini, DeepSeek |
| 🎨 توليد الصور | Midjourney, Flux, Stable Diffusion |
| 🎬 توليد الفيديو | Sora, Runway, Kling, Luma AI |
| 🎵 توليد الموسيقى | Suno |
| 🗿 نماذج ثلاثية الأبعاد | Tripo3D |
| 🎤 الصوت | Text-to-Speech, Speech-to-Text |
| 📊 التمثيلات المتجهة | text-embedding-3 |
| 🔄 إعادة الترتيب | bce-reranker, qwen3-rerank |

## نصائح للاستخدام

### 1. فقط اذكر احتياجاتك

<Tabs>
  <Tab title="❌ لا تفعل هذا">
    ```
    Help me check if LemonData has an image generation API
    ```
  </Tab>
  <Tab title="✅ افعل هذا بدلاً من ذلك">
    ```
    I want to generate images in Python
    ```
  </Tab>
</Tabs>

### 2. حدد لغتك

إذا كان لديك متطلب لغة معين، فقط قل ذلك:

```
Use JavaScript to call GPT-4
Use Python for speech recognition
Use Go to generate images
```

### 3. صف السيناريو الخاص بك

إذا كانت لديك احتياجات محددة، فصفها بالتفصيل:

```
I'm building a chatbot, need to use GPT-4
My website needs to let users upload and transform images
I'm making a voice notes app, need speech-to-text
```

## اعتبارات هامة

### أمان API Key

<CardGroup cols={2}>
  <Card title="❌ لا تفعل هذا أبداً" icon="xmark" color="#ef4444">
    - وضع API Key مباشرة في كود الواجهة الأمامية
    - رفع API Key إلى GitHub
    - مشاركة API Key مع الآخرين
  </Card>
  <Card title="✅ أفضل الممارسات" icon="check" color="#22c55e">
    - استخدام متغيرات البيئة
    - استدعاء الـ APIs من الواجهة الخلفية للمواقع الإلكترونية
    - استخدام أطر عمل مثل Next.js، Express
  </Card>
</CardGroup>

### إدارة التكاليف

- كل استدعاء API يستهلك رصيداً
- عيّن حدود الاستخدام في [لوحة التحكم](https://lemondata.cc/dashboard) الخاصة بك
- استخدم أحجام بيانات صغيرة للاختبار
- تحقق من `error.balance_usd` و `error.estimated_cost_usd` لمراقبة التكاليف برمجياً

### معالجة الأخطاء

يتضمن الكود المُنشأ معالجة أخطاء بنهج Agent-First تقوم بـ:
- قراءة تلميحات الخطأ المنظمة (`did_you_mean`، `suggestions`، `retry_after`)
- التعافي التلقائي من الأخطاء الشائعة
- التراجع إلى نماذج بديلة عند الحاجة

## الأسئلة الشائعة

<AccordionGroup>
  <Accordion title="المهارة لا تعمل تلقائياً؟">
    حاول ذكر "LemonData" أو "LemonData API" في طلبك، أو صف حاجتك بشكل أكثر تحديداً:
    ```
    Use LemonData to integrate GPT-4 in my project
    ```
  </Accordion>

  <Accordion title="كيف يساعد الـ API كلود في العثور على النموذج الصحيح؟">
    واجهة برمجة تطبيقات LemonData بنهج Agent-First تُرجع تلميحات خطأ منظمة. إذا خمّن Claude اسم نموذج خاطئ، تتضمن استجابة الخطأ `did_you_mean` مع الاسم الصحيح و `suggestions` مع نماذج بديلة. يقرأ Claude هذه التلميحات ويصحح تلقائياً في المحاولة التالية.
  </Accordion>

  <Accordion title="الكود المُنشأ يحتوي على أخطاء؟">
    تحقق من هذه النقاط:
    1. هل API Key صحيح؟
    2. هل المكتبات المطلوبة مثبتة؟ (مثل `openai` لـ Python)
    3. هل يمكنك الوصول إلى خوادم LemonData؟
    4. هل يحتوي API Key الخاص بك على رصيد كافٍ؟
  </Accordion>

  <Accordion title="كيف يتم تحديث المهارة؟">
    أرسل رابط GitHub إلى Claude Code مرة أخرى:
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
  </Accordion>
</AccordionGroup>

## الموارد

<CardGroup cols={2}>
  <Card title="موقع LemonData" icon="globe" href="https://lemondata.cc">
    الموقع الرئيسي ولوحة التحكم
  </Card>
  <Card title="توثيق الـ API" icon="book" href="https://docs.lemondata.cc">
    مرجع الـ API الكامل
  </Card>
  <Card title="النماذج" icon="robot" href="https://lemondata.cc/en/models">
    تصفح جميع النماذج المتاحة
  </Card>
  <Card title="llms.txt" icon="file-lines" href="https://lemondata.cc/llms.txt">
    نظرة عامة على الـ API القابلة للقراءة آلياً لوكلاء الذكاء الاصطناعي
  </Card>
</CardGroup>

## ابدأ الآن!

الآن تعرف كيفية استخدام هذه المهارة. جرب سؤال Claude Code:

```
I want to use GPT-4 in my project
```

أو

```
Help me integrate image generation
```

سيستخدم Claude Code هذه المهارة تلقائياً لإكمال جميع الأعمال نيابة عنك — وإذا حدث خطأ ما، فإن تلميحات الخطأ المنظمة من الـ API ستوجهه إلى الإجابة الصحيحة!

<Info>
**لديك أسئلة؟** تحقق من [GitHub Issues](https://github.com/hedging8563/lemondata-api-skill/issues) الخاصة بنا أو تواصل مع [support@lemondata.cc](mailto:support@lemondata.cc)
</Info>
