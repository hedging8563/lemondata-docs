---
title: "Vercel AI SDK"
description: "دمج LemonData مع Vercel AI SDK باستخدام موفّرات متوافقة مع OpenAI أو موفّرات أصلية لـ Responses"
---

## نظرة عامة

بالنسبة إلى LemonData، فإن الإعداد الافتراضي الأكثر استقرارًا في Vercel AI SDK هو **الموفّر المتوافق مع OpenAI**.

إذا كنت تحتاج تحديدًا إلى سلوك **أصلي لـ Responses**، فيمكنك التبديل إلى موفّر OpenAI مع الاحتفاظ بنفس عنوان URL الأساسي الخاص بـ LemonData.

## الإعداد الافتراضي الموصى به: الموفّر المتوافق مع OpenAI

```bash
npm install ai @ai-sdk/openai-compatible
```

```typescript
import { createOpenAICompatible } from '@ai-sdk/openai-compatible';

export const lemondata = createOpenAICompatible({
  name: 'lemondata',
  apiKey: process.env.LEMONDATA_API_KEY,
  baseURL: 'https://api.lemondata.cc/v1',
});
```

## توليد النص

```typescript
import { generateText } from 'ai';
import { lemondata } from './lemondata';

const { text } = await generateText({
  model: lemondata.chatModel('gpt-5.4'),
  prompt: 'Explain LemonData in one sentence.',
});

console.log(text);
```

## بث النص

```typescript
import { streamText } from 'ai';
import { lemondata } from './lemondata';

const result = await streamText({
  model: lemondata.chatModel('gpt-5.4'),
  prompt: 'Write a short poem about coding.',
});

for await (const textPart of result.textStream) {
  process.stdout.write(textPart);
}
```

## استدعاء الأدوات

```typescript
import { generateText, tool } from 'ai';
import { z } from 'zod';
import { lemondata } from './lemondata';

const result = await generateText({
  model: lemondata.chatModel('gpt-5.4'),
  prompt: 'What is the weather in San Francisco?',
  tools: {
    weather: tool({
      description: 'Get weather in a location',
      parameters: z.object({
        location: z.string(),
      }),
      execute: async ({ location }) => ({
        location,
        temperature: 72,
        condition: 'sunny',
      }),
    }),
  },
});

console.log(result.text);
```

## المخرجات المهيكلة

```typescript
import { generateObject } from 'ai';
import { z } from 'zod';
import { lemondata } from './lemondata';

const { object } = await generateObject({
  model: lemondata.chatModel('gpt-5.4'),
  schema: z.object({
    name: z.string(),
    role: z.string(),
  }),
  prompt: 'Generate a fake developer profile.',
});

console.log(object);
```

## إذا كنت تحتاج صراحةً إلى سلوك أصلي لـ Responses

```bash
npm install ai @ai-sdk/openai
```

```typescript
import { createOpenAI } from '@ai-sdk/openai';

export const lemondataResponses = createOpenAI({
  apiKey: process.env.LEMONDATA_API_KEY,
  baseURL: 'https://api.lemondata.cc/v1',
});
```

```typescript
import { generateText } from 'ai';
import { lemondataResponses } from './lemondata-responses';

const { text } = await generateText({
  model: lemondataResponses('gpt-5.4'),
  prompt: 'Explain LemonData in one sentence.',
});
```

<Note>
استخدم `@ai-sdk/openai-compatible` كإعداد افتراضي آمن لعمليات الدمج بأسلوب proxy. انتقل إلى `@ai-sdk/openai` فقط عندما تريد صراحةً مسار موفّر مبنيًا على `/v1/responses`.
</Note>

## متغيرات البيئة

```bash
# .env.local
LEMONDATA_API_KEY=sk-your-lemondata-key
```

## أفضل الممارسات

<AccordionGroup>
  <Accordion title="فضّل openai-compatible أولاً">
    بالنسبة إلى بوابات الطرف الثالث وواجهات proxy الخلفية، يكون `@ai-sdk/openai-compatible` عادةً نقطة البداية الأقل إثارة للمفاجآت.
  </Accordion>

  <Accordion title="انتقل إلى Responses فقط عند الحاجة">
    إذا كنت تحتاج إلى سلوك موفّر مرتبط بـ `/v1/responses`، فقم بتبديل حزمة الموفّر بشكل متعمّد بدلًا من مزج النمطين في عميل واحد.
  </Accordion>

  <Accordion title="احتفظ بالأسرار على الخادم">
    لا تكشف أبدًا عن مفتاح LemonData API الخاص بك في كود جهة العميل. ضع إعداد الموفّر في ملفات الخادم أو مسارات API.
  </Accordion>
</AccordionGroup>