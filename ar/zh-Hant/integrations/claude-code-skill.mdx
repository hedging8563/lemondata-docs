---
title: "✨ مهارات Claude Code"
description: "دليل البدء السريع في 5 دقائق - اجعل Claude Code يدمج لك أي LemonData API تلقائيًا"
---

<Note>
تتيح لك هذه المهارة دمج أي من **مئات واجهات برمجة التطبيقات (AI APIs)** الخاصة بـ LemonData بمجرد وصف احتياجاتك بلغة بسيطة!
</Note>

## ماذا يمكن لهذه المهارة أن تفعل؟

عندما تريد استخدام ميزات AI في الكود الخاص بك (مثل GPT-4، توليد الصور، التعرف على الكلام، إلخ)، ستقوم هذه المهارة بـ:

1. ✅ **البحث التلقائي** في مئات الـ APIs الخاصة بـ LemonData
2. ✅ **العثور على الأنسب** لاحتياجاتك من الـ APIs
3. ✅ **توليد كود كامل وقابل للتشغيل**
4. ✅ **تكوين الـ API Key الخاص بك**
5. ✅ **توفير أمثلة للاستخدام** وأفضل الممارسات

**باختصار: ما عليك سوى وصف احتياجاتك بلغة طبيعية للحصول على كود دمج API كامل!**

## الخطوة 1: تثبيت المهارة

<Tabs>
  <Tab title="npx (موصى به)">
    أسهل طريقة للتثبيت:

    ```bash
    npx add-skill hedging8563/lemondata-api-skill -y
    ```

    سيؤدي هذا إلى تثبيت المهارة تلقائيًا لجميع مساعدي البرمجة المكتشفين (Claude Code, Cursor, Copilot، إلخ).
  </Tab>
  <Tab title="مشاركة رابط GitHub">
    1. افتح **Claude Code** في الطرفية (Terminal) أو بيئة التطوير (IDE) الخاصة بك
    2. الصق هذا الرابط في المحادثة:
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
    3. سيتعرف Claude Code عليه كمستودع مهارات وسيساعدك في التثبيت
  </Tab>
  <Tab title="Git Clone">
    **تثبيت شخصي** (ينطبق على جميع المشاريع):
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git ~/.claude/skills/lemondata-api-integration
    ```

    **تثبيت خاص بالمشروع** (يتم مشاركته مع الفريق عبر git):
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git .claude/skills/lemondata-api-integration
    ```
  </Tab>
</Tabs>

### التحقق من التثبيت

اسأل Claude Code:
```
What skills are available?
```

إذا رأيت `lemondata-api-integration`، فهذا يعني أن التثبيت تم بنجاح!

## الخطوة 2: الحصول على الـ API Key الخاص بك

قبل استخدام هذه المهارة، ستحتاج إلى LemonData API Key.

<Tip>
**لا تحتاج إلى تكوين الـ API Key مسبقًا!** عند استخدام هذه المهارة، سيطلب منك Claude Code الـ API Key الخاص بك تلقائيًا ويقوم بتضمينه في الكود المولد.
</Tip>

<Steps>
  <Step title="زيارة LemonData">
    انتقل إلى [lemondata.cc](https://lemondata.cc)
  </Step>
  <Step title="تسجيل الدخول">
    أنشئ حسابًا أو سجل الدخول
  </Step>
  <Step title="الحصول على الـ API Key">
    انتقل إلى [Dashboard → API Keys](https://lemondata.cc/dashboard/api) وأنشئ مفتاحًا جديدًا
  </Step>
  <Step title="نسخ المفتاح">
    يبدأ مفتاحك بـ `sk-...` - يرجى حفظه بأمان
  </Step>
</Steps>

<Warning>
**تنبيه هام:**
- تستخدم جميع واجهات برمجة تطبيقات LemonData نفس الـ API Key
- لا تشارك مفتاحك مع الآخرين
- لا ترفع مفتاحك إلى GitHub
</Warning>

## الخطوة 3: البدء في الاستخدام

استخدام هذه المهارة بسيط مثل الدردشة!

### مثال 1: استخدام GPT-4

**أنت تقول:**
```
I want to use GPT-4 in my Python project
```

**سيقوم Claude Code بـ:**
1. طلب الـ API Key الخاص بك
2. البحث عن الـ APIs المتعلقة بـ GPT-4
3. عرض خيارات الـ API المتاحة
4. توليد كود Python كامل
5. شرح كيفية استخدامه

**ستحصل على:**
```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.lemondata.cc/v1"
)

response = client.chat.completions.create(
    model="gpt-4o",
    messages=[{"role": "user", "content": "Hello!"}]
)

print(response.choices[0].message.content)
```

### مثال 2: توليد الصور

**أنت تقول:**
```
How do I generate images with Flux in Node.js?
```

**سيقوم Claude Code بـ:**
- البحث عن APIs توليد الصور
- توليد كود JavaScript
- تضمين كود تنزيل وحفظ الصور

### مثال 3: التعرف على الكلام

**أنت تقول:**
```
Integrate a speech-to-text API
```

**سيقوم Claude Code بـ:**
- سؤالك عن لغة البرمجة التي تستخدمها
- البحث عن APIs التعرف على الكلام
- توليد الكود باللغة التي اخترتها
- توفير أمثلة لمعالجة ملفات الصوت

## الميزات المدعومة

يمكن لهذه المهارة مساعدتك في دمج كل قدرات الذكاء الاصطناعي هذه:

| نوع الميزة | أمثلة |
|-------------|----------|
| 💬 المحادثة | GPT-4o, Claude, Gemini, DeepSeek |
| 🎨 توليد الصور | Midjourney, Flux, Stable Diffusion |
| 🎬 توليد الفيديو | Sora, Runway, Kling, Luma AI |
| 🎵 توليد الموسيقى | Suno |
| 🗿 نماذج ثلاثية الأبعاد (3D) | Tripo3D |
| 🎤 الصوت | تحويل النص إلى كلام (TTS)، تحويل الكلام إلى نص (STT) |
| 📊 التضمينات المتجهة | text-embedding-3 |
| 🔄 إعادة الترتيب | bce-reranker, qwen3-rerank |

## نصائح الاستخدام

### 1. وضح احتياجاتك مباشرة

<Tabs>
  <Tab title="❌ لا تفعل هذا">
    ```
    Help me check if LemonData has an image generation API
    ```
  </Tab>
  <Tab title="✅ يُنصح بفعل هذا">
    ```
    I want to generate images in Python
    ```
  </Tab>
</Tabs>

### 2. حدد لغتك

إذا كان لديك متطلبات لغة معينة، فما عليك سوى توضيح ذلك:

```
Use JavaScript to call GPT-4
Use Python for speech recognition
Use Go to generate images
```

### 3. صف السيناريو الخاص بك

إذا كان لديك احتياجات محددة، يرجى وصفها بالتفصيل:

```
I'm building a chatbot, need to use GPT-4
My website needs to let users upload and transform images
I'm making a voice notes app, need speech-to-text
```

## ملاحظات هامة

### أمان الـ API Key

<CardGroup cols={2}>
  <Card title="❌ لا تفعل هذا أبدًا" icon="xmark" color="#ef4444">
    - وضع الـ API Key مباشرة في كود الواجهة الأمامية (Frontend)
    - رفع الـ API Key إلى GitHub
    - مشاركة الـ API Key مع الآخرين
  </Card>
  <Card title="✅ أفضل الممارسات" icon="check" color="#22c55e">
    - استخدام متغيرات البيئة (Environment Variables)
    - بالنسبة للمواقع الإلكترونية، استدعِ الـ API من الخلفية (Backend)
    - استخدام أطر عمل مثل Next.js، Express، إلخ.
  </Card>
</CardGroup>

### إدارة التكاليف

- كل استدعاء للـ API يستهلك رصيدًا
- قم بتعيين حدود الاستخدام في [لوحة التحكم](https://lemondata.cc/dashboard) الخاصة بك
- استخدم كميات صغيرة من البيانات عند الاختبار

### معالجة الأخطاء

يحتوي الكود المولد على معالجة أساسية للأخطاء، ولكن قد تحتاج أيضًا إلى:
- إضافة آلية إعادة المحاولة
- تسجيل سجلات الأخطاء (Logging)
- توفير رسائل خطأ سهلة الاستخدام

## الأسئلة الشائعة (FAQ)

<AccordionGroup>
  <Accordion title="المهارة لم يتم تفعيلها تلقائيًا؟">
    حاول ذكر "LemonData" أو "LemonData API" في طلبك، أو صف احتياجاتك بشكل أكثر تحديدًا:
    ```
    Use LemonData to integrate GPT-4 in my project
    ```
  </Accordion>

  <Accordion title="لا يمكن العثور على الـ API الذي أريده؟">
    جرب كلمات رئيسية مختلفة:
    - "chat" ← "dialogue" أو "conversation"
    - "image" ← "picture" أو "generation"
    - "voice" ← "audio" أو "speech"
  </Accordion>

  <Accordion title="الكود المولد يحتوي على أخطاء؟">
    تحقق من النقاط التالية:
    1. هل الـ API Key صحيح؟
    2. هل تم تثبيت المكتبات اللازمة؟ (مثل `openai` لـ Python)
    3. هل يمكنك الوصول إلى خوادم LemonData؟
    4. هل يحتوي الـ API Key الخاص بك على رصيد كافٍ؟
  </Accordion>

  <Accordion title="كيفية تحديث المهارة؟">
    أرسل رابط GitHub مرة أخرى إلى Claude Code:
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
  </Accordion>
</AccordionGroup>

## الموارد

<CardGroup cols={2}>
  <Card title="موقع LemonData" icon="globe" href="https://lemondata.cc">
    الموقع الرئيسي ولوحة التحكم
  </Card>
  <Card title="وثائق الـ API" icon="book" href="https://docs.lemondata.cc">
    دليل مرجعي كامل للـ API
  </Card>
  <Card title="النماذج" icon="robot" href="https://lemondata.cc/zh-TW/models">
    تصفح جميع النماذج المتاحة
  </Card>
  <Card title="الأسعار" icon="dollar-sign" href="https://lemondata.cc/#pricing">
    أقل بنسبة 30% من الأسعار الرسمية
  </Card>
</CardGroup>

## ابدأ الآن!

الآن بعد أن عرفت كيفية استخدام هذه المهارة، جرب سؤال Claude Code:

```
I want to use GPT-4 in my project
```

أو

```
Help me integrate image generation
```

سيقوم Claude Code تلقائيًا باستخدام هذه المهارة للقيام بكل العمل نيابة عنك!

<Info>
**هل لديك أي أسئلة؟** يرجى مراجعة [GitHub Issues](https://github.com/hedging8563/lemondata-api-skill/issues) الخاصة بنا أو التواصل عبر [support@lemondata.cc](mailto:support@lemondata.cc)
</Info>