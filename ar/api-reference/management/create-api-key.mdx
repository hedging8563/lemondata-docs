---
title: "إنشاء مفتاح API"
openapi: "POST /v1/management/api-keys"
description: "ينشئ مفتاح API جديدًا للمستخدم للمؤسسة الحالية."
---
## نظرة عامة

ينشئ هذا endpoint مفتاح API جديدًا من نوع `type=user` داخل المؤسسة المرتبطة برمز الإدارة الحالي.

## جسم الطلب

| الحقل | النوع | القيم الافتراضية / القيود | ملاحظات |
|-------|------|------------------|-------|
| name | string | required, default Default Key, length 1-50 | Display name, trimmed server-side |
| limitAmount | number \| null | min 0, max 100000 | null or omitted = unlimited, 0 = zero quota, positive = USD spending cap |
| models | string[] | default [] | Optional logical model allowlist |
| expiresAt | string \| null | RFC3339 datetime | null means no expiry |

## ملاحظات

- القيمة الكاملة لمفتاح API تظهر مرة واحدة فقط في الاستجابة.
- `models: []` تعني عدم وجود قيود إضافية على النماذج خارج سياسة المؤسسة.
- `limitAmount: 0` ينشئ مفتاحًا صالحًا لكن بلا رصيد قابل للاستهلاك حتى يتم تحديثه لاحقًا.

## مثال

<RequestExample>
```bash cURL
curl -X POST "https://api.lemondata.cc/v1/management/api-keys" \
  -H "Authorization: Bearer mt-your-management-token" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Backend Worker",
    "limitAmount": 500,
    "models": ["veo3.1", "sora-2"],
    "expiresAt": "2026-12-31T23:59:59Z"
  }'
```
</RequestExample>

## الطلب / الاستجابة

استخدم لوحة OpenAPI التفاعلية أعلاه للحصول على الـ schema الدقيقة وحقول الاستجابة.
