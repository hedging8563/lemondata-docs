---
title: "قائمة المهام (متوافق مع Volc)"
openapi: "GET /api/v3/contents/generations/tasks"
description: "اعرض مهام Seedance المتوافقة الحديثة في المؤسسة الحالية."
---

## نظرة عامة

هذه الواجهات مخصصة للفرق التي تستخدم بالفعل شكل مهام Seedance بأسلوب Volc. يتم التوثيق باستخدام TokenLab Bearer API key. يبدأ معرف المهمة المرجع بـ `cgt-`؛ قم بالاستعلام عن الحالة حتى تصبح `status` إحدى `succeeded` أو `failed` أو `cancelled` أو `expired`.

انظر أيضا [نماذج فيديو Seedance 2.0](/ar/guides/seedance-2-video) و [توليد الفيديو](/ar/guides/video-generation).

## شكل الاستجابة

تعيد استجابة القائمة `items` و `total`. الفلاتر المدعومة هي `filter.status` و `filter.task_ids` و `filter.model` و `filter.service_tier`، مع `page_num` و `page_size` للترقيم.

## مثال

```bash
curl "https://api.tokenlab.sh/api/v3/contents/generations/tasks?page_num=1&page_size=20&filter.status=succeeded" \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```
