---
title: "إلغاء مهمة (متوافق مع Volc)"
openapi: "DELETE /api/v3/contents/generations/tasks/{id}"
description: "ألغ مهمة Seedance متوافقة في الانتظار أو ما زالت قابلة للإلغاء."
---

## نظرة عامة

هذه الواجهات مخصصة للفرق التي تستخدم بالفعل شكل مهام Seedance بأسلوب Volc. يتم التوثيق باستخدام TokenLab Bearer API key. يبدأ معرف المهمة المرجع بـ `cgt-`؛ قم بالاستعلام عن الحالة حتى تصبح `status` إحدى `succeeded` أو `failed` أو `cancelled` أو `expired`.

انظر أيضا [نماذج فيديو Seedance 2.0](/ar/guides/seedance-2-video) و [توليد الفيديو](/ar/guides/video-generation).

## شكل الاستجابة

يعيد طلب الإلغاء الناجح HTTP 200 مع جسم فارغ. استعلم عن المهمة بعد ذلك إذا احتجت إلى تأكيد الحالة النهائية.

## مثال

```bash
curl -X DELETE https://api.tokenlab.sh/api/v3/contents/generations/tasks/cgt-0123456789abcdef0123456789abcdef \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```
