---
title: "التثبيت الذاتي"
description: "قم بتشغيل LemonClaw على خادمك الخاص مع LemonData AI API — أمر واحد، بدون Docker"
---

<Note>
**التثبيت الذاتي** يعني أنك تقوم بتشغيل LemonClaw مباشرة على خادم VPS الخاص بك أو جهازك. أنت تدفع فقط مقابل استخدام نماذج الذكاء الاصطناعي من رصيد LemonData الخاص بك — بدون رسوم للمثيل.
</Note>

## التثبيت السريع

<CodeGroup>
```bash Linux / macOS
curl -fsSL https://raw.githubusercontent.com/hedging8563/lemonclaw/main/deploy/self-hosted/install.sh | bash
```

```bash Non-interactive
LEMONDATA_API_KEY=sk-xxx curl -fsSL https://raw.githubusercontent.com/hedging8563/lemonclaw/main/deploy/self-hosted/install.sh | bash
```
</CodeGroup>

سيطلب منك المثبّت **مفتاح LemonData API Key** الخاص بك. احصل على واحد من [لوحة التحكم](https://lemondata.cc/dashboard).

## ما الذي يتم تثبيته

<Steps>
  <Step title="Node.js 22+">
    يتم تثبيته تلقائيًا إذا لم يكن موجودًا (عبر NodeSource على Linux، Homebrew على macOS)
  </Step>
  <Step title="LemonClaw">
    حزمة LemonClaw الرسمية من npm، مُعدّة مسبقًا مع LemonData API
  </Step>
  <Step title="15 نموذج ذكاء اصطناعي">
    Claude، GPT-5، Gemini، DeepSeek، والمزيد — جاهزة للاستخدام
  </Step>
  <Step title="إضافات المراسلة الفورية">
    إضافات DingTalk و WeCom و QQ مثبتة مسبقًا
  </Step>
  <Step title="خدمة النظام">
    تبدأ تلقائيًا عند التشغيل (systemd على Linux، launchd على macOS)
  </Step>
</Steps>

## المتطلبات

| المتطلب | التفاصيل |
|---------|----------|
| **نظام التشغيل** | Linux (Ubuntu/Debian/CentOS/RHEL) أو macOS |
| **البنية** | x64 أو arm64 |
| **الذاكرة** | 1 جيجابايت كحد أدنى، 2 جيجابايت موصى بها |
| **API Key** | [احصل على واحد هنا](https://lemondata.cc) |

## بعد التثبيت

يعرض المثبّت بيانات الوصول الخاصة بك:

```
✓ LemonClaw installed successfully!

  WebUI:        http://your-ip:18789
  Token:        your-gateway-token
  Auto-login:   http://your-ip:18789?token=your-gateway-token
  Model:        claude-sonnet-4-6
```

<Warning>
**احفظ الـ Token الخاص بك!** تحتاجه للوصول إلى WebUI وربط منصات المراسلة.
</Warning>

## ربط منصات المراسلة

قم بالوصول إلى WebUI على `http://your-ip:18789` باستخدام الـ Token الخاص بك، ثم اتبع الأدلة الخاصة بكل منصة:

<CardGroup cols={2}>
  <Card title="Telegram" icon="paper-plane" href="/claw/telegram" />
  <Card title="Discord" icon="discord" href="/claw/discord" />
  <Card title="WhatsApp" icon="whatsapp" href="/claw/whatsapp" />
  <Card title="Slack" icon="slack" href="/claw/slack" />
  <Card title="DingTalk" icon="bell" href="/claw/dingtalk" />
  <Card title="WeCom" icon="building" href="/claw/wecom" />
</CardGroup>

## أوامر الإدارة

<Tabs>
  <Tab title="Linux">
    ```bash
    systemctl --user status lemonclaw    # التحقق من الحالة
    journalctl --user -u lemonclaw -f    # عرض السجلات
    systemctl --user restart lemonclaw   # إعادة التشغيل
    systemctl --user stop lemonclaw      # الإيقاف
    ```
  </Tab>
  <Tab title="macOS">
    ```bash
    tail -f ~/.lemonclaw/lemonclaw.log    # عرض السجلات
    launchctl stop cc.lemondata.lemonclaw && launchctl start cc.lemondata.lemonclaw  # إعادة التشغيل
    launchctl stop cc.lemondata.lemonclaw  # الإيقاف
    ```
  </Tab>
</Tabs>

## الإعدادات

موقع ملف الإعدادات: `~/.lemonclaw/config.json`

بعد التعديل، أعد تشغيل الخدمة لتطبيق التغييرات.

### تغيير النموذج الافتراضي

```json
{
  "agents": {
    "defaults": {
      "model": {
        "primary": "lemondata-claude/claude-opus-4-6"
      }
    }
  }
}
```

بادئات النماذج المتاحة:
- `lemondata-claude/` — نماذج Claude (anthropic-messages API)
- `lemondata-minimax/` — نماذج MiniMax (anthropic-messages API)
- `lemondata/` — جميع النماذج الأخرى (openai-completions API)

## الترقية

```bash
curl -fsSL https://raw.githubusercontent.com/hedging8563/lemonclaw/main/deploy/self-hosted/install.sh | bash
# ثم أعد تشغيل الخدمة
systemctl --user restart lemonclaw  # Linux
# أو
launchctl stop cc.lemondata.lemonclaw && launchctl start cc.lemondata.lemonclaw  # macOS
```

## إلغاء التثبيت

```bash
# إيقاف وتعطيل الخدمة
systemctl --user stop lemonclaw && systemctl --user disable lemonclaw  # Linux
launchctl unload ~/Library/LaunchAgents/cc.lemondata.lemonclaw.plist  # macOS

# إزالة LemonClaw
rm -rf ~/.lemonclaw
rm -rf ~/.lemonclaw
```

## التثبيت الذاتي مقابل المُدار

| | التثبيت الذاتي | المُدار (Claw) |
|---|---|---|
| **تكلفة المثيل** | مجاني | $20 / 30 يومًا |
| **استخدام الذكاء الاصطناعي** | الدفع حسب الاستخدام | الدفع حسب الاستخدام |
| **البنية التحتية** | خادمك الخاص | نحن ندير كل شيء |
| **التحديثات** | يدوي (`npm update`) | تلقائي |
| **الدعم** | المجتمع | أولوية |

<Info>
**الكود المصدري**: [github.com/hedging8563/lemonclaw](https://github.com/hedging8563/lemonclaw)
</Info>
