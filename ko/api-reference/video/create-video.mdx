---
title: "비디오 생성"
openapi: "POST /v1/videos/generations"
description: "비디오 생성 작업을 생성합니다"
---

## 개요

비디오 생성은 비동기 방식입니다. 요청을 제출하면 작업 ID와 `poll_url`을 받은 다음, 결과를 polling합니다.

<Info>
가장 안정적인 polling 동작을 위해 생성 요청에서 반환된 정확한 `poll_url`을 따르세요.
</Info>

<Warning>
오디오 출력은 모델에 따라 달라집니다. LemonData 에서는 `output_audio` 를 생략하면 Veo 3 계열이 기본적으로 오디오 켜짐으로 처리됩니다. 모델이 오디오 제어를 지원하는 경우에는 `output_audio` 로 명시적으로 전환하세요. camelCase 별칭 `outputAudio` 도 호환성을 위해 허용됩니다.
</Warning>


## 요청 본문

<ParamField body="model" type="string" default="sora-2">
  비디오 모델 ID입니다. API 기본값은 `sora-2`입니다. 현재 모델 매트릭스와 지원 기능은 [Video Generation Guide](/guides/video-generation)를 참조하세요.
</ParamField>

<ParamField body="prompt" type="string" required>
  생성할 비디오에 대한 텍스트 설명입니다.
</ParamField>

<ParamField body="image_url" type="string">
  image-to-video (I2V) 생성에 사용할 시작 이미지의 공개적으로 접근 가능한 URL입니다. 모델 간 호환성을 최대화하려면 `image_url` 사용을 권장합니다.
</ParamField>

<ParamField body="image" type="string">
  data URL 형식의 인라인 이미지입니다(예: `data:image/jpeg;base64,...`). 호환되는 모델에서 지원되지만, `image_url`이 비디오 모델 전반에서 가장 넓은 호환성을 제공합니다.
</ParamField>

<ParamField body="duration" type="integer">
  비디오 길이(초)입니다(모델에 따라 다름).
</ParamField>

<ParamField body="aspect_ratio" type="string">
  화면 비율입니다(예: `16:9`, `9:16`, `1:1`).
</ParamField>

<ParamField body="resolution" type="string">
  비디오 해상도입니다(예: `1080p`, `720p`).
</ParamField>

<ParamField body="output_audio" type="boolean">
  모델 종속 오디오 출력 토글입니다. LemonData 에서는 이 필드를 생략하면 Veo 3 계열이 기본값 `true` 로 처리됩니다. 다른 비디오 모델은 각자의 정의된 기본 동작을 따릅니다. camelCase 별칭 `outputAudio` 도 호환성을 위해 허용됩니다.
</ParamField>

<ParamField body="fps" type="integer">
  초당 프레임 수입니다(1-120).
</ParamField>

<ParamField body="negative_prompt" type="string">
  비디오 생성에서 피하고 싶은 요소입니다.
</ParamField>

<ParamField body="seed" type="integer">
  재현 가능한 생성을 위한 랜덤 seed입니다.
</ParamField>

<ParamField body="cfg_scale" type="number">
  classifier-free guidance scale입니다(0-20).
</ParamField>

<ParamField body="motion_strength" type="number">
  움직임 강도입니다(0-1).
</ParamField>

<ParamField body="start_image" type="string">
  시작 프레임 이미지의 URL입니다.
</ParamField>

<ParamField body="end_image" type="string">
  종료 프레임 이미지의 URL입니다.
</ParamField>

<ParamField body="user" type="string">
  최종 사용자를 위한 고유 식별자입니다.
</ParamField>

## 응답

<ResponseField name="task_id" type="string">
  polling에 사용할 고유 작업 식별자입니다.
</ResponseField>

<ResponseField name="poll_url" type="string">
  이 작업에 권장되는 polling URL입니다. 상태를 확인할 때 이 정확한 경로를 사용하세요.
</ResponseField>

<ResponseField name="status" type="string">
  초기 상태: `pending`.
</ResponseField>

<ResponseField name="model" type="string">
  사용된 모델입니다.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST "https://api.lemondata.cc/v1/videos/generations" \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sora-2",
    "prompt": "A cat walking through a garden, cinematic lighting",
    "operation": "text-to-video",
    "duration": 4,
    "aspect_ratio": "16:9"
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.lemondata.cc/v1/videos/generations",
    headers={
        "Authorization": "Bearer sk-your-api-key",
        "Content-Type": "application/json"
    },
    json={
        "model": "sora-2",
        "prompt": "A cat walking through a garden, cinematic lighting",
        "operation": "text-to-video",
        "duration": 4,
        "aspect_ratio": "16:9"
    }
)

data = response.json()
task_id = data["task_id"]
print(f"Poll URL: {data['poll_url']}")
print(f"Task ID: {task_id}")
```

```javascript JavaScript
const response = await fetch('https://api.lemondata.cc/v1/videos/generations', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer sk-your-api-key',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'sora-2',
    prompt: 'A cat walking through a garden, cinematic lighting',
    operation: 'text-to-video',
    duration: 4,
    aspect_ratio: '16:9'
  })
});

const data = await response.json();
console.log(`Poll URL: ${data.poll_url}`);
console.log(`Task ID: ${data.task_id}`);
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "net/http"
)

func main() {
    payload := map[string]interface{}{
        "model":    "sora-2",
        "prompt":   "A cat walking through a garden, cinematic lighting",
        "operation": "text-to-video",
        "duration": 4,
        "aspect_ratio": "16:9",
    }
    body, _ := json.Marshal(payload)

    req, _ := http.NewRequest("POST", "https://api.lemondata.cc/v1/videos/generations", bytes.NewBuffer(body))
    req.Header.Set("Authorization", "Bearer sk-your-api-key")
    req.Header.Set("Content-Type", "application/json")

    client := &http.Client{}
    resp, _ := client.Do(req)
    defer resp.Body.Close()

    var result map[string]interface{}
    json.NewDecoder(resp.Body).Decode(&result)
    fmt.Printf("Poll URL: %s\n", result["poll_url"])
    fmt.Printf("Task ID: %s\n", result["task_id"])
}
```

```php PHP
<?php
$ch = curl_init('https://api.lemondata.cc/v1/videos/generations');

curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_HTTPHEADER => [
        'Content-Type: application/json',
        'Authorization: Bearer sk-your-api-key'
    ],
    CURLOPT_POSTFIELDS => json_encode([
        'model' => 'sora-2',
        'prompt' => 'A cat walking through a garden, cinematic lighting',
        'operation' => 'text-to-video',
        'duration' => 4,
        'aspect_ratio' => '16:9'
    ])
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
echo "Poll URL: " . $data['poll_url'] . "\n";
echo "Task ID: " . $data['task_id'];
```
</RequestExample>

<ResponseExample>
```json Response
{
  "task_id": "video_abc123",
  "poll_url": "/v1/tasks/video_abc123",
  "status": "pending",
  "model": "sora-2",
  "created_at": 1706000000
}
```
</ResponseExample>

## 이미지에서 비디오로

```python
response = requests.post(
    "https://api.lemondata.cc/v1/videos/generations",
    headers={"Authorization": "Bearer sk-your-api-key"},
    json={
        "model": "hailuo-2.3-fast",
        "prompt": "The scene begins from the provided image and adds gentle natural motion.",
        "operation": "image-to-video",
        "image_url": "https://example.com/image.jpg",
        "duration": 6,
        "aspect_ratio": "16:9"
    }
)
```

## 키프레임 제어

첫 번째 프레임과 마지막 프레임을 제어하려면 `start_image`와 `end_image`를 사용하세요(Vidu Q2 Pro에서 지원):

```python
response = requests.post(
    "https://api.lemondata.cc/v1/videos/generations",
    headers={"Authorization": "Bearer sk-your-api-key"},
    json={
        "model": "viduq2-pro",
        "prompt": "Smooth transition from day to night",
        "operation": "시작-종료 프레임 기반 비디오",
        "start_image": "https://example.com/day.jpg",
        "end_image": "https://example.com/night.jpg",
        "duration": 5,
        "resolution": "720p",
        "aspect_ratio": "16:9"
    }
)
```

## 사용 가능한 모델

<Info>
모델 목록은 자주 업데이트됩니다. 최신 사용 가능 모델과 가격은 [Models page](https://lemondata.cc/ko/models)에서 확인하세요.
</Info>

기능 비교와 사용 예제를 포함한 전체 가이드는 [Video Generation Guide](/guides/video-generation)를 참조하세요.

## 현재 활성화된 공개 비디오 모델

<Info>
이 목록은 현재 문서 빌드 시점의 공개 비디오 모델 활성 inventory 와 일치합니다. 최신 상태는 [Models API](/api-reference/models/list-models)를 확인하세요.
</Info>

### OpenAI

| Model | 공개 작업 |
|-------|-------------------|
| `sora-2` | 텍스트-투-비디오, image-to-video |
| `sora-2-pro` | 텍스트-투-비디오, image-to-video |
| `sora-2-pro-storyboard` | 이미지-투-비디오 |

### Kuaishou

| Model | 공개 작업 |
|-------|-------------------|
| `kling-3.0-motion-control` | 모션 제어 |
| `kling-3.0-video` | 텍스트-투-비디오, image-to-video, 시작-종료 프레임 기반 비디오 |
| `kling-v2.5-turbo-pro` | 텍스트-투-비디오, image-to-video, 시작-종료 프레임 기반 비디오 |
| `kling-v2.5-turbo-std` | 텍스트-투-비디오, image-to-video |
| `kling-v2.6-pro` | 텍스트-투-비디오, image-to-video, 시작-종료 프레임 기반 비디오 |
| `kling-v2.6-std` | 텍스트-투-비디오, image-to-video |
| `kling-v3.0-pro` | 텍스트-투-비디오, image-to-video, 시작-종료 프레임 기반 비디오 |
| `kling-v3.0-std` | 텍스트-투-비디오, image-to-video, 시작-종료 프레임 기반 비디오 |
| `kling-video-o1-pro` | 텍스트-투-비디오, image-to-video, reference-to-video, 시작-종료 프레임 기반 비디오, video-to-video |
| `kling-video-o1-std` | 텍스트-투-비디오, image-to-video, reference-to-video, 시작-종료 프레임 기반 비디오, video-to-video |

### Google

| Model | 공개 작업 |
|-------|-------------------|
| `veo3` | 텍스트-투-비디오, image-to-video |
| `veo3-fast` | 텍스트-투-비디오, image-to-video |
| `veo3-pro` | 텍스트-투-비디오, image-to-video |
| `veo3.1` | 텍스트-투-비디오, image-to-video, reference-to-video, 시작-종료 프레임 기반 비디오 |
| `veo3.1-fast` | 텍스트-투-비디오, image-to-video, reference-to-video, 시작-종료 프레임 기반 비디오 |
| `veo3.1-pro` | 텍스트-투-비디오, image-to-video, 시작-종료 프레임 기반 비디오 |

### ByteDance

| Model | 공개 작업 |
|-------|-------------------|
| `seedance-1.5-pro` | 텍스트-투-비디오, image-to-video |

### MiniMax

| Model | 공개 작업 |
|-------|-------------------|
| `hailuo-2.3-fast` | 이미지-투-비디오 |
| `hailuo-2.3-pro` | 텍스트-투-비디오, image-to-video |
| `hailuo-2.3-standard` | 텍스트-투-비디오, image-to-video |

### Alibaba

| Model | 공개 작업 |
|-------|-------------------|
| `wan-2.2-plus` | 텍스트-투-비디오, image-to-video |
| `wan-2.5` | 텍스트-투-비디오, image-to-video |
| `wan-2.6` | 텍스트-투-비디오, image-to-video, reference-to-video |

### Shengshu

| Model | 공개 작업 |
|-------|-------------------|
| `viduq2` | 텍스트-투-비디오, reference-to-video |
| `viduq2-pro` | 이미지-투-비디오, reference-to-video, 시작-종료 프레임 기반 비디오 |
| `viduq2-pro-fast` | 이미지-투-비디오, 시작-종료 프레임 기반 비디오 |
| `viduq2-turbo` | 이미지-투-비디오, 시작-종료 프레임 기반 비디오 |
| `viduq3-pro` | 텍스트-투-비디오, image-to-video, 시작-종료 프레임 기반 비디오 |
| `viduq3-turbo` | 텍스트-투-비디오, image-to-video, 시작-종료 프레임 기반 비디오 |

### xAI

| Model | 공개 작업 |
|-------|-------------------|
| `grok-imagine-image-to-video` | 이미지-투-비디오 |
| `grok-imagine-text-to-video` | 텍스트-투-비디오 |
| `grok-imagine-upscale` | 비디오-투-비디오 |


### Other

| Model | 공개 작업 |
|-------|-------------------|
| `topaz-video-upscale` | 비디오-투-비디오 |
