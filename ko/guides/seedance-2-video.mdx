---
title: "Seedance 2.0 비디오 모델"
description: "Seedance 2.0, Seedance 2.0 Fast, Seedance 2.0 Mini 중 비디오 생성 모델을 선택합니다."
---

Seedance 2.0은 text-to-video, image-to-video, reference-to-video, 비디오 편집, 비디오 확장을 위한 비디오 모델 패밀리입니다. `model`에는 TokenLab에 표시되는 모델 ID를 사용하고, `operation`으로 워크플로를 선택하세요.

## 모델 선택

| 모델 | 적합한 용도 | 공개 해상도 | 참조 입력 |
|------|-------------|-------------|-----------|
| `seedance-2.0` | 최고 품질 및 4K 출력 | `480p`, `720p`, `1080p`, `4k` | `reference-to-video`에서 최대 9개 참조 이미지, 최대 3개 참조 비디오, 3개 참조 오디오 |
| `seedance-2.0-fast` | 더 빠르고 저렴한 480p/720p 출력 | `480p`, `720p` | `reference-to-video`에서 최대 9개 참조 이미지, 최대 3개 참조 비디오, 3개 참조 오디오 |
| `seedance-2.0-mini` | 가장 낮은 비용의 480p/720p 출력 | `480p`, `720p` | Seedance 2.0 작업 패밀리를 지원합니다. 정확한 참조 개수 제한에 의존하기 전에 현재 모델 세부정보를 확인하세요 |

`seedance-2.0-fast`와 `seedance-2.0-mini`는 `480p` / `720p`만 지원합니다. 이 모델들에 `1080p` 또는 `4k`를 지정하면 오류가 반환됩니다.

## 지원 작업

Seedance 2.0 패밀리의 모델 세부정보은 다음을 지원합니다.

| 작업 | 일반 입력 |
|------|-----------|
| `text-to-video` | `prompt` |
| `image-to-video` | `prompt`, `image_url` 또는 호환 이미지 입력 |
| `start-end-to-video` | `prompt`, `start_image`, `end_image` |
| `reference-to-video` | `prompt`, `reference_images`, 선택적 `video_urls`, 선택적 `audio_urls` |
| `video-to-video` | `prompt`, `video_url` 또는 호환 비디오 입력 |
| `video-extension` | `task_id` 또는 모델별 확장 입력 |

오디오만 사용하거나 텍스트와 오디오만 사용하는 Seedance 요청은 모델 세부정보에 포함되지 않습니다. 오디오는 지원되는 `reference-to-video` 흐름의 참조 입력으로만 사용하세요.

## 소재 그룹과 소재 ID

Seedance 2.0 소재는 TokenLab 소재 그룹으로 관리됩니다. 가상 인물/아바타 소재는 `library_type: "aigc_avatar"`, 인증된 실제 인물 소재는 `library_type: "liveness_face"` 를 사용합니다. [소재 그룹 엔드포인트](/ko/api-reference/video/create-material-asset-group)로 그룹을 만들거나 조회한 뒤 [소재 만들기](/ko/api-reference/video/create-material-asset)로 업로드하세요.

전체 업로드 요청 본문, 상태 필드, 예시는 [소재 만들기](/ko/api-reference/video/create-material-asset) 레퍼런스를 확인하세요.

`group_id` 는 이 엔드포인트가 반환하는 TokenLab 소재 그룹 ID입니다. 생략하면 TokenLab은 선택한 `library_type` 에 대한 현재 조직의 기본 그룹을 사용합니다. 소재가 `ACTIVE` 상태가 되면 반환된 `id` 를 `material_asset_id` 로 전달하거나 `material_asset_ids` 에 포함해 `POST /v1/videos/generations` 를 호출하세요. 소재 ID는 Seedance의 최대 9개 이미지 참조 제한에 포함됩니다.

## 4K Reference-To-Video

4K 출력이 필요하면 `seedance-2.0`을 사용하세요. 기본 모델은 관리된 모델 세부정보에서 `reference-to-video`를 포함해 `4k`를 지원합니다.

```bash
curl -X POST "https://api.tokenlab.sh/v1/videos/generations" \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "seedance-2.0",
    "operation": "reference-to-video",
    "prompt": "같은 제품 정체성과 조명을 유지하면서 느린 영화적 회전을 만듭니다.",
    "reference_images": [
      "https://example.com/product-front.jpg",
      "https://example.com/product-side.jpg"
    ],
    "video_urls": ["https://example.com/motion-reference.mp4"],
    "duration": 6,
    "resolution": "4k",
    "aspect_ratio": "16:9"
  }'
```

## Fast 및 Mini 요청

지연 시간이 중요하면 `seedance-2.0-fast`를 사용하고, 가장 낮은 비용이 더 중요하면 `seedance-2.0-mini`를 사용하세요.

```bash
curl -X POST "https://api.tokenlab.sh/v1/videos/generations" \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "seedance-2.0-fast",
    "operation": "text-to-video",
    "prompt": "흰색 스튜디오 배경에서 깨끗한 제품 공개 장면, 부드러운 카메라 푸시인.",
    "duration": 5,
    "resolution": "720p",
    "aspect_ratio": "16:9"
  }'
```

## 가격 및 청구

Seedance 2.0 가격은 token 기반이며 출력 해상도와 요청에 비디오 입력이 포함되는지에 따라 달라집니다.

제품 UI에 가격을 고정하기 전에 [Pricing API](/ko/api-reference/pricing/get-pricing) 또는 [Models API](/ko/api-reference/models/list-models)로 현재 청구 가격을 확인하세요.

## 비동기 결과

비디오 생성은 비동기입니다. 생성 응답의 `poll_url`을 따르거나 반환된 작업 ID로 `GET /v1/tasks/{id}`를 호출하세요.

엔드포인트 세부정보는 [비디오 생성](/ko/api-reference/video/create-video), [작업 상태 가져오기](/ko/api-reference/tasks/get-task-status), [작업 취소](/ko/api-reference/tasks/cancel-task)를 참조하세요.
