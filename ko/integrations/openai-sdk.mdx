---
title: "OpenAI SDK"
description: "공식 OpenAI SDK 및 OpenAI 호환 클라이언트와 함께 LemonData 사용하기"
---

## 개요

LemonData는 클라이언트를 `https://api.lemondata.cc/v1`로 지정하여 공식 OpenAI SDK와 함께 동작합니다.

새 프로젝트에서는 **Responses API**를 우선적으로 사용하세요. 프레임워크, plugin 또는 레거시 코드 경로가 여전히 이를 요구하는 경우에만 **Chat Completions**를 유지하세요.

<Note>
Python, JavaScript, Go에는 공식 OpenAI SDK가 있습니다. PHP는 OpenAI 호환 community client와 잘 동작하지만, 공식 OpenAI SDK는 아닙니다.
</Note>

## 설치

<CodeGroup>

```bash Python
pip install openai
```

```bash JavaScript
npm install openai
```

```bash Go
go get github.com/openai/openai-go/v3
```

</CodeGroup>

## 클라이언트 구성

<CodeGroup>

```python Python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-lemondata-key",
    base_url="https://api.lemondata.cc/v1",
)
```

```javascript JavaScript
import OpenAI from 'openai';

const client = new OpenAI({
  apiKey: 'sk-your-lemondata-key',
  baseURL: 'https://api.lemondata.cc/v1',
});
```

```go Go
package main

import (
    openai "github.com/openai/openai-go/v3"
    "github.com/openai/openai-go/v3/option"
)

func main() {
    client := openai.NewClient(
        option.WithAPIKey("sk-your-lemondata-key"),
        option.WithBaseURL("https://api.lemondata.cc/v1"),
    )

    _ = client
}
```

</CodeGroup>

## 권장 사항: Responses API

<CodeGroup>

```python Python
response = client.responses.create(
    model="gpt-5.4",
    input="Explain what LemonData does in one sentence."
)

print(response.output_text)
```

```javascript JavaScript
const response = await client.responses.create({
  model: 'gpt-5.4',
  input: 'Explain what LemonData does in one sentence.',
});

console.log(response.output_text);
```

```bash cURL
curl https://api.lemondata.cc/v1/responses \
  -H "Authorization: Bearer sk-your-lemondata-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-5.4",
    "input": "Explain what LemonData does in one sentence."
  }'
```

</CodeGroup>

## Responses로 스트리밍

<CodeGroup>

```python Python
stream = client.responses.create(
    model="gpt-5.4",
    input="Write a short poem about coding.",
    stream=True,
)

for event in stream:
    if event.type == "response.output_text.delta":
        print(event.delta, end="")
```

```javascript JavaScript
const stream = await client.responses.create({
  model: 'gpt-5.4',
  input: 'Write a short poem about coding.',
  stream: true,
});

for await (const event of stream) {
  if (event.type === 'response.output_text.delta') {
    process.stdout.write(event.delta);
  }
}
```

</CodeGroup>

## Tools / Function Calling

```python
response = client.responses.create(
    model="gpt-5.4",
    input="What's the weather in Tokyo?",
    tools=[{
        "type": "function",
        "name": "get_weather",
        "description": "Get weather for a location",
        "parameters": {
            "type": "object",
            "properties": {
                "location": {"type": "string"}
            },
            "required": ["location"]
        }
    }]
)

for item in response.output:
    if item.type == "function_call":
        print(item.name)
        print(item.arguments)
```

## Responses로 Vision 사용

```python
response = client.responses.create(
    model="gpt-4o",
    input=[{
        "role": "user",
        "content": [
            {"type": "input_text", "text": "What's in this image?"},
            {"type": "input_image", "image_url": "https://example.com/image.jpg"}
        ]
    }]
)

print(response.output_text)
```

## Embeddings

```python
response = client.embeddings.create(
    model="text-embedding-3-small",
    input="Hello world"
)

print(response.data[0].embedding[:5])
```

## Chat Completions 호환성

프레임워크 또는 plugin이 여전히 Chat Completions를 요구하는 경우, LemonData는 표준 OpenAI 호환 경로도 지원합니다:

```python
response = client.chat.completions.create(
    model="gpt-4o",
    messages=[
        {"role": "system", "content": "You are a helpful assistant."},
        {"role": "user", "content": "Hello!"}
    ]
)

print(response.choices[0].message.content)
```

<Warning>
호환성을 위해서만 Chat Completions를 사용하세요. 새 통합에는 `client.responses.create(...)`를 우선적으로 사용하세요.
</Warning>

## 문제 해결

<AccordionGroup>
  <Accordion title="연결 오류">
    - base URL이 정확히 `https://api.lemondata.cc/v1`인지 확인하세요
    - proxy 간섭 또는 사용자 지정 HTTP client override가 있는지 확인하세요
    - provider 동작을 디버깅하기 전에 SDK 버전이 최신인지 확인하세요
  </Accordion>

  <Accordion title="인증 실패">
    - API key가 `sk-`로 시작하는지 확인하세요
    - LemonData dashboard에서 key가 활성 상태인지 확인하세요
    - SDK가 `Authorization: Bearer ...`를 전송하고 있는지 확인하세요
  </Accordion>

  <Accordion title="잘못된 API 경로">
    - `responses.create(...)`는 요청을 `/v1/responses`로 보냅니다
    - `chat.completions.create(...)`는 요청을 `/v1/chat/completions`로 보냅니다
    - 현재 앱이 Chat Completions만 지원한다면, 해당 호환 경로를 계속 사용하세요
  </Accordion>
</AccordionGroup>