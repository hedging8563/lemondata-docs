---
title: "Gemini CLI"
description: "LemonData API를 사용하기 위한 Google Gemini CLI 설정"
---

## 개요

Google Gemini CLI는 Gemini 모델과 상호작용하기 위한 명령줄 도구입니다. LemonData는 Gemini CLI를 사용하여 300개 이상의 모델에 액세스할 수 있도록 호환되는 엔드포인트를 제공합니다.

## 시스템 요구사항

- **Node.js**: 버전 20.0+
- **운영체제**: Windows 10/11, macOS 10.15+, Ubuntu 20.04+, 또는 Debian 10+

## 설치

```bash
npm install -g @google/gemini-cli
```

설치 확인:

```bash
gemini --version
```

## 설정

### 1단계: API Key 받기

1. [LemonData Dashboard](https://lemondata.cc/dashboard)에 로그인합니다.
2. [API Keys](https://lemondata.cc/dashboard/api)로 이동합니다.
3. API Key를 생성하고 복사합니다 (형식: `sk-...`).

### 2단계: 환경 변수 설정

**임시 (현재 세션):**

```bash
export GEMINI_API_KEY="sk-your-lemondata-key"
export GOOGLE_GEMINI_BASE_URL="https://api.lemondata.cc"
```

**영구 설정:**

사용 중인 shell 설정 파일에 추가하세요:

<Tabs>
  <Tab title="Bash">
    ```bash
    echo 'export GEMINI_API_KEY="sk-your-lemondata-key"' >> ~/.bashrc
    echo 'export GOOGLE_GEMINI_BASE_URL="https://api.lemondata.cc"' >> ~/.bashrc
    source ~/.bashrc
    ```
  </Tab>
  <Tab title="Zsh">
    ```bash
    echo 'export GEMINI_API_KEY="sk-your-lemondata-key"' >> ~/.zshrc
    echo 'export GOOGLE_GEMINI_BASE_URL="https://api.lemondata.cc"' >> ~/.zshrc
    source ~/.zshrc
    ```
  </Tab>
  <Tab title="Fish">
    ```bash
    set -Ux GEMINI_API_KEY "sk-your-lemondata-key"
    set -Ux GOOGLE_GEMINI_BASE_URL "https://api.lemondata.cc"
    ```
  </Tab>
</Tabs>

<Warning>
설정을 완료한 후 변경 사항을 적용하려면 터미널을 다시 시작하세요.
</Warning>

<Note>
**알려진 제한 사항**: 일부 경우 캐시된 Google 세션이 있으면 Gemini CLI가 `GOOGLE_GEMINI_BASE_URL`을 따르지 않을 수 있습니다. 연결 문제가 발생하면 CLI 캐시를 지우거나 새 터미널 세션을 시작해 보세요.
</Note>

## 기본 사용법

프로젝트 디렉토리에서 Gemini CLI를 실행합니다:

```bash
cd your-project
gemini
```

처음 실행할 때 다음 작업이 필요합니다:
1. 테마 선택
2. 보안 주의사항 확인
3. 작업 디렉토리 신뢰

## 사용 가능한 모델

| 모델 | 설명 |
|-------|-------------|
| `gemini-2.5-pro` | 가장 강력한 Gemini 모델 |
| `gemini-2.5-flash` | 대부분의 작업에 적합하며 빠르고 효율적임 |
| `gemini-2.0-flash` | 이전 세대 버전, 안정적임 |

## 주요 명령어

**질문하기:**

```
> What is the best way to structure a React app?
```

**코드 분석:**

```
> Explain the code in src/main.ts
```

**코드 생성:**

```
> Create a Python function to parse JSON files
```

**변경 사항 검토:**

```
> Review the recent git changes and suggest improvements
```

## 설정 확인

```bash
# 환경 변수 확인
echo $GEMINI_API_KEY
echo $GOOGLE_GEMINI_BASE_URL

# 연결 테스트
gemini
```

## 문제 해결

<AccordionGroup>
  <Accordion title="연결 오류">
    - `GOOGLE_GEMINI_BASE_URL`이 `https://api.lemondata.cc`로 설정되었는지 확인하세요.
    - 참고: Gemini 엔드포인트에는 `/v1` 접미사가 필요하지 않습니다.
    - 네트워크 연결을 확인하세요.
  </Accordion>

  <Accordion title="인증 실패">
    - `GEMINI_API_KEY` 환경 변수가 설정되었는지 확인하세요.
    - Key가 `sk-`로 시작하는지 확인하세요.
    - LemonData 대시보드(dashboard)에서 해당 Key가 활성화 상태인지 확인하세요.
  </Accordion>

  <Accordion title="모델 사용 불가">
    - [lemondata.cc/en/models](https://lemondata.cc/zh-TW/models)에서 Gemini 모델의 가용성을 확인하세요.
    - 다른 Gemini 모델 변형을 시도해 보세요.
  </Accordion>
</AccordionGroup>

## 권장 사항

<AccordionGroup>
  <Accordion title="프로젝트 디렉토리에서 사용">
    컨텍스트를 더 잘 이해할 수 있도록 항상 프로젝트 루트 디렉토리에서 Gemini CLI를 실행하세요.
  </Accordion>

  <Accordion title="디렉토리 신뢰 시 주의">
    본인이 소유한 디렉토리만 신뢰하세요. Gemini CLI는 파일을 읽고 수정할 수 있습니다.
  </Accordion>

  <Accordion title="생성된 코드 검토">
    프로젝트에 커밋하기 전에 항상 AI가 생성한 코드를 검토하세요.
  </Accordion>
</AccordionGroup>