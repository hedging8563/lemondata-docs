---
title: "✨ Claude Code 스킬"
description: "5분 퀵스타트 가이드 - Claude Code가 모든 LemonData API를 자동으로 통합하도록 하세요"
---

<Note>
이 스킬을 사용하면 간단한 영어로 요구 사항을 설명하는 것만으로 LemonData의 **수백 개의 AI API** 중 하나를 통합할 수 있습니다!
</Note>

## 이 스킬로 무엇을 할 수 있나요?

코드에서 AI 기능(GPT-4, 이미지 생성, 음성 인식 등)을 사용하고 싶을 때, 이 스킬은 다음과 같은 작업을 수행합니다:

1. ✅ LemonData의 수백 개의 API를 **자동으로 검색**합니다
2. ✅ 요구 사항에 **가장 적합한** API를 찾습니다
3. ✅ **완전하고 실행 가능한 코드 생성**
4. ✅ **API Key 설정**
5. ✅ **사용 예시** 및 모범 사례 제공

**간단히 말해: 자연어로 요구 사항을 설명하기만 하면 완전한 API 통합 코드를 얻을 수 있습니다!**

## 1단계: 스킬 설치

<Tabs>
  <Tab title="npx (추천)">
    가장 간단한 설치 방법:

    ```bash
    npx add-skill hedging8563/lemondata-api-skill -y
    ```

    이 명령은 감지된 모든 프로그래밍 어시스턴트(Claude Code, Cursor, Copilot 등)에 이 스킬을 자동으로 설치합니다.
  </Tab>
  <Tab title="GitHub URL 공유">
    1. 터미널이나 IDE에서 **Claude Code**를 엽니다
    2. 대화창에 이 링크를 붙여넣으세요:
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
    3. Claude Code가 이를 스킬 저장소로 인식하고 설치를 도와줍니다
  </Tab>
  <Tab title="Git Clone">
    **개별 설치**(모든 프로젝트에 적용):
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git ~/.claude/skills/lemondata-api-integration
    ```

    **프로젝트별 설치**(git을 통해 팀과 공유):
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git .claude/skills/lemondata-api-integration
    ```
  </Tab>
</Tabs>

### 설치 확인

Claude Code에게 물어보세요:
```
What skills are available?
```

`lemondata-api-integration`이 보인다면 설치가 성공한 것입니다!

## 2단계: API Key 가져오기

이 스킬을 사용하기 전에 LemonData API Key가 필요합니다.

<Tip>
**API Key를 미리 설정할 필요가 없습니다!** 이 스킬을 사용할 때 Claude Code가 자동으로 API Key를 묻고 생성된 코드에 포함시킵니다.
</Tip>

<Steps>
  <Step title="LemonData 방문">
    [lemondata.cc](https://lemondata.cc)로 이동하세요
  </Step>
  <Step title="로그인">
    계정을 생성하거나 로그인하세요
  </Step>
  <Step title="API Key 가져오기">
    [Dashboard → API Keys](https://lemondata.cc/dashboard/api)로 이동하여 새 키를 생성하세요
  </Step>
  <Step title="키 복사">
    키는 `sk-...`로 시작합니다 - 안전하게 보관하세요
  </Step>
</Steps>

<Warning>
**중요 팁:**
- 모든 LemonData API는 동일한 API Key를 사용합니다
- 키를 타인과 공유하지 마세요
- 키를 GitHub에 커밋하지 마세요
</Warning>

## 3단계: 사용 시작하기

이 스킬을 사용하는 것은 채팅만큼 간단합니다!

### 예시 1: GPT-4 사용

**당신의 말:**
```
I want to use GPT-4 in my Python project
```

**Claude Code는 다음과 같이 수행합니다:**
1. API Key 요청
2. GPT-4 관련 API 검색
3. 사용 가능한 API 옵션 표시
4. 완전한 Python 코드 생성
5. 사용 방법 설명

**결과물:**
```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.lemondata.cc/v1"
)

response = client.chat.completions.create(
    model="gpt-4o",
    messages=[{"role": "user", "content": "Hello!"}]
)

print(response.choices[0].message.content)
```

### 예시 2: 이미지 생성

**당신의 말:**
```
How do I generate images with Flux in Node.js?
```

**Claude Code는 다음과 같이 수행합니다:**
- 이미지 생성 API 검색
- JavaScript 코드 생성
- 이미지 다운로드 및 저장 코드 포함

### 예시 3: 음성 인식

**당신의 말:**
```
Integrate a speech-to-text API
```

**Claude Code는 다음과 같이 수행합니다:**
- 사용 중인 프로그래밍 언어 확인
- 음성 인식 API 검색
- 선택한 언어로 코드 생성
- 오디오 파일 처리 예시 제공

## 지원되는 기능

이 스킬은 다음과 같은 모든 AI 기능을 통합하는 데 도움이 됩니다:

| 기능 유형 | 예시 |
|-------------|----------|
| 💬 대화 | GPT-4o, Claude, Gemini, DeepSeek |
| 🎨 이미지 생성 | Midjourney, Flux, Stable Diffusion |
| 🎬 비디오 생성 | Sora, Runway, Kling, Luma AI |
| 🎵 음악 생성 | Suno |
| 🗿 3D 모델 | Tripo3D |
| 🎤 오디오 | 텍스트 음성 변환 (TTS), 음성 텍스트 변환 (STT) |
| 📊 벡터 임베딩 | text-embedding-3 |
| 🔄 재순위화 | bce-reranker, qwen3-rerank |

## 사용 팁

### 1. 요구 사항을 직접 설명하세요

<Tabs>
  <Tab title="❌ 이렇게 하지 마세요">
    ```
    Help me check if LemonData has an image generation API
    ```
  </Tab>
  <Tab title="✅ 이렇게 하기를 권장합니다">
    ```
    I want to generate images in Python
    ```
  </Tab>
</Tabs>

### 2. 언어 지정

특정 언어 요구 사항이 있는 경우 직접 말씀하세요:

```
Use JavaScript to call GPT-4
Use Python for speech recognition
Use Go to generate images
```

### 3. 시나리오 설명

특정 요구 사항이 있는 경우 자세히 설명하세요:

```
I'm building a chatbot, need to use GPT-4
My website needs to let users upload and transform images
I'm making a voice notes app, need speech-to-text
```

## 중요 주의 사항

### API Key 보안

<CardGroup cols={2}>
  <Card title="❌ 절대 하지 마세요" icon="xmark" color="#ef4444">
    - API Key를 프론트엔드 코드에 직접 넣기
    - API Key를 GitHub에 커밋
    - 타인과 API Key 공유
  </Card>
  <Card title="✅ 모범 사례" icon="check" color="#22c55e">
    - 환경 변수 사용
    - 웹사이트의 경우 백엔드에서 API 호출
    - Next.js, Express 등의 프레임워크 사용
  </Card>
</CardGroup>

### 비용 관리

- API 호출마다 크레딧이 소모됩니다
- [대시보드](https://lemondata.cc/dashboard)에서 사용량 제한을 설정하세요
- 테스트 시 적은 데이터 양을 사용하세요

### 오류 처리

생성된 코드에는 기본적인 오류 처리가 포함되어 있지만, 다음이 추가로 필요할 수 있습니다:
- 재시도 메커니즘 추가
- 오류 로그 기록
- 사용자 친화적인 오류 메시지 제공

## 자주 묻는 질문 (FAQ)

<AccordionGroup>
  <Accordion title="스킬이 자동으로 트리거되지 않나요?">
    요청 시 "LemonData" 또는 "LemonData API"를 언급하거나 요구 사항을 더 구체적으로 설명해 보세요:
    ```
    Use LemonData to integrate GPT-4 in my project
    ```
  </Accordion>

  <Accordion title="원하는 API를 찾을 수 없나요?">
    다른 키워드를 시도해 보세요:
    - "chat" → "dialogue" 또는 "conversation"
    - "image" → "picture" 또는 "generation"
    - "voice" → "audio" 또는 "speech"
  </Accordion>

  <Accordion title="생성된 코드에 오류가 있나요?">
    다음 사항을 확인하세요:
    1. API Key가 정확한가요?
    2. 필요한 라이브러리가 설치되었나요? (예: Python의 `openai`)
    3. LemonData 서버에 접속할 수 있나요?
    4. API Key에 충분한 크레딧이 있나요?
  </Accordion>

  <Accordion title="스킬을 어떻게 업데이트하나요?">
    GitHub 링크를 Claude Code에게 다시 보내세요:
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
  </Accordion>
</AccordionGroup>

## 리소스

<CardGroup cols={2}>
  <Card title="LemonData 웹사이트" icon="globe" href="https://lemondata.cc">
    메인 웹사이트 및 대시보드
  </Card>
  <Card title="API 문서" icon="book" href="https://docs.lemondata.cc">
    전체 API 참조 가이드
  </Card>
  <Card title="모델" icon="robot" href="https://lemondata.cc/zh-TW/models">
    사용 가능한 모든 모델 탐색
  </Card>
  <Card title="가격" icon="dollar-sign" href="https://lemondata.cc/#pricing">
    공식 가격보다 30% 저렴
  </Card>
</CardGroup>

## 시작하기!

이제 이 스킬을 사용하는 방법을 알게 되었습니다. Claude Code에게 물어보세요:

```
I want to use GPT-4 in my project
```

또는

```
Help me integrate image generation
```

Claude Code가 이 스킬을 사용하여 자동으로 모든 작업을 완료해 줄 것입니다!

<Info>
**문의 사항이 있으신가요?** [GitHub Issues](https://github.com/hedging8563/lemondata-api-skill/issues)를 확인하거나 [support@lemondata.cc](mailto:support@lemondata.cc)로 문의해 주세요
</Info>