---
title: "소개"
description: "통합 API를 통해 300개 이상의 AI 모델에 액세스하세요"
---

## LemonData란 무엇인가요?

LemonData는 OpenAI, Anthropic, Google, DeepSeek 등 주요 제공업체의 **300개 이상의 AI 모델**에 대한 통합 액세스를 제공하는 AI API 애그리게이터 플랫폼입니다.

<CardGroup cols={2}>
  <Card title="OpenAI 호환" icon="plug">
    OpenAI API를 직접 대체할 수 있습니다. base URL만 변경하면 됩니다.
  </Card>
  <Card title="300개 이상의 모델" icon="robot">
    GPT-4o, Claude, Gemini, DeepSeek, Midjourney, Sora 등에 액세스하세요.
  </Card>
  <Card title="종량제 요금제" icon="coins">
    구독이 필요 없습니다. 사용한 만큼만 지불하며, 매우 경쟁력 있는 가격을 제공합니다.
  </Card>
  <Card title="고가용성" icon="shield-check">
    자동 장애 조치(failover) 및 부하 분산으로 안정적인 서비스를 보장합니다.
  </Card>
</CardGroup>

## 지원 기능

| 기능 | 설명 | 예시 모델 |
|------------|-------------|----------------|
| **대화** | 텍스트 생성 및 대화 | GPT-4o, Claude 3.5, Gemini 2.0 |
| **비전** | 이미지 이해 및 분석 | GPT-4o, Claude 3.5 Sonnet, Gemini |
| **이미지 생성** | 텍스트 기반 이미지 생성 | DALL-E 3, Midjourney, Flux, Ideogram |
| **비디오 생성** | 텍스트/이미지 기반 비디오 생성 | Sora, Runway Gen-3, Kling |
| **오디오** | 텍스트 음성 변환 및 전사 | Whisper, TTS-1, MiniMax |
| **Embeddings** | 텍스트 벡터화 | text-embedding-3-small |
| **음악** | AI 음악 생성 | Suno |
| **3D 생성** | 3D 모델 생성 | Tripo3D |

## 핵심 기능

<CardGroup cols={2}>
  <Card title="멀티 포맷 API" icon="shuffle" href="/guides/api-formats">
    단일 API key로 OpenAI, Anthropic 또는 Gemini의 기본 포맷을 사용할 수 있습니다. SDK를 전환할 필요가 없습니다.
  </Card>
  <Card title="시맨틱 캐싱" icon="bolt" href="/guides/caching">
    지능형 캐싱으로 비용과 지연 시간을 줄입니다. 유사한 쿼리는 자동으로 캐시를 사용합니다.
  </Card>
  <Card title="스마트 라우팅" icon="route">
    제공업체 간의 자동 장애 조치로 고가용성과 최적의 가격을 보장합니다.
  </Card>
  <Card title="확장 사고" icon="brain">
    기본 Anthropic 포맷을 통해 Claude의 확장 사고 모드 액세스를 지원합니다.
  </Card>
</CardGroup>

## 빠른 예제

<CodeGroup>

```bash cURL
curl https://api.lemondata.cc/v1/chat/completions \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o",
    "messages": [{"role": "user", "content": "Hello!"}]
  }'
```

```python Python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.lemondata.cc/v1"
)

response = client.chat.completions.create(
    model="gpt-4o",
    messages=[{"role": "user", "content": "Hello!"}]
)

print(response.choices[0].message.content)
```

```javascript JavaScript
import OpenAI from 'openai';

const client = new OpenAI({
  apiKey: 'sk-your-api-key',
  baseURL: 'https://api.lemondata.cc/v1'
});

const response = await client.chat.completions.create({
  model: 'gpt-4o',
  messages: [{ role: 'user', content: 'Hello!' }]
});

console.log(response.choices[0].message.content);
```

```go Go
package main

import (
    "context"
    "fmt"
    "github.com/sashabaranov/go-openai"
)

func main() {
    config := openai.DefaultConfig("sk-your-api-key")
    config.BaseURL = "https://api.lemondata.cc/v1"

    client := openai.NewClientWithConfig(config)

    resp, _ := client.CreateChatCompletion(
        context.Background(),
        openai.ChatCompletionRequest{
            Model: "gpt-4o",
            Messages: []openai.ChatCompletionMessage{
                {Role: openai.ChatMessageRoleUser, Content: "Hello!"},
            },
        },
    )
    fmt.Println(resp.Choices[0].Message.Content)
}
```

```php PHP
<?php
$ch = curl_init('https://api.lemondata.cc/v1/chat/completions');

curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_HTTPHEADER => [
        'Content-Type: application/json',
        'Authorization: Bearer sk-your-api-key'
    ],
    CURLOPT_POSTFIELDS => json_encode([
        'model' => 'gpt-4o',
        'messages' => [
            ['role' => 'user', 'content' => 'Hello!']
        ]
    ])
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
echo $data['choices'][0]['message']['content'];
```

</CodeGroup>

## 다음 단계

<CardGroup cols={2}>
  <Card title="빠른 시작" icon="rocket" href="/quickstart">
    2 분 안에 API key를 받고 첫 번째 요청을 보내보세요.
  </Card>
  <Card title="API 참조" icon="code" href="/api-reference/introduction">
    전체 API 문서를 살펴보세요.
  </Card>
  <Card title="모델" icon="robot" href="https://lemondata.cc/zh-TW/models">
    사용 가능한 300개 이상의 모든 모델을 찾아보세요.
  </Card>
  <Card title="가격" icon="dollar-sign" href="https://lemondata.cc/zh-TW/models">
    모든 모델의 투명한 가격 정책을 확인하세요.
  </Card>
</CardGroup>