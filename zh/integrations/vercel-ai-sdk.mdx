---
title: "Vercel AI SDK"
description: "使用 OpenAI-compatible 或 Responses-native provider 将 LemonData 集成到 Vercel AI SDK"
---

## 概述

对于 LemonData，在 Vercel AI SDK 中最稳定的默认选项是 **OpenAI-compatible provider**。

如果你明确需要 **Responses-native** 行为，可以切换到 OpenAI provider，同时保留相同的 LemonData base URL。

## 推荐默认方案：OpenAI-Compatible Provider

```bash
npm install ai @ai-sdk/openai-compatible
```

```typescript
import { createOpenAICompatible } from '@ai-sdk/openai-compatible';

export const lemondata = createOpenAICompatible({
  name: 'lemondata',
  apiKey: process.env.LEMONDATA_API_KEY,
  baseURL: 'https://api.lemondata.cc/v1',
});
```

## 生成文本

```typescript
import { generateText } from 'ai';
import { lemondata } from './lemondata';

const { text } = await generateText({
  model: lemondata.chatModel('gpt-5.4'),
  prompt: 'Explain LemonData in one sentence.',
});

console.log(text);
```

## 流式输出文本

```typescript
import { streamText } from 'ai';
import { lemondata } from './lemondata';

const result = await streamText({
  model: lemondata.chatModel('gpt-5.4'),
  prompt: 'Write a short poem about coding.',
});

for await (const textPart of result.textStream) {
  process.stdout.write(textPart);
}
```

## 工具调用

```typescript
import { generateText, tool } from 'ai';
import { z } from 'zod';
import { lemondata } from './lemondata';

const result = await generateText({
  model: lemondata.chatModel('gpt-5.4'),
  prompt: 'What is the weather in San Francisco?',
  tools: {
    weather: tool({
      description: 'Get weather in a location',
      parameters: z.object({
        location: z.string(),
      }),
      execute: async ({ location }) => ({
        location,
        temperature: 72,
        condition: 'sunny',
      }),
    }),
  },
});

console.log(result.text);
```

## 结构化输出

```typescript
import { generateObject } from 'ai';
import { z } from 'zod';
import { lemondata } from './lemondata';

const { object } = await generateObject({
  model: lemondata.chatModel('gpt-5.4'),
  schema: z.object({
    name: z.string(),
    role: z.string(),
  }),
  prompt: 'Generate a fake developer profile.',
});

console.log(object);
```

## 如果你明确需要 Responses-Native 行为

```bash
npm install ai @ai-sdk/openai
```

```typescript
import { createOpenAI } from '@ai-sdk/openai';

export const lemondataResponses = createOpenAI({
  apiKey: process.env.LEMONDATA_API_KEY,
  baseURL: 'https://api.lemondata.cc/v1',
});
```

```typescript
import { generateText } from 'ai';
import { lemondataResponses } from './lemondata-responses';

const { text } = await generateText({
  model: lemondataResponses('gpt-5.4'),
  prompt: 'Explain LemonData in one sentence.',
});
```

<Note>
对于代理式集成，使用 `@ai-sdk/openai-compatible` 作为安全的默认选项。仅当你明确希望使用基于 `/v1/responses` 构建的 provider 路径时，才切换到 `@ai-sdk/openai`。
</Note>

## 环境变量

```bash
# .env.local
LEMONDATA_API_KEY=sk-your-lemondata-key
```

## 最佳实践

<AccordionGroup>
  <Accordion title="优先使用 openai-compatible">
    对于第三方网关和代理后端，`@ai-sdk/openai-compatible` 通常是最稳妥、最不容易产生意外的起点。
  </Accordion>

  <Accordion title="仅在需要时切换到 Responses">
    如果你需要与 `/v1/responses` 绑定的 provider 行为，请有意识地切换 provider package，而不是在同一个 client 中混用这两种模式。
  </Accordion>

  <Accordion title="在服务器端保存密钥">
    不要在客户端代码中暴露你的 LemonData API key。请将 provider 设置放在服务器文件或 API 路由中。
  </Accordion>
</AccordionGroup>