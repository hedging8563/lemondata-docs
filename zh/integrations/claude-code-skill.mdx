---
title: "✨ Claude Code 技能"
description: "5 分钟快速入门指南 - 让 Claude Code 为您自动集成任何 LemonData API"
---

<Note>
此技能让您只需用简单的英语描述需求，即可集成 LemonData 的**数百个 AI API**。该 API 是 **Agent-First**（智能体优先）的 —— 即使 Claude 猜错了模型名称，错误响应也会准确告知其修复方法。
</Note>

## 此技能可以做什么？

当您想在代码中使用 AI 功能（如 GPT-4、图像生成、语音识别等）时，此技能将：

1. ✅ **直接尝试 API** — 无需搜索
2. ✅ **从错误中自我纠正** — 结构化错误提示将引导下一次尝试
3. ✅ **以您选择的语言生成完整的、可运行的代码**
4. ✅ **根据实时价格和可用性选择最佳模型**
5. ✅ **通过原生端点建议优化您的集成**

**简而言之：只需描述您的需求，即可获得可用的 API 集成代码 —— 即使第一次尝试并不完美，API 也会引导 Claude 找到正确答案。**

## 步骤 1：安装技能

<Tabs>
  <Tab title="npx (推荐)">
    最简单的安装方式：

    ```bash
    npx add-skill hedging8563/lemondata-api-skill -y
    ```

    这将自动将技能安装到所有检测到的编程智能体（Claude Code、Cursor、Copilot 等）。
  </Tab>
  <Tab title="分享 GitHub URL">
    1. 在终端或 IDE 中打开 **Claude Code**
    2. 在聊天中粘贴此链接：
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
    3. Claude Code 将识别其为技能仓库并帮助您安装
  </Tab>
  <Tab title="Git Clone">
    **个人安装**（在所有项目中可用）：
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git ~/.claude/skills/lemondata-api-integration
    ```

    **特定项目安装**（通过 git 与团队共享）：
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git .claude/skills/lemondata-api-integration
    ```
  </Tab>
</Tabs>

### 验证安装

询问 Claude Code：
```
What skills are available?
```

如果您看到 `lemondata-api-integration`，则表示安装成功！

## 步骤 2：获取您的 API Key

在使用此技能之前，您需要一个 LemonData API Key。

<Tip>
**您不需要提前配置 API Key！** 当您使用该技能时，Claude Code 会自动询问您的 API Key 并将其包含在生成的代码中。
</Tip>

<Steps>
  <Step title="访问 LemonData">
    前往 [lemondata.cc](https://lemondata.cc)
  </Step>
  <Step title="登录">
    创建账户或登录
  </Step>
  <Step title="获取 API Key">
    导航至 [Dashboard → API Keys](https://lemondata.cc/dashboard/api) 并创建一个新 Key
  </Step>
  <Step title="复制 Key">
    您的 Key 以 `sk-...` 开头 - 请妥善保存
  </Step>
</Steps>

<Warning>
**重要提示：**
- 所有 LemonData API 都使用相同的 API Key
- 不要与他人分享您的 Key
- 不要将您的 Key 提交到 GitHub
</Warning>

## 步骤 3：开始使用

使用该技能就像聊天一样简单！

### 示例 1：使用 GPT-4

**您说：**
```
I want to use GPT-4 in my Python project
```

**Claude Code 将：**
1. 尝试使用最佳模型猜测直接调用 API
2. 如果模型名称错误，从错误中读取 `did_you_mean` 并自动纠正
3. 使用正确的模型生成完整的 Python 代码

**您得到：**
```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.lemondata.cc/v1"
)

response = client.chat.completions.create(
    model="gpt-4o",
    messages=[{"role": "user", "content": "Hello!"}]
)

print(response.choices[0].message.content)
```

### 示例 2：生成图像

**您说：**
```
How do I generate images with Flux in Node.js?
```

**Claude Code 将：**
- 调用图像生成 API
- 生成带有适当错误处理的 JavaScript 代码
- 包含图像下载和保存代码

### 示例 3：语音识别

**您说：**
```
Integrate a speech-to-text API
```

**Claude Code 将：**
- 询问您正在使用的编程语言
- 使用正确的音频端点生成代码
- 提供音频文件处理示例

## Agent-First API 特性

LemonData 的 API 专为 AI Agent 设计。每个错误响应都包含结构化提示：

### 智能错误恢复

| 错误 | API 返回内容 | Claude 的操作 |
|-------|---------------------|-----------------|
| 模型名称错误 | `did_you_mean` + `suggestions` | 自动纠正并重试 |
| 余额不足 | `balance_usd` + 更便宜的 `suggestions` | 切换到负担得起的模型 |
| 模型不可用 | `alternatives` + `retry_after` | 切换到可用模型 |
| 频率限制 | `retry_after` (精确秒数) | 等待并重试 |
| 上下文过长 | 带有更大模型的 `suggestions` | 切换到更大上下文的模型 |

### 模型发现

无需搜索文档。使用这些 API 端点：

```bash
# 读取机器可读的 API 概览
curl https://lemondata.cc/llms.txt

# 按类别列出模型
curl "https://api.lemondata.cc/v1/models?category=chat" -H "Authorization: Bearer sk-KEY"
curl "https://api.lemondata.cc/v1/models?category=image" -H "Authorization: Bearer sk-KEY"

# 按能力过滤
curl "https://api.lemondata.cc/v1/models?tag=coding" -H "Authorization: Bearer sk-KEY"
```

### 原生端点优化

当 Claude 对 Claude 或 Gemini 模型使用 `/v1/chat/completions` 时，API 会返回优化标头：

```
X-LemonData-Hint: This model supports native Anthropic format. Use POST /v1/messages for better performance.
X-LemonData-Native-Endpoint: /v1/messages
```

Claude Code 将自动切换到原生端点以获得更好的性能。

## 支持的功能

此技能可以帮助您集成所有这些 AI 能力：

| 功能类型 | 示例 |
|-------------|----------|
| 💬 聊天 | GPT-4o, Claude, Gemini, DeepSeek |
| 🎨 图像生成 | Midjourney, Flux, Stable Diffusion |
| 🎬 视频生成 | Sora, Runway, Kling, Luma AI |
| 🎵 音乐生成 | Suno |
| 🗿 3D 模型 | Tripo3D |
| 🎤 音频 | Text-to-Speech, Speech-to-Text |
| 📊 嵌入 | text-embedding-3 |
| 🔄 重排序 | bce-reranker, qwen3-rerank |

## 使用技巧

### 1. 只需说明您的需求

<Tabs>
  <Tab title="❌ 不要这样做">
    ```
    Help me check if LemonData has an image generation API
    ```
  </Tab>
  <Tab title="✅ 而要这样做">
    ```
    I want to generate images in Python
    ```
  </Tab>
</Tabs>

### 2. 指定您的语言

如果您有特定的语言要求，直接说出来即可：

```
Use JavaScript to call GPT-4
Use Python for speech recognition
Use Go to generate images
```

### 3. 描述您的场景

如果您有特定需求，请详细描述：

```
I'm building a chatbot, need to use GPT-4
My website needs to let users upload and transform images
I'm making a voice notes app, need speech-to-text
```

## 重要注意事项

### API Key 安全

<CardGroup cols={2}>
  <Card title="❌ 切勿这样做" icon="xmark" color="#ef4444">
    - 将 API Key 直接放在前端代码中
    - 将 API Key 提交到 GitHub
    - 与他人分享 API Key
  </Card>
  <Card title="✅ 最佳实践" icon="check" color="#22c55e">
    - 使用环境变量
    - 在网站后端调用 API
    - 使用 Next.js, Express 等框架
  </Card>
</CardGroup>

### 成本管理

- 每次 API 调用都会消耗额度
- 在您的 [控制面板](https://lemondata.cc/dashboard) 中设置使用限制
- 使用小数据量进行测试
- 检查 `error.balance_usd` 和 `error.estimated_cost_usd` 以通过编程方式监控成本

### 错误处理

生成的代码包含 Agent-First 错误处理，它可以：
- 读取结构化错误提示（`did_you_mean`、`suggestions`、`retry_after`）
- 从常见错误中自动恢复
- 在需要时回退到备选模型

## FAQ

<AccordionGroup>
  <Accordion title="技能没有自动触发？">
    尝试在您的请求中提及 “LemonData” 或 “LemonData API”，或者更具体地描述您的需求：
    ```
    Use LemonData to integrate GPT-4 in my project
    ```
  </Accordion>

  <Accordion title="API 如何帮助 Claude 找到正确的模型？">
    LemonData 的 Agent-First API 返回结构化错误提示。如果 Claude 猜错了模型名称，错误响应会包含带有正确名称的 `did_you_mean` 以及带有备选模型的 `suggestions`。Claude 会读取这些内容并在下一次尝试中自动纠正。
  </Accordion>

  <Accordion title="生成的代码有错误？">
    检查以下几点：
    1. API Key 是否正确？
    2. 是否安装了所需的库？（例如 Python 的 `openai`）
    3. 您能否访问 LemonData 的服务器？
    4. 您的 API Key 是否有足够的额度？
  </Accordion>

  <Accordion title="如何更新技能？">
    再次向 Claude Code 发送 GitHub 链接：
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
  </Accordion>
</AccordionGroup>

## 资源

<CardGroup cols={2}>
  <Card title="LemonData 官网" icon="globe" href="https://lemondata.cc">
    主站和控制面板
  </Card>
  <Card title="API 文档" icon="book" href="https://docs.lemondata.cc">
    完整的 API 参考
  </Card>
  <Card title="模型" icon="robot" href="https://lemondata.cc/zh/models">
    浏览所有可用模型
  </Card>
  <Card title="llms.txt" icon="file-lines" href="https://lemondata.cc/llms.txt">
    为 AI Agent 提供的机器可读 API 概览
  </Card>
</CardGroup>

## 开始使用！

现在您已经知道如何使用此技能了。尝试询问 Claude Code：

```
I want to use GPT-4 in my project
```

或者

```
Help me integrate image generation
```

Claude Code 将自动使用此技能为您完成所有工作 —— 如果出现任何问题，API 的结构化错误提示将引导它找到正确答案！

<Info>
**有疑问？** 请查看我们的 [GitHub Issues](https://github.com/hedging8563/lemondata-api-skill/issues) 或联系 [support@lemondata.cc](mailto:support@lemondata.cc)
</Info>