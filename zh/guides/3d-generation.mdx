---
title: "3D 生成"
description: "使用 TokenLab 3D 生成 API 和异步任务轮询，从文本或图片创建 3D 资产。"
---
## 推荐流程

1. 调用 [模型列表](/zh/api-reference/models/list-models)，带上 `recommended_for=3d`。
2. 用 [创建 3D](/zh/api-reference/3d/create-3d) 发送文本 `prompt`；只有支持图生 3D 的模型才传 `image` 或 `image_url`。
3. 按模型契约选择 `format`、`quality`、`style`、`seed` 等可选字段。
4. 轮询 `poll_url` 到资产就绪，然后下载 `model_url` 或 `glb_url` / `fbx_url` 等格式化 URL。

## 模型发现

3D 生成是任务型 API。应以模型目录为真值，不要假设只有某一个供应商或固定模型。

```bash
curl "https://api.tokenlab.sh/v1/models?recommended_for=3d" \
  -H "Authorization: Bearer sk-your-api-key"
```

## 创建 3D 任务

文生 3D 请求需要 `prompt`。图生 3D 请求再增加 `image` base64 或 `image_url`。`format` 和 `quality` 只在模型契约列出时使用。

```bash
curl https://api.tokenlab.sh/v1/3d/generations \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "tripo3d-v2.5",
    "prompt": "A stylized low-poly robot mascot with clean topology",
    "format": "glb",
    "quality": "standard"
  }'
```

## 轮询结果

优先使用返回的 `poll_url`。稳定兜底入口是 [查询任务状态](/zh/api-reference/tasks/get-task-status)。完成后的 3D 任务返回 `model_url`，也可能包含 `glb_url` 或 `fbx_url` 等格式字段。

```bash
curl "https://api.tokenlab.sh/v1/tasks/$TASK_ID" \
  -H "Authorization: Bearer sk-your-api-key"
```

## API 参考

| 主题 | 参考 |
|-------|-----------|
| 创建 3D | [创建 3D](/zh/api-reference/3d/create-3d) |
| 查询 3D 状态 | [查询 3D 状态](/zh/api-reference/3d/get-3d-status) |
| 查询任务状态 | [查询任务状态](/zh/api-reference/tasks/get-task-status) |
| 模型列表 | [模型列表](/zh/api-reference/models/list-models) |
| 模型详情 | [模型详情](/zh/api-reference/models/get-model) |
