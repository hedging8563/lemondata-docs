---
title: "快速开始"
description: "在 2 分钟内开始使用 LemonData API"
---

## 第 1 步：获取您的 API Key

<Steps>
  <Step title="创建账户">
    使用您的邮箱、Gmail 或 GitHub 账户在 [lemondata.cc](https://lemondata.cc) 注册。
  </Step>
  <Step title="充值额度">
    打开控制台并为您的账户充值额度。LemonData 采用按量付费定价，无最低承诺要求。
  </Step>
  <Step title="创建 API key">
    前往 **Dashboard → API Keys** 并创建一个新 key。请安全保存它，因为它只会显示一次。
  </Step>
</Steps>

<Warning>
请妥善保管您的 API key。切勿在客户端代码或公开代码仓库中暴露它。
</Warning>

## 第 2 步：安装客户端

<CodeGroup>

```bash Python
pip install openai
```

```bash JavaScript
npm install openai
```

```bash Go
go get github.com/openai/openai-go/v3
```

```bash PHP
composer require openai-php/client
```

</CodeGroup>

## 第 3 步：发起您的第一个请求

对于新项目，建议从 **Responses API** 开始。

<CodeGroup>

```bash cURL
curl https://api.lemondata.cc/v1/responses \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-5.4",
    "input": "What is the capital of France?"
  }'
```

```python Python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.lemondata.cc/v1"
)

response = client.responses.create(
    model="gpt-5.4",
    input="What is the capital of France?"
)

print(response.output_text)
```

```javascript JavaScript
import OpenAI from 'openai';

const client = new OpenAI({
  apiKey: 'sk-your-api-key',
  baseURL: 'https://api.lemondata.cc/v1'
});

const response = await client.responses.create({
  model: 'gpt-5.4',
  input: 'What is the capital of France?',
});

console.log(response.output_text);
```

</CodeGroup>

<Note>
如果您的框架或工具仍然需要 Chat Completions，LemonData 也支持 `POST /v1/chat/completions`。对于新的集成，优先使用 `POST /v1/responses`。
</Note>

## 尝试不同的模型

LemonData 支持 300+ 模型。只需修改 `model` 字段：

```python
response = client.responses.create(model="gpt-5.4", input="Hello")
response = client.responses.create(model="gpt-5-mini", input="Hello")
response = client.responses.create(model="claude-sonnet-4-6", input="Hello")
response = client.responses.create(model="gemini-2.5-flash", input="Hello")
response = client.responses.create(model="deepseek-r1", input="Hello")
```

## 启用流式输出

```python
stream = client.responses.create(
    model="gpt-5.4",
    input="Tell me a short story.",
    stream=True
)

for event in stream:
    if event.type == "response.output_text.delta":
        print(event.delta, end="")
```

## 接下来做什么？

<CardGroup cols={2}>
  <Card title="身份验证" icon="key" href="/authentication">
    了解 API key 管理与安全。
  </Card>
  <Card title="OpenAI SDK" icon="code" href="/zh/integrations/openai-sdk">
    使用官方 OpenAI SDK 与 LemonData 集成。
  </Card>
  <Card title="API 参考" icon="book" href="/zh/api-reference/introduction">
    查看完整的 endpoint 参考。
  </Card>
  <Card title="模型" icon="robot" href="https://lemondata.cc/zh/models">
    浏览当前可用模型及其定价。
  </Card>
</CardGroup>