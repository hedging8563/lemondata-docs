---
title: "查询素材详情"
openapi: "GET /v1/videos/assets/{assetId}"
description: "通过 TokenLab 素材 ID 查询一个 Seedance 素材。"
---

查询一个素材及其入库状态。

状态包括 `PROCESSING`、`ACTIVE`、`FAILED`、`DELETED`。只有 `ACTIVE` 后才能用于视频生成。如果是 `FAILED`，读取 `error_message`，修正素材后重新创建。
