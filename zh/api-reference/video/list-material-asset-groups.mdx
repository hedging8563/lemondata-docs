---
title: "查询素材组列表"
openapi: "GET /v1/videos/assets/groups"
description: "查询当前组织拥有的 Seedance 素材组。"
---

查询当前组织拥有的素材组。用 `library_type` 区分 `aigc_avatar` 虚拟人像素材组和 `liveness_face` 真人素材组。

这里返回的真人素材组来自认证绑定流程。上传真人素材时，使用返回的 TokenLab `id` 作为 `group_id`。
