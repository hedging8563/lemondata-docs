---
title: "查询 Seedance 任务列表（火山兼容）"
openapi: "GET /api/v3/contents/generations/tasks"
description: "查询当前组织最近的 Seedance 兼容任务。"
---

## 概览

这些接口面向已经使用火山风格 Seedance 任务结构的团队。鉴权使用 TokenLab Bearer API Key。返回的任务 ID 以 `cgt-` 开头；请轮询状态接口，直到 `status` 变为 `succeeded`、`failed`、`cancelled` 或 `expired`。

另见 [Seedance 2.0 视频模型](/zh/guides/seedance-2-video) 和 [视频生成](/zh/guides/video-generation).

## 响应结构

列表响应返回 `items` 和 `total`。支持 `filter.status`、`filter.task_ids`、`filter.model`、`filter.service_tier` 过滤，并支持 `page_num`、`page_size` 分页。

## 示例

```bash
curl "https://api.tokenlab.sh/api/v3/contents/generations/tasks?page_num=1&page_size=20&filter.status=succeeded" \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```
