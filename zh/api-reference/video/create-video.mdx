---
title: "创建视频"
openapi: "POST /v1/videos/generations"
description: "创建一个视频生成任务"
---

## 概述

视频生成是异步的。你提交请求后，会收到一个任务 ID 和 `poll_url`，然后通过轮询获取结果。

<Info>
为了获得最可靠的轮询行为，请严格使用创建请求返回的 `poll_url`。
</Info>

## 请求体

<ParamField body="model" type="string" default="sora-2">
  视频模型 ID。API 默认值为 `sora-2`。有关当前模型矩阵和支持能力，请参阅 [视频生成指南](/guides/video-generation)。
</ParamField>

<ParamField body="prompt" type="string" required>
  要生成视频的文本描述。
</ParamField>

<ParamField body="image_url" type="string">
  用于 image-to-video (I2V) 生成的起始图片的公开可访问 URL。为了获得最佳的跨模型兼容性，建议优先使用 `image_url`。
</ParamField>

<ParamField body="image" type="string">
  以内联 data URL 形式提供的图片（例如，`data:image/jpeg;base64,...`）。兼容模型支持该方式，但 `image_url` 可在各视频模型之间提供最广泛的兼容性。
</ParamField>

<ParamField body="duration" type="integer">
  视频时长，单位为秒（取决于模型）。
</ParamField>

<ParamField body="aspect_ratio" type="string">
  宽高比（例如：`16:9`、`9:16`、`1:1`）。
</ParamField>

<ParamField body="resolution" type="string">
  视频分辨率（例如：`1080p`、`720p`）。
</ParamField>

<ParamField body="fps" type="integer">
  每秒帧数（1-120）。
</ParamField>

<ParamField body="negative_prompt" type="string">
  视频生成中需要避免的内容。
</ParamField>

<ParamField body="seed" type="integer">
  用于可复现生成的随机种子。
</ParamField>

<ParamField body="cfg_scale" type="number">
  无分类器引导强度（0-20）。
</ParamField>

<ParamField body="motion_strength" type="number">
  运动强度（0-1）。
</ParamField>

<ParamField body="start_image" type="string">
  起始帧图片的 URL。
</ParamField>

<ParamField body="end_image" type="string">
  结束帧图片的 URL。
</ParamField>

<ParamField body="user" type="string">
  终端用户的唯一标识符。
</ParamField>

## 响应

<ResponseField name="task_id" type="string">
  用于轮询的唯一任务标识符。
</ResponseField>

<ResponseField name="poll_url" type="string">
  此任务建议使用的轮询 URL。检查状态时请使用此精确路径。
</ResponseField>

<ResponseField name="status" type="string">
  初始状态：`pending`。
</ResponseField>

<ResponseField name="model" type="string">
  所使用的模型。
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST "https://api.lemondata.cc/v1/videos/generations" \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sora-2",
    "prompt": "A cat walking through a garden, cinematic lighting",
    "duration": 5
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.lemondata.cc/v1/videos/generations",
    headers={
        "Authorization": "Bearer sk-your-api-key",
        "Content-Type": "application/json"
    },
    json={
        "model": "sora-2",
        "prompt": "A cat walking through a garden, cinematic lighting",
        "duration": 5
    }
)

data = response.json()
task_id = data["task_id"]
print(f"Poll URL: {data['poll_url']}")
print(f"Task ID: {task_id}")
```

```javascript JavaScript
const response = await fetch('https://api.lemondata.cc/v1/videos/generations', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer sk-your-api-key',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'sora-2',
    prompt: 'A cat walking through a garden, cinematic lighting',
    duration: 5
  })
});

const data = await response.json();
console.log(`Poll URL: ${data.poll_url}`);
console.log(`Task ID: ${data.task_id}`);
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "net/http"
)

func main() {
    payload := map[string]interface{}{
        "model":    "kling-v2.6-pro",
        "prompt":   "A cat walking through a garden, cinematic lighting",
        "duration": 5,
    }
    body, _ := json.Marshal(payload)

    req, _ := http.NewRequest("POST", "https://api.lemondata.cc/v1/videos/generations", bytes.NewBuffer(body))
    req.Header.Set("Authorization", "Bearer sk-your-api-key")
    req.Header.Set("Content-Type", "application/json")

    client := &http.Client{}
    resp, _ := client.Do(req)
    defer resp.Body.Close()

    var result map[string]interface{}
    json.NewDecoder(resp.Body).Decode(&result)
    fmt.Printf("Poll URL: %s\n", result["poll_url"])
    fmt.Printf("Task ID: %s\n", result["task_id"])
}
```

```php PHP
<?php
$ch = curl_init('https://api.lemondata.cc/v1/videos/generations');

curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_HTTPHEADER => [
        'Content-Type: application/json',
        'Authorization: Bearer sk-your-api-key'
    ],
    CURLOPT_POSTFIELDS => json_encode([
        'model' => 'kling-v2.6-pro',
        'prompt' => 'A cat walking through a garden, cinematic lighting',
        'duration' => 5
    ])
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
echo "Poll URL: " . $data['poll_url'] . "\n";
echo "Task ID: " . $data['task_id'];
```
</RequestExample>

<ResponseExample>
```json Response
{
  "task_id": "video_abc123",
  "poll_url": "/v1/tasks/video_abc123",
  "status": "pending",
  "model": "sora-2",
  "created_at": 1706000000
}
```
</ResponseExample>

## 图生视频

```python
response = requests.post(
    "https://api.lemondata.cc/v1/videos/generations",
    headers={"Authorization": "Bearer sk-your-api-key"},
    json={
        "model": "wan2.6-i2v",
        "prompt": "The person starts walking forward",
        "image_url": "https://example.com/image.jpg"
    }
)
```

## 关键帧控制

使用 `start_image` 和 `end_image` 控制第一帧和最后一帧（由 Seedance 2.0 支持）：

```python
response = requests.post(
    "https://api.lemondata.cc/v1/videos/generations",
    headers={"Authorization": "Bearer sk-your-api-key"},
    json={
        "model": "seedance-2-0",
        "prompt": "Smooth transition from day to night",
        "start_image": "https://example.com/day.jpg",
        "end_image": "https://example.com/night.jpg",
        "duration": 5
    }
)
```

## 可用模型

<Info>
模型列表会频繁更新。要查看最新可用模型和价格，请访问[模型页面](https://lemondata.cc/zh/models)。
</Info>

有关能力对比和使用示例的完整指南，请参阅[视频生成指南](/guides/video-generation)。

### Sora (OpenAI)

| 模型 | 能力 |
|-------|-------------|
| `sora-2` | T2V（默认） |
| `sora-2-pro` | T2V，更高质量 |
| `sora-2-characters` | T2V，角色聚焦 |

### Kling (Kuaishou)

| 模型 | 能力 |
|-------|-------------|
| `kling-v2.6-pro` | T2V，最新一代 |
| `kling-v2.6-std` | T2V，快速 |
| `kling-v2.5-turbo-std` | T2V，turbo 层级 |
| `kling-v2.1-master` | T2V，I2V，master 质量 |
| `kling-v2.1-pro` | T2V，I2V |
| `kling-video` | T2V，I2V |
| `kling-video-extend` | 视频扩展 |
| `kling-video-o1-pro` | T2V，O1 推理 |
| `kling-effects` | 视觉特效 |

### Veo (Google)

| 模型 | 能力 |
|-------|-------------|
| `veo2` | T2V |
| `veo3.1` | T2V |
| `veo3.1-pro` | T2V，专业版 |
| `veo3.1-4k` | T2V，4K 输出 |
| `veo3.1-fast` | T2V，快速 |
| `veo3` | T2V，上一代 |

### Seedance (ByteDance)

| 模型 | 能力 |
|-------|-------------|
| `seedance-2-0` | T2V，I2V，关键帧，扩展，编辑 |
| `seedance-1-5-pro` | T2V，I2V |
| `seedance-1-0-pro` | T2V，I2V |
| `seedance-1-0-lite-t2v` | T2V，轻量级 |
| `seedance-1-0-lite-i2v` | I2V，轻量级 |

### 其他提供商

| 模型 | 提供商 | 能力 |
|-------|----------|-------------|
| `hailuo-2.3` | MiniMax | T2V，I2V |
| `hailuo-2.3-pro` | MiniMax | T2V，I2V，更高质量 |
| `hailuo-2.3-fast` | MiniMax | T2V，I2V，快速层级 |
| `wan-2.6` | Alibaba | T2V |
| `wan2.6-i2v` | Alibaba | I2V |
| `runwayml-gen4-turbo-5` | Runway | T2V，I2V（5 秒） |
| `runwayml-gen4-turbo-10` | Runway | T2V，I2V（10 秒） |
| `luma-video-api` | Luma | T2V |
| `luma-video-extend-api` | Luma | 视频扩展 |
| `viduq3-pro` | Shengshu | T2V，I2V |
| `viduq3-turbo` | Shengshu | T2V，I2V，turbo 层级 |
| `viduq2-pro` | Shengshu | T2V，I2V |
| `grok-video-3` | xAI | T2V |
| `higgsfield-turbo` | Higgsfield | T2V |