---
title: "查询素材列表"
openapi: "GET /v1/videos/assets"
description: "查询当前组织拥有的 Seedance 素材。"
---

查询当前组织拥有的素材。可用 `library_type`、`group_id` 或 `status` 过滤，找到对应生成流程需要的素材。

只有 `status` 为 `ACTIVE` 的素材可用于生成。响应里的素材 URL 可能是临时检查地址，部分 URL 可能约 12 小时后过期。
