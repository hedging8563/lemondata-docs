---
title: "获取任务状态"
description: "使用创建接口返回的 poll_url 获取任何异步任务的状态和结果"
---

## 概览

使用此端点对视频、图像、音乐和 3D 生成等任务类型进行统一的异步轮询。

<Info>
如果创建响应中包含 `poll_url`，请调用该确切路径。某些图像模型可能会在特定于图像的状态路径或 `/v1/tasks/{id}` 下返回基于任务的响应。
</Info>

## 路径参数

<ParamField path="id" type="string" required>
  创建请求返回的任务 ID。
</ParamField>

## 响应

<ResponseField name="id" type="string">
  规范的异步任务标识符。
</ResponseField>

<ResponseField name="task_id" type="string">
  异步任务标识符别名。
</ResponseField>

<ResponseField name="poll_url" type="string">
  当创建响应提供轮询 URL 时，首选的轮询 URL。
</ResponseField>

<ResponseField name="status" type="string">
  任务状态，例如 `pending`、`processing`、`completed` 或 `failed`。
</ResponseField>

<ResponseField name="progress" type="number">
  提供商报告时的进度百分比。
</ResponseField>

<ResponseField name="video_url" type="string">
  任务完成并生成视频时的结果资源 URL。
</ResponseField>

<ResponseField name="video" type="object">
  包含 `url`、`duration`、`width` 和 `height` 的单个视频负载（如果可用）。
</ResponseField>

<ResponseField name="videos" type="array">
  当上游返回多个输出时的多个视频负载。
</ResponseField>

<ResponseField name="error" type="string">
  任务失败时的错误消息。
</ResponseField>

<ResponseField name="created" type="integer">
  创建时间戳（如果可用）。
</ResponseField>

<ResponseField name="updated" type="integer">
  最后更新时间戳（如果可用）。
</ResponseField>

<ResponseField name="model" type="string">
  提供商报告时任务所使用的模型。
</ResponseField>

## 示例

```bash cURL
curl "https://api.lemondata.cc/v1/tasks/video_abc123" \
  -H "Authorization: Bearer sk-your-api-key"
```

```python Python
import requests

poll_url = "/v1/tasks/video_abc123"
response = requests.get(
    f"https://api.lemondata.cc{poll_url}",
    headers={"Authorization": "Bearer sk-your-api-key"},
)
print(response.json())
```

```javascript JavaScript
const pollUrl = '/v1/tasks/video_abc123';
const response = await fetch(`https://api.lemondata.cc${pollUrl}`, {
  headers: { Authorization: 'Bearer sk-your-api-key' },
});
console.log(await response.json());
```