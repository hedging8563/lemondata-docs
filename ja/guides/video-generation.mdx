---
title: "動画生成"
description: "11のプロバイダーにまたがるモデルで動画を生成 — text-to-video、image-to-video、keyframe制御、動画延長、編集"
---

## 概要

LemonData は、11 のプロバイダーの動画生成モデルへのアクセスを、単一の統一 API を通じて提供します。動画生成は **非同期** です — リクエストを送信すると、task ID と `poll_url` を受け取り、その後結果を取得するためにポーリングします。

<Info>
モデル一覧は頻繁に更新されます。最新の利用可能なモデルと価格については、[Models page](https://lemondata.cc/ja/models) を参照するか、[Models API](/api-reference/models/list-models) を使用してください。
</Info>

<Warning>
Audio behavior is model-dependent. In LemonData, Veo 3 family requests default to audio-on when `output_audio` is omitted. Some models are silent by default or silent-only. For passthrough-governed models, pass `output_audio=true` to make a best-effort upstream audio request.
</Warning>


### 非同期ワークフロー

```mermaid
sequenceDiagram
    participant Client
    participant LemonData
    participant Provider

    Client->>LemonData: POST /v1/videos/generations
    LemonData->>Provider: Forward request
    Provider-->>LemonData: task_id + pending
    LemonData-->>Client: { task_id, poll_url, status: "pending" }

    loop Poll every 5-10s
        Client->>LemonData: GET poll_url
        LemonData->>Provider: Query status
        Provider-->>LemonData: Status update
        LemonData-->>Client: { status, video_url? }
    end
```

<CodeGroup>

```python Python
import requests
import time

API_KEY = "sk-your-api-key"
BASE = "https://api.lemondata.cc/v1"
headers = {"Authorization": f"Bearer {API_KEY}"}

# Step 1: Submit generation request
resp = requests.post(f"{BASE}/videos/generations",
    headers=headers,
    json={
        "model": "sora-2",
        "prompt": "A calm cinematic shot of a cat walking through a sunlit garden.",
        "operation": "text-to-video",
        "duration": 4,
        "aspect_ratio": "16:9"
    }
)
create_data = resp.json()
task_id = create_data["task_id"]
poll_url = create_data["poll_url"]

# Step 2: Poll for result
while True:
    status = requests.get(f"https://api.lemondata.cc{poll_url}", headers=headers).json()
    if status["status"] in ("completed", "succeeded"):
        print(f"Video URL: {status['video_url']}")
        break
    elif status["status"] == "failed":
        print(f"Failed: {status.get('error')}")
        break
    time.sleep(10)
```

```javascript JavaScript
const API_KEY = 'sk-your-api-key';
const BASE = 'https://api.lemondata.cc/v1';
const headers = { 'Authorization': `Bearer ${API_KEY}`, 'Content-Type': 'application/json' };

// Step 1: Submit
const { task_id, poll_url } = await fetch(`${BASE}/videos/generations`, {
  method: 'POST', headers,
  body: JSON.stringify({
    model: 'sora-2',
    prompt: 'A calm cinematic shot of a cat walking through a sunlit garden.',
    operation: 'text-to-video',
    duration: 4,
    aspect_ratio: '16:9'
  })
}).then(r => r.json());

// Step 2: Poll
const poll = setInterval(async () => {
  const status = await fetch(`https://api.lemondata.cc${poll_url}`, { headers }).then(r => r.json());
  if (['completed', 'succeeded'].includes(status.status)) {
    console.log('Video URL:', status.video_url);
    clearInterval(poll);
  } else if (status.status === 'failed') {
    console.error('Failed:', status.error);
    clearInterval(poll);
  }
}, 10000);
```

</CodeGroup>

## モデル機能マトリクス

モデルごとに得意なタスクは異なります。このマトリクスを使用して、ユースケースに適したモデルを選択してください。

**凡例**: ✅ 対応 | ❌ 非対応

| Series | Provider | T2V | I2V | Keyframe | Extension | Editing | Max Duration |
|--------|----------|-----|-----|----------|-----------|---------|-------------|
| **Sora** | OpenAI | ✅ | ❌ | ❌ | ❌ | ❌ | ~20s |
| **Kling** | Kuaishou | ✅ | ✅ | ❌ | ✅ | ❌ | 10s |
| **Veo** | Google | ✅ | ✅ | ✅ | ❌ | ❌ | 8s |
| **Seedance** | ByteDance | ✅ | ✅ | ✅ | ✅ | ✅ | 10s |
| **Hailuo** | MiniMax | ✅ | ✅ | ❌ | ❌ | ❌ | 10s |
| **Wan** | Alibaba | ✅ | ✅ | ❌ | ❌ | ❌ | 5s |
| **Runway** | Runway | ❌ | ✅ | ❌ | ❌ | ❌ | 10s |
| **Luma** | Luma | ✅ | ❌ | ❌ | ✅ | ❌ | 5s |
| **Vidu** | Vidu | ✅ | ✅ | ✅ | ❌ | ❌ | 8s |
| **Grok** | xAI | ✅ | ❌ | ❌ | ❌ | ❌ | ~15s |
| **Higgsfield** | Higgsfield | ✅ | ❌ | ❌ | ❌ | ❌ | ~5s |

### 機能の定義

- **T2V (Text-to-Video)**: テキストプロンプトから動画を生成
- **I2V (Image-to-Video)**: 静止画像を動画化します。最も広い互換性のため、公開された `image_url` を指定してください。
- **Keyframe**: `start_image` + `end_image` で開始フレームと終了フレームを制御
- **Extension**: 既存動画の長さを延長
- **Editing**: 既存動画の特定の要素を変更

## 現在の公開モデル一覧

### OpenAI

| Model | 公開オペレーション |
|-------|-------------------|
| `sora-2` | テキストから動画, 画像から動画 |
| `sora-2-pro` | テキストから動画, 画像から動画 |
| `sora-2-pro-storyboard` | 画像から動画 |

### Kuaishou

| Model | 公開オペレーション |
|-------|-------------------|
| `kling-3.0-motion-control` | モーション制御 |
| `kling-3.0-video` | テキストから動画, 画像から動画, 開始・終了フレームから動画 |
| `kling-v2.5-turbo-pro` | テキストから動画, 画像から動画, 開始・終了フレームから動画 |
| `kling-v2.5-turbo-std` | テキストから動画, 画像から動画 |
| `kling-v2.6-pro` | テキストから動画, 画像から動画, 開始・終了フレームから動画 |
| `kling-v2.6-std` | テキストから動画, 画像から動画 |
| `kling-v3.0-pro` | テキストから動画, 画像から動画, 開始・終了フレームから動画 |
| `kling-v3.0-std` | テキストから動画, 画像から動画, 開始・終了フレームから動画 |
| `kling-video-o1-pro` | テキストから動画, 画像から動画, 参考画像から動画, 開始・終了フレームから動画, 動画から動画 |
| `kling-video-o1-std` | テキストから動画, 画像から動画, 参考画像から動画, 開始・終了フレームから動画, 動画から動画 |

### Google

| Model | 公開オペレーション |
|-------|-------------------|
| `veo3` | テキストから動画, 画像から動画 |
| `veo3-fast` | テキストから動画, 画像から動画 |
| `veo3-pro` | テキストから動画, 画像から動画 |
| `veo3.1` | テキストから動画, 画像から動画, 参考画像から動画, 開始・終了フレームから動画 |
| `veo3.1-fast` | テキストから動画, 画像から動画, 参考画像から動画, 開始・終了フレームから動画 |
| `veo3.1-pro` | テキストから動画, 画像から動画, 開始・終了フレームから動画 |

### ByteDance

| Model | 公開オペレーション |
|-------|-------------------|
| `seedance-1.5-pro` | テキストから動画, 画像から動画 |

### MiniMax

| Model | 公開オペレーション |
|-------|-------------------|
| `hailuo-2.3-fast` | 画像から動画 |
| `hailuo-2.3-pro` | テキストから動画, 画像から動画 |
| `hailuo-2.3-standard` | テキストから動画, 画像から動画 |

### Alibaba

| Model | 公開オペレーション |
|-------|-------------------|
| `wan-2.2-plus` | テキストから動画, 画像から動画 |
| `wan-2.5` | テキストから動画, 画像から動画 |
| `wan-2.6` | テキストから動画, 画像から動画, 参考画像から動画 |

### Shengshu

| Model | 公開オペレーション |
|-------|-------------------|
| `viduq2` | テキストから動画, 参考画像から動画 |
| `viduq2-pro` | 画像から動画, 参考画像から動画, 開始・終了フレームから動画 |
| `viduq2-pro-fast` | 画像から動画, 開始・終了フレームから動画 |
| `viduq2-turbo` | 画像から動画, 開始・終了フレームから動画 |
| `viduq3-pro` | テキストから動画, 画像から動画, 開始・終了フレームから動画 |
| `viduq3-turbo` | テキストから動画, 画像から動画, 開始・終了フレームから動画 |

### xAI

| Model | 公開オペレーション |
|-------|-------------------|
| `grok-imagine-image-to-video` | 画像から動画 |
| `grok-imagine-text-to-video` | テキストから動画 |
| `grok-imagine-upscale` | 動画から動画 |


### Other

| Model | 公開オペレーション |
|-------|-------------------|
| `topaz-video-upscale` | 動画から動画 |


## 使用例

### Text-to-Video (T2V)

最も一般的なユースケースです。すべてのモデルがこれをサポートしています。

```python
response = requests.post(f"{BASE}/videos/generations",
    headers=headers,
    json={
        "model": "sora-2",
        "prompt": "A calm cinematic shot of a cat walking through a sunlit garden.",
        "operation": "テキストから動画",
        "duration": 4,
        "aspect_ratio": "16:9"
    }
)
```

### Image-to-Video (I2V)

静止画像を動画化します。動画モデル間で最も広い互換性を得るには、元画像をホストし、`image_url` を渡してください。

```python
# Using image URL
response = requests.post(f"{BASE}/videos/generations",
    headers=headers,
    json={
        "model": "hailuo-2.3-fast",
        "prompt": "The scene begins from the provided image and adds gentle natural motion.",
        "operation": "画像から動画",
        "image_url": "https://example.com/portrait.jpg",
        "duration": 6,
        "aspect_ratio": "16:9"
    }
)

```

### Keyframe 制御 (開始 + 終了画像)

正確なトランジションのために、最初と最後のフレームの両方を制御します。現在は Seedance 2.0 でサポートされています。

```python
response = requests.post(f"{BASE}/videos/generations",
    headers=headers,
    json={
        "model": "viduq2-pro",
        "prompt": "Smooth transition from day to night, city lights gradually turning on",
        "operation": "開始・終了フレームから動画",
        "start_image": "https://example.com/city-day.jpg",
        "end_image": "https://example.com/city-night.jpg",
        "duration": 5,
        "resolution": "720p",
        "aspect_ratio": "16:9"
    }
)
```

### 動画延長

既存動画の長さを延長します。延長機能を持つモデルを使用してください。

```python
response = requests.post(f"{BASE}/videos/generations",
    headers=headers,
    json={
        "model": "kling-video-extend",
        "prompt": "Continue the scene naturally",
        "image_url": "https://example.com/last-frame.jpg"
    }
)
```

## パラメータリファレンス

| Parameter | Type | Description |
|-----------|------|-------------|
| `model` | string | モデル ID（デフォルト: `sora-2`） |
| `prompt` | string | **必須。** 動画のテキスト説明 |
| `image_url` | string | 開始画像の URL（I2V 用） |
| `image` | string | data URL としてのインライン画像。互換モデルでサポートされますが、`image_url` が最も広い互換性を提供します。 |
| `duration` | integer | 動画の長さ（秒）（1-60、モデル依存） |
| `aspect_ratio` | string | `16:9`, `9:16`, `1:1` など |
| `resolution` | string | `1080p`, `720p`, `4k` |
| `fps` | integer | 1 秒あたりのフレーム数（1-120） |
| `negative_prompt` | string | 生成時に避けたい内容 |
| `seed` | integer | 再現性のためのランダム seed |
| `cfg_scale` | number | ガイダンススケール（0-20） |
| `motion_strength` | number | モーション強度（0-1） |
| `start_image` | string | 開始 keyframe の URL |
| `end_image` | string | 終了 keyframe の URL |

<Warning>
すべてのパラメータがすべてのモデルでサポートされているわけではありません。未対応のパラメータは黙って無視されます。サポートされているパラメータについては、各モデルのドキュメントを確認してください。
</Warning>

## モデル選択ガイド

<CardGroup cols={2}>
  <Card title="最高品質" icon="crown">
    **Seedance 2.0** または **Kling v2.6 Pro** — シネマティック品質、豊かなディテール、自然なモーション
  </Card>
  <Card title="最速生成" icon="bolt">
    **Higgsfield Turbo** または **Hailuo 2.3** — プロトタイピングと反復に素早い結果
  </Card>
  <Card title="最も汎用的" icon="wand-magic-sparkles">
    **Seedance 2.0** — 1 つのモデルで T2V、I2V、keyframe、extension、editing をサポート
  </Card>
  <Card title="最高のコストパフォーマンス" icon="coins">
    **Wan 2.6** または **Hailuo 2.3** — 生成あたりのコストを抑えつつ競争力のある品質
  </Card>
</CardGroup>

## 課金

動画生成では **生成ごとの固定価格** が使用されます。動画の長さにかかわらず、タスク送信時に 1 回課金されます。生成に失敗した場合、料金は自動的に返金されます。

現在の価格は [Models page](https://lemondata.cc/ja/models) または [Pricing API](/api-reference/pricing/get-pricing) で確認してください。
