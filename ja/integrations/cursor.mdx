---
title: "Cursor"
description: "LemonDataをCursor AIコードエディタで使用する"
---

## 概要

[Cursor](https://cursor.sh)はAI搭載のコードエディタです。LemonDataをカスタムAPIプロバイダーとして使用することで、300以上のモデルにアクセスできます。

## 設定

1. Cursorの設定を開きます (`Cmd/Ctrl + ,`)
2. 左サイドバーの **Models** に移動します
3. **OpenAI API Key** セクションまでスクロールします
4. LemonDataのAPIキーを入力します
5. **Override OpenAI Base URL** を有効にします
6. Base URLを以下に設定します:

```
https://api.lemondata.cc/v1
```

## モデルの選択

設定後、Cursorで任意のLemonDataモデルを選択できるようになります:

### コーディングに推奨されるモデル

| モデル | 最適な用途 |
|-------|----------|
| `claude-opus-4-5` | 複雑なコーディング、アーキテクチャ |
| `claude-sonnet-4-5` | 一般的なコーディング、デバッグ |
| `gpt-4o` | コード生成、解説 |
| `deepseek-r1` | 高度な推論タスク |
| `gpt-4o-mini` | 高速な補完、単純なタスク |

## 機能

### チャット

任意のLemonDataモデルを使用して、AIチャットパネル (`Cmd/Ctrl + L`) を利用できます:

- コードの解説
- バグ修正
- リファクタリングの提案
- ドキュメント生成

### Composer

Composer機能 (`Cmd/Ctrl + I`) は、LemonDataモデルを使用して以下のことが可能です:

- 複数ファイルの編集
- 機能の実装
- コードの移行

### タブ補完

タブ補完には、高速なモデルの使用を推奨します:

- `gpt-4o-mini`
- `claude-haiku-4-5`
- `gemini-2.5-flash`

## トラブルシューティング

<AccordionGroup>
  <Accordion title="モデルが表示されない">
    Cursorはモデルリストをキャッシュします。API設定後にCursorを再起動してみてください。
  </Accordion>

  <Accordion title="レスポンスが遅い">
    選択したモデルを確認してください。素早いタスクには `gpt-4o-mini` のような高速なモデルを使用してください。
  </Accordion>

  <Accordion title="認証エラー">
    APIキーが正しく、十分な残高があることを確認してください。
  </Accordion>
</AccordionGroup>

## ヒント

<CardGroup cols={2}>
  <Card title="適切なモデルの使用" icon="gauge-high">
    タブ補完には `gpt-4o-mini` を、複雑な推論にはより大規模なモデルを使用してください。
  </Card>
  <Card title="残高の確認" icon="wallet">
    LemonDataダッシュボードで使用状況を確認してください。
  </Card>
</CardGroup>