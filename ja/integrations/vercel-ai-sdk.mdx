---
title: "Vercel AI SDK"
description: "OpenAI互換またはResponses-nativeプロバイダーを使用してLemonDataをVercel AI SDKに統合します"
---

## 概要



<Note>
**種類**: フレームワーク / プラットフォーム

**主要パス**: OpenAI-compatible default

**サポートレベル**: 推奨統合パターン
</Note>

LemonData では、Vercel AI SDK における最も安定したデフォルトは **OpenAI互換プロバイダー** です。

特に **Responses-native** の動作が必要な場合は、OpenAIプロバイダーに切り替えつつ、同じ LemonData の base URL を維持できます。

このページは推奨される統合パターンとして読むべきであり、Vercel AI SDK のすべての helper に対して、このリポジトリで専用の end-to-end 回帰があるという意味ではありません。

## 推奨デフォルト: OpenAI互換プロバイダー

```bash
npm install ai @ai-sdk/openai-compatible
```

```typescript
import { createOpenAICompatible } from '@ai-sdk/openai-compatible';

export const lemondata = createOpenAICompatible({
  name: 'lemondata',
  apiKey: process.env.LEMONDATA_API_KEY,
  baseURL: 'https://api.lemondata.cc/v1',
});
```

## テキスト生成

```typescript
import { generateText } from 'ai';
import { lemondata } from './lemondata';

const { text } = await generateText({
  model: lemondata.chatModel('gpt-5.4'),
  prompt: 'Explain LemonData in one sentence.',
});

console.log(text);
```

## テキストストリーミング

```typescript
import { streamText } from 'ai';
import { lemondata } from './lemondata';

const result = await streamText({
  model: lemondata.chatModel('gpt-5.4'),
  prompt: 'Write a short poem about coding.',
});

for await (const textPart of result.textStream) {
  process.stdout.write(textPart);
}
```

## Tool Calling

```typescript
import { generateText, tool } from 'ai';
import { z } from 'zod';
import { lemondata } from './lemondata';

const result = await generateText({
  model: lemondata.chatModel('gpt-5.4'),
  prompt: 'What is the weather in San Francisco?',
  tools: {
    weather: tool({
      description: 'Get weather in a location',
      parameters: z.object({
        location: z.string(),
      }),
      execute: async ({ location }) => ({
        location,
        temperature: 72,
        condition: 'sunny',
      }),
    }),
  },
});

console.log(result.text);
```

## 構造化出力

```typescript
import { generateObject } from 'ai';
import { z } from 'zod';
import { lemondata } from './lemondata';

const { object } = await generateObject({
  model: lemondata.chatModel('gpt-5.4'),
  schema: z.object({
    name: z.string(),
    role: z.string(),
  }),
  prompt: 'Generate a fake developer profile.',
});

console.log(object);
```

## 明示的にResponses-Nativeの動作が必要な場合

```bash
npm install ai @ai-sdk/openai
```

```typescript
import { createOpenAI } from '@ai-sdk/openai';

export const lemondataResponses = createOpenAI({
  apiKey: process.env.LEMONDATA_API_KEY,
  baseURL: 'https://api.lemondata.cc/v1',
});
```

```typescript
import { generateText } from 'ai';
import { lemondataResponses } from './lemondata-responses';

const { text } = await generateText({
  model: lemondataResponses('gpt-5.4'),
  prompt: 'Explain LemonData in one sentence.',
});
```

<Note>
プロキシスタイルの統合では、安全なデフォルトとして `@ai-sdk/openai-compatible` を使用してください。`/v1/responses` 上に構築されたプロバイダーパスを明示的に使いたい場合にのみ、`@ai-sdk/openai` に切り替えてください。
</Note>

## 環境変数

```bash
# .env.local
LEMONDATA_API_KEY=sk-your-lemondata-key
```

## ベストプラクティス

<AccordionGroup>
  <Accordion title="まず openai-compatible を優先する">
    サードパーティのゲートウェイやプロキシバックエンドでは、`@ai-sdk/openai-compatible` が通常、最も予測しやすい出発点です。
  </Accordion>

  <Accordion title="必要な場合にのみ Responses に移行する">
    `/v1/responses` に紐づくプロバイダー動作が必要な場合は、1つの client で両方のパターンを混在させるのではなく、意図的にプロバイダーパッケージを切り替えてください。
  </Accordion>

  <Accordion title="シークレットはサーバー側で管理する">
    LemonData API key をクライアントサイドのコードに公開してはいけません。プロバイダーの設定はサーバーファイルまたは API routes に配置してください。
  </Accordion>
</AccordionGroup>