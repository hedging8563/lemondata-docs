---
title: "タスクステータスを取得"
description: "create エンドポイントで返される poll_url を使用して、任意の async タスクのステータスと結果を取得します"
---

## 概要

video、image、music、3D generation などのタスクタイプ全体で統一された async ポーリングを行うには、このエンドポイントを使用します。

<Info>
create レスポンスに `poll_url` が含まれている場合は、その完全に一致するパスを呼び出してください。新しい async ワークフローでは、`poll_url` は `/v1/tasks/{id}` を指す場合があります。
</Info>

## パスパラメータ

<ParamField path="id" type="string" required>
  create リクエストによって返されるタスク ID。
</ParamField>

## レスポンス

<ResponseField name="id" type="string">
  正規の非同期タスク ID です。
</ResponseField>

<ResponseField name="task_id" type="string">
  非同期タスク ID の別名です。
</ResponseField>

<ResponseField name="poll_url" type="string">
  作成レスポンスが返す優先ポーリング URL です。
</ResponseField>

<ResponseField name="status" type="string">
  `pending`、`processing`、`completed`、または `failed` などのタスクステータス。
</ResponseField>

<ResponseField name="progress" type="number">
  利用可能な場合の進捗率です。
</ResponseField>

<ResponseField name="video_url" type="string">
  タスクが完了して video を生成した場合の結果アセット URL。
</ResponseField>

<ResponseField name="video" type="object">
  利用可能な場合、`url`、`duration`、`width`、`height` を含む単一動画オブジェクトです。
</ResponseField>

<ResponseField name="videos" type="array">
  上流が複数出力を返す場合の動画配列です。
</ResponseField>

<ResponseField name="error" type="string">
  タスクが失敗した場合のエラーメッセージ。
</ResponseField>

<ResponseField name="created" type="integer">
  利用可能な場合の作成タイムスタンプです。
</ResponseField>

<ResponseField name="updated" type="integer">
  利用可能な場合の最終更新タイムスタンプです。
</ResponseField>

<ResponseField name="model" type="string">
  利用可能な場合の使用モデルです。
</ResponseField>

## 例

```bash cURL
curl "https://api.lemondata.cc/v1/tasks/video_abc123" \
  -H "Authorization: Bearer sk-your-api-key"
```

```python Python
import requests

poll_url = "/v1/tasks/video_abc123"
response = requests.get(
    f"https://api.lemondata.cc{poll_url}",
    headers={"Authorization": "Bearer sk-your-api-key"},
)
print(response.json())
```

```javascript JavaScript
const pollUrl = '/v1/tasks/video_abc123';
const response = await fetch(`https://api.lemondata.cc${pollUrl}`, {
  headers: { Authorization: 'Bearer sk-your-api-key' },
});
console.log(await response.json());
```
