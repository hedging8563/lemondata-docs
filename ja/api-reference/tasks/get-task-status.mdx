---
title: "タスクステータスの取得"
description: "作成エンドポイントから返された poll_url を使用して、任意の非同期タスクのステータスと結果を取得します"
---

## 概要

ビデオ、画像、音楽、3D生成などのタスクタイプにわたる、統合された非同期ポーリングにこのエンドポイントを使用します。

<Info>
作成レスポンスに `poll_url` が含まれている場合は、その正確なパスを呼び出してください。一部の画像モデルは、画像固有のステータスパスまたは `/v1/tasks/{id}` のいずれかでタスクベースのレスポンスを返す場合があります。
</Info>

## パスパラメータ

<ParamField path="id" type="string" required>
  作成リクエストによって返されたタスクID。
</ParamField>

## レスポンス

<ResponseField name="id" type="string">
  正規の非同期タスク識別子。
</ResponseField>

<ResponseField name="task_id" type="string">
  非同期タスク識別子のエイリアス。
</ResponseField>

<ResponseField name="poll_url" type="string">
  作成レスポンスで提供される、推奨されるポーリングURL。
</ResponseField>

<ResponseField name="status" type="string">
  `pending`、`processing`、`completed`、または `failed` などのタスクステータス。
</ResponseField>

<ResponseField name="progress" type="number">
  プロバイダーから報告された場合の進捗率（パーセンテージ）。
</ResponseField>

<ResponseField name="video_url" type="string">
  タスクが完了し、ビデオが生成された際の結果アセットURL。
</ResponseField>

<ResponseField name="video" type="object">
  利用可能な場合、`url`、`duration`、`width`、および `height` を含む単一のビデオペイロード。
</ResponseField>

<ResponseField name="videos" type="array">
  アップストリームが複数の出力を返す場合の、複数のビデオペイロード。
</ResponseField>

<ResponseField name="error" type="string">
  タスクが失敗した際のエラーメッセージ。
</ResponseField>

<ResponseField name="created" type="integer">
  利用可能な場合の作成タイムスタンプ。
</ResponseField>

<ResponseField name="updated" type="integer">
  利用可能な場合の最終更新タイムスタンプ。
</ResponseField>

<ResponseField name="model" type="string">
  プロバイダーから報告された場合の、タスクで使用されたモデル。
</ResponseField>

## 例

```bash cURL
curl "https://api.lemondata.cc/v1/tasks/video_abc123" \
  -H "Authorization: Bearer sk-your-api-key"
```

```python Python
import requests

poll_url = "/v1/tasks/video_abc123"
response = requests.get(
    f"https://api.lemondata.cc{poll_url}",
    headers={"Authorization": "Bearer sk-your-api-key"},
)
print(response.json())
```

```javascript JavaScript
const pollUrl = '/v1/tasks/video_abc123';
const response = await fetch(`https://api.lemondata.cc${pollUrl}`, {
  headers: { Authorization: 'Bearer sk-your-api-key' },
});
console.log(await response.json());
```