---
title: "AutoGen"
description: "Use modelos do TokenLab com o Microsoft AutoGen através de clientes compatíveis com OpenAI"
---

## Visão Geral

O AutoGen pode usar o TokenLab através do `OpenAIChatCompletionClient`, o mesmo caminho de cliente compatível com OpenAI que ele utiliza para outros endpoints personalizados de Chat Completions.

<Note>
**Tipo**: Framework de Agentes

**Caminho Principal**: Chat completions compatíveis com OpenAI

**Nível de Suporte**: Caminho de endpoint personalizado suportado
</Note>

## Instalação

```bash
pip install -U autogen-agentchat "autogen-ext[openai]" pyyaml
```

## Ambiente

```bash
export TOKENLAB_API_KEY="sk-your-tokenlab-key"
```

## Configuração de Modelo YAML

Use o endpoint `/v1` do TokenLab como `base_url`:

```yaml
provider: autogen_ext.models.openai.OpenAIChatCompletionClient
config:
  model: claude-sonnet-5
  base_url: https://api.tokenlab.sh/v1
  api_key: ${TOKENLAB_API_KEY}
  model_info:
    function_calling: true
    json_output: true
    vision: false
    family: claude
```

## Cliente Python

```python
import os

from autogen_agentchat.agents import AssistantAgent
from autogen_ext.models.openai import OpenAIChatCompletionClient

model_client = OpenAIChatCompletionClient(
    model="claude-sonnet-5",
    base_url="https://api.tokenlab.sh/v1",
    api_key=os.environ["TOKENLAB_API_KEY"],
    model_info={
        "function_calling": True,
        "json_output": True,
        "vision": False,
        "family": "claude",
    },
)

agent = AssistantAgent("assistant", model_client=model_client)
```

## Notas sobre o Endpoint

A extensão OpenAI do AutoGen espera o formato de requisição compatível com OpenAI. Para recursos nativos do provedor, como Anthropic Messages ou `generateContent` do Gemini, utilize os endpoints nativos do TokenLab a partir de um cliente que suporte esses protocolos diretamente.