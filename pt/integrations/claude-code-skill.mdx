---
title: "✨ Claude Code Skill"
description: "Guia de início rápido em 5 minutos - Deixe o Claude Code integrar automaticamente qualquer API do LemonData para você"
---

<Note>
Esta skill permite integrar qualquer uma das **centenas de APIs de IA** do LemonData apenas descrevendo o que você precisa em linguagem natural. A API é **Agent-First** — mesmo que o Claude erre o nome de um modelo, a resposta de erro indica exatamente como corrigir.
</Note>

## O que esta Skill pode fazer?

Quando você quiser usar recursos de IA em seu código (como GPT-4, geração de imagens, reconhecimento de voz, etc.), esta skill irá:

1. ✅ **Testar a API diretamente** — sem necessidade de pesquisar
2. ✅ **Se autocorrigir com os erros** — dicas estruturadas guiam a próxima tentativa
3. ✅ **Gerar código completo e executável** na linguagem de sua escolha
4. ✅ **Escolher o melhor modelo** com base em preços e disponibilidade em tempo real
5. ✅ **Otimizar sua integração** com sugestões de endpoints nativos

**Em resumo: Basta descrever suas necessidades e obter código de integração funcional — mesmo que a primeira tentativa não seja perfeita, a API guia o Claude até a resposta correta.**

## Passo 1: Instalar a Skill

<Tabs>
  <Tab title="npx (Recomendado)">
    A maneira mais fácil de instalar:

    ```bash
    npx add-skill hedging8563/lemondata-api-skill -y
    ```

    Isso instalará automaticamente a skill em todos os agentes de codificação detectados (Claude Code, Cursor, Copilot, etc.).
  </Tab>
  <Tab title="Compartilhar URL do GitHub">
    1. Abra o **Claude Code** no seu terminal ou IDE
    2. Cole este link no chat:
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
    3. O Claude Code o reconhecerá como um repositório de skill e ajudará você a instalá-lo
  </Tab>
  <Tab title="Git Clone">
    **Instalação pessoal** (disponível em todos os projetos):
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git ~/.claude/skills/lemondata-api-integration
    ```

    **Instalação específica do projeto** (compartilhada com a equipe via git):
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git .claude/skills/lemondata-api-integration
    ```
  </Tab>
</Tabs>

### Verificar a instalação

Pergunte ao Claude Code:
```
What skills are available?
```

Se você vir `lemondata-api-integration`, a instalação foi bem-sucedida!

## Passo 2: Obter sua API Key

Antes de usar a skill, você precisa de uma API Key do LemonData.

<Tip>
**Você não precisa configurar a API Key antecipadamente!** Quando você usar a skill, o Claude Code solicitará automaticamente sua API Key e a incluirá no código gerado.
</Tip>

<Steps>
  <Step title="Visite o LemonData">
    Acesse [lemondata.cc](https://lemondata.cc)
  </Step>
  <Step title="Entrar">
    Crie uma conta ou faça login
  </Step>
  <Step title="Obter API Key">
    Navegue até [Dashboard → API Keys](https://lemondata.cc/dashboard/api) e crie uma nova chave
  </Step>
  <Step title="Copiar a chave">
    Sua chave começa com `sk-...` - salve-a com segurança
  </Step>
</Steps>

<Warning>
**Importante:**
- Todas as APIs do LemonData usam a mesma API Key
- Não compartilhe sua chave com outras pessoas
- Não faça commit da sua chave no GitHub
</Warning>

## Passo 3: Começar a usar

Usar a skill é tão simples quanto conversar no chat!

### Exemplo 1: Usando o GPT-4

**Você diz:**
```
I want to use GPT-4 in my Python project
```

**O Claude Code irá:**
1. Tentar chamar a API diretamente com a melhor estimativa do modelo
2. Se o nome do modelo estiver errado, ler `did_you_mean` do erro e se autocorrigir
3. Gerar código Python completo com o modelo correto

**Você recebe:**
```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.lemondata.cc/v1"
)

response = client.chat.completions.create(
    model="gpt-4o",
    messages=[{"role": "user", "content": "Hello!"}]
)

print(response.choices[0].message.content)
```

### Exemplo 2: Gerar imagens

**Você diz:**
```
How do I generate images with Flux in Node.js?
```

**O Claude Code irá:**
- Chamar a API de geração de imagens
- Gerar código JavaScript com tratamento de erros adequado
- Incluir código para download e salvamento da imagem

### Exemplo 3: Reconhecimento de voz

**Você diz:**
```
Integrate a speech-to-text API
```

**O Claude Code irá:**
- Perguntar qual linguagem de programação você está usando
- Gerar código com o endpoint de áudio correto
- Fornecer exemplos de manipulação de arquivos de áudio

## Recursos Agent-First da API

A API do LemonData foi projetada para agentes de IA. Cada resposta de erro inclui dicas estruturadas:

### Recuperação inteligente de erros

| Erro | O que a API retorna | O que o Claude faz |
|------|--------------------|--------------------|
| Nome de modelo errado | `did_you_mean` + `suggestions` | Se autocorrige e tenta novamente |
| Saldo insuficiente | `balance_usd` + `suggestions` mais baratas | Muda para um modelo mais acessível |
| Modelo indisponível | `alternatives` + `retry_after` | Muda para um modelo disponível |
| Limite de taxa | `retry_after` (segundos exatos) | Aguarda e tenta novamente |
| Contexto muito longo | `suggestions` com modelos maiores | Muda para um modelo com contexto maior |

### Descoberta de modelos

Não é necessário pesquisar na documentação. Use estes endpoints da API:

```bash
# Ler a visão geral da API legível por máquinas
curl https://lemondata.cc/llms.txt

# Listar modelos por categoria
curl "https://api.lemondata.cc/v1/models?category=chat" -H "Authorization: Bearer sk-KEY"
curl "https://api.lemondata.cc/v1/models?category=image" -H "Authorization: Bearer sk-KEY"

# Filtrar por capacidade
curl "https://api.lemondata.cc/v1/models?tag=coding" -H "Authorization: Bearer sk-KEY"
```

### Otimização de endpoint nativo

Quando o Claude usa `/v1/chat/completions` com um modelo Claude ou Gemini, a API retorna headers de otimização:

```
X-LemonData-Hint: This model supports native Anthropic format. Use POST /v1/messages for better performance.
X-LemonData-Native-Endpoint: /v1/messages
```

O Claude Code mudará automaticamente para o endpoint nativo para melhor desempenho.

## Recursos suportados

Esta skill pode ajudar você a integrar todas estas capacidades de IA:

| Tipo de recurso | Exemplos |
|-------------|----------|
| 💬 Chat | GPT-4o, Claude, Gemini, DeepSeek |
| 🎨 Geração de imagem | Midjourney, Flux, Stable Diffusion |
| 🎬 Geração de vídeo | Sora, Runway, Kling, Luma AI |
| 🎵 Geração de música | Suno |
| 🗿 Modelos 3D | Tripo3D |
| 🎤 Áudio | Text-to-Speech, Speech-to-Text |
| 📊 Embeddings | text-embedding-3 |
| 🔄 Rerank | bce-reranker, qwen3-rerank |

## Dicas de uso

### 1. Apenas declare suas necessidades

<Tabs>
  <Tab title="❌ Não faça isso">
    ```
    Help me check if LemonData has an image generation API
    ```
  </Tab>
  <Tab title="✅ Faça isto em vez disso">
    ```
    I want to generate images in Python
    ```
  </Tab>
</Tabs>

### 2. Especifique sua linguagem

Se você tiver um requisito de linguagem específico, basta dizer:

```
Use JavaScript to call GPT-4
Use Python for speech recognition
Use Go to generate images
```

### 3. Descreva seu cenário

Se você tiver necessidades específicas, descreva-as em detalhes:

```
I'm building a chatbot, need to use GPT-4
My website needs to let users upload and transform images
I'm making a voice notes app, need speech-to-text
```

## Considerações importantes

### Segurança da API Key

<CardGroup cols={2}>
  <Card title="❌ Nunca faça isso" icon="xmark" color="#ef4444">
    - Colocar a API Key diretamente no código do frontend
    - Fazer commit da API Key no GitHub
    - Compartilhar a API Key com outras pessoas
  </Card>
  <Card title="✅ Melhores práticas" icon="check" color="#22c55e">
    - Usar variáveis de ambiente
    - Chamar APIs do backend para sites
    - Usar frameworks como Next.js, Express
  </Card>
</CardGroup>

### Gerenciamento de custos

- Cada chamada de API consome créditos
- Defina limites de uso no seu [dashboard](https://lemondata.cc/dashboard)
- Use pequenos volumes de dados para testes
- Consulte `error.balance_usd` e `error.estimated_cost_usd` para monitorar custos programaticamente

### Tratamento de erros

O código gerado inclui tratamento de erros Agent-First que:
- Lê as dicas estruturadas de erro (`did_you_mean`, `suggestions`, `retry_after`)
- Se recupera automaticamente de erros comuns
- Recorre a modelos alternativos quando necessário

## FAQ

<AccordionGroup>
  <Accordion title="A skill não está sendo ativada automaticamente?">
    Tente mencionar "LemonData" ou "LemonData API" em sua solicitação, ou descreva sua necessidade de forma mais específica:
    ```
    Use LemonData to integrate GPT-4 in my project
    ```
  </Accordion>

  <Accordion title="Como a API ajuda o Claude a encontrar o modelo correto?">
    A API Agent-First do LemonData retorna dicas estruturadas nos erros. Se o Claude errar o nome de um modelo, a resposta de erro inclui `did_you_mean` com o nome correto e `suggestions` com modelos alternativos. O Claude lê essas dicas e se autocorrige na próxima tentativa.
  </Accordion>

  <Accordion title="O código gerado apresenta erros?">
    Verifique estes pontos:
    1. A API Key está correta?
    2. As bibliotecas necessárias estão instaladas? (ex: `openai` para Python)
    3. Você consegue acessar os servidores do LemonData?
    4. Sua API Key tem créditos suficientes?
  </Accordion>

  <Accordion title="Como atualizar a skill?">
    Envie o link do GitHub para o Claude Code novamente:
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
  </Accordion>
</AccordionGroup>

## Recursos

<CardGroup cols={2}>
  <Card title="Site do LemonData" icon="globe" href="https://lemondata.cc">
    Site principal e dashboard
  </Card>
  <Card title="Documentação da API" icon="book" href="https://docs.lemondata.cc">
    Referência completa da API
  </Card>
  <Card title="Modelos" icon="robot" href="https://lemondata.cc/en/models">
    Navegue por todos os modelos disponíveis
  </Card>
  <Card title="llms.txt" icon="file-lines" href="https://lemondata.cc/llms.txt">
    Visão geral da API legível por máquinas para agentes de IA
  </Card>
</CardGroup>

## Comece agora!

Agora você sabe como usar esta skill. Tente perguntar ao Claude Code:

```
I want to use GPT-4 in my project
```

ou

```
Help me integrate image generation
```

O Claude Code usará automaticamente esta skill para completar todo o trabalho para você — e se algo der errado, as dicas estruturadas da API o guiarão até a resposta correta!

<Info>
**Tem dúvidas?** Verifique nossos [GitHub Issues](https://github.com/hedging8563/lemondata-api-skill/issues) ou entre em contato com [support@lemondata.cc](mailto:support@lemondata.cc)
</Info>
