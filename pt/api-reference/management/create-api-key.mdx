---
title: "Criar chave API"
openapi: "POST /v1/management/api-keys"
description: "Cria uma nova chave API de usuário para a organização atual."
---
## Visão geral

Este endpoint cria uma nova API Key do tipo `type=user` na organização associada ao management token atual.

## Corpo da requisição

| Campo | Tipo | Padrões / limites | Observações |
|-------|------|------------------|-------|
| name | string | obrigatório, padrão Default Key, tamanho 1-50 | Nome de exibição, normalizado no servidor |
| limitAmount | number \| null | mín. 0, máx. 100000 | null ou omitido = ilimitado, 0 = sem quota utilizável, valor positivo = teto em USD |
| models | string[] | padrão [] | Allowlist opcional de modelos lógicos |
| expiresAt | string \| null | datetime RFC3339 | null significa sem expiração |

## Observações

- O valor completo da API Key é mostrado apenas uma vez na resposta.
- `models: []` significa que a chave não possui restrição adicional de modelos além da política da organização.
- `limitAmount: 0` cria uma chave válida, mas sem quota utilizável até ser atualizada depois.

## Exemplo

<RequestExample>
```bash cURL
curl -X POST "https://api.lemondata.cc/v1/management/api-keys" \
  -H "Authorization: Bearer mt-your-management-token" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Backend Worker",
    "limitAmount": 500,
    "models": ["veo3.1", "sora-2"],
    "expiresAt": "2026-12-31T23:59:59Z"
  }'
```
</RequestExample>

## Requisição / Resposta

Use o painel interativo de OpenAPI acima para ver o schema exato e os campos de resposta.
