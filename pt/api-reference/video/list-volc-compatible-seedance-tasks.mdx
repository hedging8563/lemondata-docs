---
title: "Listar tarefas (compatível com Volc)"
openapi: "GET /api/v3/contents/generations/tasks"
description: "Liste tarefas compatíveis com Seedance recentes da organização atual."
---

## Visão geral

Estes endpoints são para equipes que já usam o formato de tarefa Seedance no estilo Volc. A autenticação usa sua API key Bearer da TokenLab. O ID retornado começa com `cgt-`; consulte o status até que `status` seja `succeeded`, `failed`, `cancelled` ou `expired`.

Veja também [Modelos de vídeo Seedance 2.0](/pt/guides/seedance-2-video) e [Geração de vídeo](/pt/guides/video-generation).

## Formato da resposta

A resposta de lista retorna `items` e `total`. Os filtros suportados são `filter.status`, `filter.task_ids`, `filter.model` e `filter.service_tier`, com `page_num` e `page_size` para paginação.

## Exemplo

```bash
curl "https://api.tokenlab.sh/api/v3/contents/generations/tasks?page_num=1&page_size=20&filter.status=succeeded" \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```
