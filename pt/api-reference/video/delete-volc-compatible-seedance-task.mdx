---
title: "Cancelar tarefa (compatível com Volc)"
openapi: "DELETE /api/v3/contents/generations/tasks/{id}"
description: "Cancele uma tarefa compatível com Seedance em fila ou ainda cancelável."
---

## Visão geral

Estes endpoints são para equipes que já usam o formato de tarefa Seedance no estilo Volc. A autenticação usa sua API key Bearer da TokenLab. O ID retornado começa com `cgt-`; consulte o status até que `status` seja `succeeded`, `failed`, `cancelled` ou `expired`.

Veja também [Modelos de vídeo Seedance 2.0](/pt/guides/seedance-2-video) e [Geração de vídeo](/pt/guides/video-generation).

## Formato da resposta

Uma solicitação de cancelamento bem-sucedida retorna HTTP 200 com corpo vazio. Consulte a tarefa depois se precisar confirmar o status final.

## Exemplo

```bash
curl -X DELETE https://api.tokenlab.sh/api/v3/contents/generations/tasks/cgt-0123456789abcdef0123456789abcdef \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```
