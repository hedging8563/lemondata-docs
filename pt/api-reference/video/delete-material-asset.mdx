---
title: "Excluir Ativo de Material"
openapi: "DELETE /v1/videos/assets/{assetId}"
description: "Exclui um ativo de material Seedance pertencente à organização atual."
---

Exclui um ativo de material. Isso não exclui o grupo de material que o contém. Após a exclusão, o ID do ativo não poderá mais ser usado como `material_asset_id`.

## Exemplo de Requisição

```bash
curl https://api.tokenlab.sh/v1/videos/assets/seedance_asset_123 \
  -X DELETE \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```

## Exemplo de Resposta

```json
{
  "deleted": true,
  "id": "seedance_asset_123"
}
```