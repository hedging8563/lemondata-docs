---
title: "Video-Generierung"
description: "Generieren Sie Videos mit Modellen von 11 Anbietern — Text-zu-Video, Bild-zu-Video, Keyframe-Steuerung, Video-Erweiterung und Bearbeitung"
---

## Überblick

LemonData bietet Zugriff auf Video-Generierungsmodelle von 11 Anbietern über eine einzige einheitliche API. Die Video-Generierung ist **asynchron** — Sie senden eine Anfrage, erhalten eine Task-ID und `poll_url` und pollen dann auf das Ergebnis.

<Info>
Die Modellliste wird häufig aktualisiert. Die neuesten verfügbaren Modelle und Preise finden Sie auf der [Modellseite](https://lemondata.cc/de/models) oder über die [Models API](/api-reference/models/list-models).
</Info>

<Warning>
Audio behavior is model-dependent. Veo 3 family defaults to audio-on. Some models are silent by default or silent-only. For passthrough-governed models, pass `output_audio=true` to make a best-effort upstream audio request.
</Warning>


### Asynchroner Workflow

```mermaid
sequenceDiagram
    participant Client
    participant LemonData
    participant Provider

    Client->>LemonData: POST /v1/videos/generations
    LemonData->>Provider: Forward request
    Provider-->>LemonData: task_id + pending
    LemonData-->>Client: { task_id, poll_url, status: "pending" }

    loop Poll every 5-10s
        Client->>LemonData: GET poll_url
        LemonData->>Provider: Query status
        Provider-->>LemonData: Status update
        LemonData-->>Client: { status, video_url? }
    end
```

<CodeGroup>

```python Python
import requests
import time

API_KEY = "sk-your-api-key"
BASE = "https://api.lemondata.cc/v1"
headers = {"Authorization": f"Bearer {API_KEY}"}

# Step 1: Submit generation request
resp = requests.post(f"{BASE}/videos/generations",
    headers=headers,
    json={
        "model": "sora-2",
        "prompt": "A calm cinematic shot of a cat walking through a sunlit garden.",
        "operation": "text-to-video",
        "duration": 4,
        "aspect_ratio": "16:9"
    }
)
create_data = resp.json()
task_id = create_data["task_id"]
poll_url = create_data["poll_url"]

# Step 2: Poll for result
while True:
    status = requests.get(f"https://api.lemondata.cc{poll_url}", headers=headers).json()
    if status["status"] in ("completed", "succeeded"):
        print(f"Video URL: {status['video_url']}")
        break
    elif status["status"] == "failed":
        print(f"Failed: {status.get('error')}")
        break
    time.sleep(10)
```

```javascript JavaScript
const API_KEY = 'sk-your-api-key';
const BASE = 'https://api.lemondata.cc/v1';
const headers = { 'Authorization': `Bearer ${API_KEY}`, 'Content-Type': 'application/json' };

// Step 1: Submit
const { task_id, poll_url } = await fetch(`${BASE}/videos/generations`, {
  method: 'POST', headers,
  body: JSON.stringify({
    model: 'sora-2',
    prompt: 'A calm cinematic shot of a cat walking through a sunlit garden.',
    operation: 'text-to-video',
    duration: 4,
    aspect_ratio: '16:9'
  })
}).then(r => r.json());

// Step 2: Poll
const poll = setInterval(async () => {
  const status = await fetch(`https://api.lemondata.cc${poll_url}`, { headers }).then(r => r.json());
  if (['completed', 'succeeded'].includes(status.status)) {
    console.log('Video URL:', status.video_url);
    clearInterval(poll);
  } else if (status.status === 'failed') {
    console.error('Failed:', status.error);
    clearInterval(poll);
  }
}, 10000);
```

</CodeGroup>

## Matrix der Modellfähigkeiten

Verschiedene Modelle eignen sich besonders gut für unterschiedliche Aufgaben. Verwenden Sie diese Matrix, um das richtige Modell für Ihren Anwendungsfall auszuwählen.

**Legende**: ✅ Unterstützt | ❌ Nicht unterstützt

| Series | Provider | T2V | I2V | Keyframe | Extension | Editing | Max Duration |
|--------|----------|-----|-----|----------|-----------|---------|-------------|
| **Sora** | OpenAI | ✅ | ❌ | ❌ | ❌ | ❌ | ~20s |
| **Kling** | Kuaishou | ✅ | ✅ | ❌ | ✅ | ❌ | 10s |
| **Veo** | Google | ✅ | ✅ | ✅ | ❌ | ❌ | 8s |
| **Seedance** | ByteDance | ✅ | ✅ | ✅ | ✅ | ✅ | 10s |
| **Hailuo** | MiniMax | ✅ | ✅ | ❌ | ❌ | ❌ | 10s |
| **Wan** | Alibaba | ✅ | ✅ | ❌ | ❌ | ❌ | 5s |
| **Runway** | Runway | ❌ | ✅ | ❌ | ❌ | ❌ | 10s |
| **Luma** | Luma | ✅ | ❌ | ❌ | ✅ | ❌ | 5s |
| **Vidu** | Vidu | ✅ | ✅ | ✅ | ❌ | ❌ | 8s |
| **Grok** | xAI | ✅ | ❌ | ❌ | ❌ | ❌ | ~15s |
| **Higgsfield** | Higgsfield | ✅ | ❌ | ❌ | ❌ | ❌ | ~5s |

### Definitionen der Fähigkeiten

- **T2V (Text-to-Video)**: Video aus einem Text-Prompt generieren
- **I2V (Image-to-Video)**: Ein statisches Bild zu einem Video animieren. Für die breiteste Kompatibilität geben Sie eine öffentliche `image_url` an.
- **Keyframe**: Start- und Endframes mit `start_image` + `end_image` steuern
- **Extension**: Die Dauer eines bestehenden Videos verlängern
- **Editing**: Bestimmte Aspekte eines bestehenden Videos ändern

## Verfügbare Modelle nach Serie

### Sora (OpenAI)

| Model | Quality | Notes |
|-------|---------|-------|
| `sora-2` | Standard | Standard-Beispielmodell |
| `sora-2-pro` | High | Höhere Qualität, längere Generierungszeit |
| `sora-2-characters` | Standard | Charakterfokussierte Generierung |

### Kling (Kuaishou)

| Model | Capability | Notes |
|-------|-----------|-------|
| `kling-v2.6-pro` | T2V | Neueste Generation, professionelle Qualität |
| `kling-v2.6-std` | T2V | Neueste Generation, schnell |
| `kling-v2.5-turbo-std` | T2V | Turbo-Stufe |
| `kling-v2.5-turbo-pro` | T2V | Turbo-Geschwindigkeit, Pro-Qualität |
| `kling-v2.1-master` | T2V/I2V | Master-Qualität |
| `kling-v2.1-pro` | T2V/I2V | Professionelle Qualität |
| `kling-v2.1-standard` | T2V/I2V | Standardqualität |
| `kling-video` | T2V/I2V | Basismodell |
| `kling-video-extend` | Extension | Bestehende Videos verlängern |
| `kling-video-o1-pro` | T2V | O1-Reasoning, Pro-Qualität |
| `kling-video-o1-std` | T2V | O1-Reasoning, Standard |
| `kling-effects` | Effects | Visuelle Effekte anwenden |
| `kling-omni-video` | T2V | Omni-Modell |
| `kling-motion-control` | T2V | Bewegungsgesteuerte Generierung |

### Veo (Google)

| Model | Quality | Notes |
|-------|---------|-------|
| `veo2` | Standard | Modell der Veo-2-Serie |
| `veo3.1` | Standard | Googles neuestes Videomodell |
| `veo3.1-pro` | High | Professionelle Qualität |
| `veo3.1-4k` | Ultra | Ausgabe in 4K-Auflösung |
| `veo3.1-fast` | Fast | Schnellere Generierung |
| `veo3.1-fast-4k` | Fast + 4K | Schnelle Generierung mit 4K-Ausgabe |
| `veo3.1-components` | Standard | Komponentenbasierte Generierung |
| `veo3` | Standard | Vorherige Generation |
| `veo3-pro` | High | Vorherige Generation, professionell |
| `veo3-fast` | Fast | Vorherige Generation, schnell |

### Seedance (ByteDance)

| Model | Capability | Notes |
|-------|-----------|-------|
| `seedance-2-0` | T2V/I2V/Keyframe/Extension/Editing | Neueste, leistungsfähigste Version |
| `seedance-1-5-pro` | T2V/I2V | Vorherige Generation, Pro-Qualität |
| `seedance-1-0-pro` | T2V/I2V | Erste Generation, Pro |
| `seedance-1-0-pro-fast` | T2V/I2V | Erste Generation, schnell |
| `seedance-1-0-lite-t2v` | T2V | Leichtgewichtiges Text-zu-Video |
| `seedance-1-0-lite-i2v` | I2V | Leichtgewichtiges Bild-zu-Video |

<Note>
Seedance 2.0 unterstützt das breiteste Spektrum an Fähigkeiten, einschließlich multimodal-to-video, Video-Erweiterung und Videobearbeitung — alles über denselben API-Endpunkt.
</Note>

### Hailuo (MiniMax)

| Model | Quality | Notes |
|-------|---------|-------|
| `hailuo-2.3` | Standard | Gute Qualität |
| `hailuo-2.3-pro` | High | Ausgabe mit höherer Qualität |
| `hailuo-2.3-fast` | Fast | Schnellere Generierung |
| `hailuo-2.3-standard` | Standard | Standard-Stufe |
| `video-01` | Standard | MiniMax video-01 |
| `video-01-live` | Standard | Live-artige Generierung |

### Wan (Alibaba)

| Model | Capability | Notes |
|-------|-----------|-------|
| `wan-2.6` | T2V | Neuestes Text-zu-Video |
| `wan2.6-i2v` | I2V | Neuestes Bild-zu-Video |
| `wan-2.5` | T2V | Vorherige Generation |
| `wan2.5-i2v-preview` | I2V | I2V der vorherigen Generation |
| `wan-2.2-plus` | T2V | Frühere Generation |
| `vace-14b` | T2V | VACE-Architektur |

### Runway

| Model | Duration | Notes |
|-------|----------|-------|
| `runwayml-gen4-turbo-5` | 5s | Schnelle Generierung |
| `runwayml-gen4-turbo-10` | 10s | Längere Clips |

### Luma

| Model | Capability | Notes |
|-------|-----------|-------|
| `luma-video-api` | T2V | Text-zu-Video |
| `luma-video-extend-api` | Extension | Bestehende Videos verlängern |

### Vidu (Shengshu)

| Model | Quality | Notes |
|-------|---------|-------|
| `viduq3-turbo` | Fast | Turbo-Stufe |
| `viduq3-pro` | High | Neueste Generation |
| `viduq2-pro` | High | Vorherige Generation, Pro |
| `viduq2-pro-fast` | Fast | Vorherige Generation, schnelles Pro |
| `viduq2` | Standard | Vorherige Generation, Standard |
| `viduq2-turbo` | Fast | Turbo-Geschwindigkeit |
| `vidu2.0` | Standard | Basismodell |

### Grok (xAI)

| Model | Notes |
|-------|-------|
| `grok-video-3` | xAIs Video-Generierungsmodell |
| `grok-video-3-10s` | 10-Sekunden-Variante |

### Higgsfield

| Model | Notes |
|-------|-------|
| `higgsfield-turbo` | Am schnellsten, geringere Kosten |
| `higgsfield-standard` | Standardqualität |
| `higgsfield-lite` | Leichtgewichtig |

## Anwendungsbeispiele

### Text-to-Video (T2V)

Der häufigste Anwendungsfall. Alle Modelle unterstützen dies.

```python
response = requests.post(f"{BASE}/videos/generations",
    headers=headers,
    json={
        "model": "sora-2",
        "prompt": "A calm cinematic shot of a cat walking through a sunlit garden.",
        "operation": "text-to-video",
        "duration": 4,
        "aspect_ratio": "16:9"
    }
)
```

### Image-to-Video (I2V)

Animieren Sie ein statisches Bild. Für die breiteste Kompatibilität über Videomodelle hinweg hosten Sie das Quellbild und übergeben `image_url`.

```python
# Using image URL
response = requests.post(f"{BASE}/videos/generations",
    headers=headers,
    json={
        "model": "hailuo-2.3-fast",
        "prompt": "The scene begins from the provided image and adds gentle natural motion.",
        "operation": "image-to-video",
        "image_url": "https://example.com/portrait.jpg",
        "duration": 6,
        "aspect_ratio": "16:9"
    }
)

```

### Keyframe-Steuerung (Start + Endbild)

Steuern Sie sowohl den ersten als auch den letzten Frame für präzise Übergänge. Derzeit unterstützt von Seedance 2.0.

```python
response = requests.post(f"{BASE}/videos/generations",
    headers=headers,
    json={
        "model": "viduq2-pro",
        "prompt": "Smooth transition from day to night, city lights gradually turning on",
        "operation": "start-end-to-video",
        "start_image": "https://example.com/city-day.jpg",
        "end_image": "https://example.com/city-night.jpg",
        "duration": 5,
        "resolution": "720p",
        "aspect_ratio": "16:9"
    }
)
```

### Video-Erweiterung

Verlängern Sie die Dauer eines bestehenden Videos. Verwenden Sie Modelle mit Erweiterungsfähigkeit.

```python
response = requests.post(f"{BASE}/videos/generations",
    headers=headers,
    json={
        "model": "kling-video-extend",
        "prompt": "Continue the scene naturally",
        "image_url": "https://example.com/last-frame.jpg"
    }
)
```

## Parameterreferenz

| Parameter | Type | Description |
|-----------|------|-------------|
| `model` | string | Modell-ID (Standard: `sora-2`) |
| `prompt` | string | **Erforderlich.** Textbeschreibung des Videos |
| `image_url` | string | URL des Startbildes (für I2V) |
| `image` | string | Inline-Bild als data URL. Von kompatiblen Modellen unterstützt, aber `image_url` bietet die breiteste Kompatibilität. |
| `duration` | integer | Videodauer in Sekunden (1–60, modellabhängig) |
| `aspect_ratio` | string | `16:9`, `9:16`, `1:1` usw. |
| `resolution` | string | `1080p`, `720p`, `4k` |
| `fps` | integer | Frames pro Sekunde (1–120) |
| `negative_prompt` | string | Was bei der Generierung vermieden werden soll |
| `seed` | integer | Zufalls-Seed für Reproduzierbarkeit |
| `cfg_scale` | number | Guidance-Skala (0–20) |
| `motion_strength` | number | Bewegungsintensität (0–1) |
| `start_image` | string | URL des Start-Keyframes |
| `end_image` | string | URL des End-Keyframes |

<Warning>
Nicht alle Parameter werden von jedem Modell unterstützt. Nicht unterstützte Parameter werden stillschweigend ignoriert. Prüfen Sie die Dokumentation des Modells auf unterstützte Parameter.
</Warning>

## Leitfaden zur Modellauswahl

<CardGroup cols={2}>
  <Card title="Beste Qualität" icon="crown">
    **Seedance 2.0** oder **Kling v2.6 Pro** — cineastische Qualität, viele Details, natürliche Bewegung
  </Card>
  <Card title="Schnellste Generierung" icon="bolt">
    **Higgsfield Turbo** oder **Hailuo 2.3** — schnelle Ergebnisse für Prototyping und Iteration
  </Card>
  <Card title="Am vielseitigsten" icon="wand-magic-sparkles">
    **Seedance 2.0** — unterstützt T2V, I2V, Keyframe, Erweiterung und Bearbeitung in einem Modell
  </Card>
  <Card title="Bestes Preis-Leistungs-Verhältnis" icon="coins">
    **Wan 2.6** oder **Hailuo 2.3** — wettbewerbsfähige Qualität bei geringeren Kosten pro Generierung
  </Card>
</CardGroup>

## Abrechnung

Die Video-Generierung verwendet **feste Preise pro Generierung**. Ihnen wird einmal bei Einreichung der Task berechnet, unabhängig von der Videodauer. Falls die Generierung fehlschlägt, wird die Gebühr automatisch erstattet.

Die aktuellen Preise finden Sie auf der [Modellseite](https://lemondata.cc/de/models) oder über die [Pricing API](/api-reference/pricing/get-pricing).