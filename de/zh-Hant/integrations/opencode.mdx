---
title: "OpenCode"
description: "Konfigurieren Sie OpenCode für die Verwendung der LemonData API"
---

## Übersicht

OpenCode ist ein Open-Source-KI-Codierungsassistent, der im Terminal ausgeführt wird. Es unterstützt mehrere LLM-Anbieter und kann so konfiguriert werden, dass es die API von LemonData verwendet, um auf über 300 Modelle zuzugreifen.

## Installation

<Tabs>
  <Tab title="Installationsskript">
    ```bash
    curl -fsSL https://opencode.ai/install | bash
    ```
  </Tab>
  <Tab title="Homebrew">
    ```bash
    brew install opencode-ai/tap/opencode
    ```

    Oder verwenden Sie den Community-Tap (wird häufiger aktualisiert):
    ```bash
    brew install anomalyco/tap/opencode
    ```
  </Tab>
  <Tab title="Go">
    ```bash
    go install github.com/opencode-ai/opencode@latest
    ```
  </Tab>
</Tabs>

Installation überprüfen:

```bash
opencode --version
```

## Konfiguration

### Schritt 1: Umgebungsvariablen setzen

```bash
export OPENAI_API_KEY="sk-your-lemondata-key"
export LOCAL_ENDPOINT="https://api.lemondata.cc/v1"
```

Für eine dauerhafte Konfiguration fügen Sie diese zu Ihrer `~/.bashrc` oder `~/.zshrc` hinzu:

```bash
echo 'export OPENAI_API_KEY="sk-your-lemondata-key"' >> ~/.zshrc
echo 'export LOCAL_ENDPOINT="https://api.lemondata.cc/v1"' >> ~/.zshrc
source ~/.zshrc
```

### Schritt 2: OpenCode konfigurieren

Erstellen oder bearbeiten Sie `~/.config/opencode/opencode.json` (global) oder `opencode.json` im Stammverzeichnis Ihres Projekts:

```json
{
  "provider": {
    "openai": {
      "options": {
        "apiKey": "sk-your-lemondata-key",
        "baseURL": "https://api.lemondata.cc/v1"
      }
    }
  },
  "model": "gpt-4o",
  "agent": {
    "coder": {
      "model": "gpt-4o"
    }
  }
}
```

## Grundlegende Verwendung

**Interaktiven Modus starten:**

```bash
opencode
```

**Mit einem Prompt ausführen:**

```bash
opencode "Explain this codebase"
```

**Spezifisches Modell angeben:**

```bash
opencode --model gpt-4o "Fix the bugs in main.py"
```

## Verfügbare Modelle

| Modell | Bestens geeignet für |
|-------|----------|
| `gpt-4o` | Komplexe Aufgaben, Code-Architektur |
| `gpt-4o-mini` | Schnelle Fixes, einfache Abfragen |
| `claude-sonnet-4-5` | Code-Reviews, Dokumentationserstellung |
| `claude-opus-4-5` | Komplexes Reasoning |
| `gemini-2.5-flash` | Schnelle Antwortzeiten |
| `deepseek-r1` | Algorithmus-Design |

## Häufige Befehle

**Code analysieren:**

```bash
opencode "What does this function do?" < src/utils.ts
```

**Code generieren:**

```bash
opencode "Create a REST API with Express"
```

**Änderungen überprüfen:**

```bash
git diff | opencode "Review these changes"
```

**Fehler beheben:**

```bash
opencode "Fix the TypeScript errors in this project"
```

## Interaktive Befehle

| Befehl | Beschreibung |
|---------|-------------|
| `/help` | Verfügbare Befehle anzeigen |
| `/model <name>` | Zu einem anderen Modell wechseln |
| `/clear` | Chat-Verlauf löschen |
| `/exit` | OpenCode beenden |

## Fehlerbehebung

<AccordionGroup>
  <Accordion title="Verbindungsfehler">
    - Überprüfen Sie, ob `LOCAL_ENDPOINT` auf `https://api.lemondata.cc/v1` gesetzt ist
    - Netzwerkverbindung prüfen
    - Testen Sie die Verbindung mit `curl https://api.lemondata.cc/v1/models`
  </Accordion>

  <Accordion title="Authentifizierung fehlgeschlagen">
    - Überprüfen Sie, ob die Umgebungsvariable `OPENAI_API_KEY` gesetzt ist
    - Stellen Sie sicher, dass der Key mit `sk-` beginnt
    - Stellen Sie sicher, dass der Key im LemonData Dashboard aktiv ist
  </Accordion>

  <Accordion title="Modell nicht gefunden">
    - Überprüfen Sie, ob der Modellname in der Konfiguration exakt übereinstimmt
    - Überprüfen Sie die Modellverfügbarkeit unter [lemondata.cc/en/models](https://lemondata.cc/zh-TW/models)
  </Accordion>
</AccordionGroup>

## Best Practices

<AccordionGroup>
  <Accordion title="Projektkontext verwenden">
    Führen Sie OpenCode im Stammverzeichnis Ihres Projekts aus, um ein besseres Verständnis Ihrer Codebasis zu ermöglichen.
  </Accordion>

  <Accordion title="Das richtige Modell wählen">
    Verwenden Sie schnellere Modelle (gpt-4o-mini) für einfache Aufgaben und leistungsstarke Modelle (gpt-4o, claude) für komplexe Aufgaben.
  </Accordion>

  <Accordion title="Generierten Code überprüfen">
    Überprüfen Sie immer den von der KI generierten Code, bevor Sie Änderungen an Ihrem Projekt vornehmen.
  </Accordion>
</AccordionGroup>