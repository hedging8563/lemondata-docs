---
title: "Video erstellen"
openapi: "POST /v1/videos/generations"
description: "Erstellt eine Video-Generierungsaufgabe"
---

## Übersicht

Die Video-Generierung ist asynchron. Sie senden eine Anfrage, erhalten eine Aufgaben-ID und `poll_url` und fragen dann das Ergebnis per Polling ab.

<Info>
Für das zuverlässigste Polling-Verhalten folgen Sie genau der `poll_url`, die von der Erstellungsanfrage zurückgegeben wird.
</Info>

<Warning>
Audio output is model-dependent. Veo 3 family defaults to audio-on. Use `output_audio` to explicitly request audio when supported. The camelCase alias `outputAudio` is also accepted for compatibility.
</Warning>


## Request-Body

<ParamField body="model" type="string" default="sora-2">
  Video-Modell-ID. Der API-Standardwert ist `sora-2`. Siehe den [Leitfaden zur Video-Generierung](/guides/video-generation) für die aktuelle Modellmatrix und unterstützte Funktionen.
</ParamField>

<ParamField body="prompt" type="string" required>
  Textbeschreibung des zu generierenden Videos.
</ParamField>

<ParamField body="image_url" type="string">
  Öffentlich zugängliche URL des Startbilds für die Bild-zu-Video-Generierung (I2V). Für die beste modellübergreifende Kompatibilität sollte `image_url` bevorzugt werden.
</ParamField>

<ParamField body="image" type="string">
  Inline-Bild als Data-URL (zum Beispiel `data:image/jpeg;base64,...`). Wird von kompatiblen Modellen unterstützt, aber `image_url` bietet die breiteste Kompatibilität über Video-Modelle hinweg.
</ParamField>

<ParamField body="duration" type="integer">
  Videodauer in Sekunden (modellabhängig).
</ParamField>

<ParamField body="aspect_ratio" type="string">
  Seitenverhältnis (z. B. `16:9`, `9:16`, `1:1`).
</ParamField>

<ParamField body="resolution" type="string">
  Videoauflösung (z. B. `1080p`, `720p`).
</ParamField>

<ParamField body="output_audio" type="boolean">
  Model-dependent audio output toggle. Veo 3 family defaults to `true` when omitted. Other video models follow their governed default behavior. The camelCase alias `outputAudio` is accepted for compatibility.
</ParamField>

<ParamField body="fps" type="integer">
  Bilder pro Sekunde (1–120).
</ParamField>

<ParamField body="negative_prompt" type="string">
  Was bei der Video-Generierung vermieden werden soll.
</ParamField>

<ParamField body="seed" type="integer">
  Zufalls-Seed für reproduzierbare Generierung.
</ParamField>

<ParamField body="cfg_scale" type="number">
  Classifier-free Guidance-Skala (0–20).
</ParamField>

<ParamField body="motion_strength" type="number">
  Bewegungsintensität (0–1).
</ParamField>

<ParamField body="start_image" type="string">
  URL des Startframe-Bilds.
</ParamField>

<ParamField body="end_image" type="string">
  URL des Endframe-Bilds.
</ParamField>

<ParamField body="user" type="string">
  Eine eindeutige Kennung für den Endbenutzer.
</ParamField>

## Antwort

<ResponseField name="task_id" type="string">
  Eindeutige Aufgabenkennung für Polling.
</ResponseField>

<ResponseField name="poll_url" type="string">
  Empfohlene Polling-URL für diese Aufgabe. Verwenden Sie beim Prüfen des Status genau diesen Pfad.
</ResponseField>

<ResponseField name="status" type="string">
  Anfangsstatus: `pending`.
</ResponseField>

<ResponseField name="model" type="string">
  Verwendetes Modell.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST "https://api.lemondata.cc/v1/videos/generations" \
  -H "Authorization: Bearer sk-your-api-key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sora-2",
    "prompt": "A cat walking through a garden, cinematic lighting",
    "operation": "text-to-video",
    "duration": 4,
    "aspect_ratio": "16:9"
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.lemondata.cc/v1/videos/generations",
    headers={
        "Authorization": "Bearer sk-your-api-key",
        "Content-Type": "application/json"
    },
    json={
        "model": "sora-2",
        "prompt": "A cat walking through a garden, cinematic lighting",
        "operation": "text-to-video",
        "duration": 4,
        "aspect_ratio": "16:9"
    }
)

data = response.json()
task_id = data["task_id"]
print(f"Poll URL: {data['poll_url']}")
print(f"Task ID: {task_id}")
```

```javascript JavaScript
const response = await fetch('https://api.lemondata.cc/v1/videos/generations', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer sk-your-api-key',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'sora-2',
    prompt: 'A cat walking through a garden, cinematic lighting',
    operation: 'text-to-video',
    duration: 4,
    aspect_ratio: '16:9'
  })
});

const data = await response.json();
console.log(`Poll URL: ${data.poll_url}`);
console.log(`Task ID: ${data.task_id}`);
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "net/http"
)

func main() {
    payload := map[string]interface{}{
        "model":    "sora-2",
        "prompt":   "A cat walking through a garden, cinematic lighting",
        "operation": "text-to-video",
        "duration": 4,
        "aspect_ratio": "16:9",
    }
    body, _ := json.Marshal(payload)

    req, _ := http.NewRequest("POST", "https://api.lemondata.cc/v1/videos/generations", bytes.NewBuffer(body))
    req.Header.Set("Authorization", "Bearer sk-your-api-key")
    req.Header.Set("Content-Type", "application/json")

    client := &http.Client{}
    resp, _ := client.Do(req)
    defer resp.Body.Close()

    var result map[string]interface{}
    json.NewDecoder(resp.Body).Decode(&result)
    fmt.Printf("Poll URL: %s\n", result["poll_url"])
    fmt.Printf("Task ID: %s\n", result["task_id"])
}
```

```php PHP
<?php
$ch = curl_init('https://api.lemondata.cc/v1/videos/generations');

curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_HTTPHEADER => [
        'Content-Type: application/json',
        'Authorization: Bearer sk-your-api-key'
    ],
    CURLOPT_POSTFIELDS => json_encode([
        'model' => 'sora-2',
        'prompt' => 'A cat walking through a garden, cinematic lighting',
        'operation' => 'text-to-video',
        'duration' => 4,
        'aspect_ratio' => '16:9'
    ])
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
echo "Poll URL: " . $data['poll_url'] . "\n";
echo "Task ID: " . $data['task_id'];
```
</RequestExample>

<ResponseExample>
```json Response
{
  "task_id": "video_abc123",
  "poll_url": "/v1/tasks/video_abc123",
  "status": "pending",
  "model": "sora-2",
  "created_at": 1706000000
}
```
</ResponseExample>

## Bild zu Video

```python
response = requests.post(
    "https://api.lemondata.cc/v1/videos/generations",
    headers={"Authorization": "Bearer sk-your-api-key"},
    json={
        "model": "hailuo-2.3-fast",
        "prompt": "The scene begins from the provided image and adds gentle natural motion.",
        "operation": "image-to-video",
        "image_url": "https://example.com/image.jpg",
        "duration": 6,
        "aspect_ratio": "16:9"
    }
)
```

## Keyframe-Steuerung

Verwenden Sie `start_image` und `end_image`, um den ersten und letzten Frame zu steuern (unterstützt von Vidu Q2 Pro):

```python
response = requests.post(
    "https://api.lemondata.cc/v1/videos/generations",
    headers={"Authorization": "Bearer sk-your-api-key"},
    json={
        "model": "viduq2-pro",
        "prompt": "Smooth transition from day to night",
        "operation": "start-end-to-video",
        "start_image": "https://example.com/day.jpg",
        "end_image": "https://example.com/night.jpg",
        "duration": 5,
        "resolution": "720p",
        "aspect_ratio": "16:9"
    }
)
```

## Verfügbare Modelle

<Info>
Die Modellliste wird häufig aktualisiert. Die neuesten verfügbaren Modelle und Preise finden Sie auf der [Modellseite](https://lemondata.cc/de/models).
</Info>

Eine vollständige Anleitung mit Fähigkeitsvergleichen und Nutzungsbeispielen finden Sie im [Leitfaden zur Video-Generierung](/guides/video-generation).

### Sora (OpenAI)

| Modell | Funktionen |
|-------|-------------|
| `sora-2` | T2V (Standard) |
| `sora-2-pro` | T2V, höhere Qualität |
| `sora-2-characters` | T2V, charakterfokussiert |

### Kling (Kuaishou)

| Modell | Funktionen |
|-------|-------------|
| `kling-v2.6-pro` | T2V, neueste Generation |
| `kling-v2.6-std` | T2V, schnell |
| `kling-v2.5-turbo-std` | T2V, Turbo-Stufe |
| `kling-v2.1-master` | T2V, I2V, Master-Qualität |
| `kling-v2.1-pro` | T2V, I2V |
| `kling-video` | T2V, I2V |
| `kling-video-extend` | Video-Erweiterung |
| `kling-video-o1-pro` | T2V, O1-Reasoning |
| `kling-effects` | Visuelle Effekte |

### Veo (Google)

| Modell | Funktionen |
|-------|-------------|
| `veo2` | T2V |
| `veo3.1` | T2V, I2V, keyframe |
| `veo3.1-pro` | T2V, professionell |
| `veo3.1-4k` | T2V, 4K-Ausgabe |
| `veo3.1-fast` | T2V, schnell |
| `veo3` | T2V, vorherige Generation |

### Seedance (ByteDance)

| Modell | Funktionen |
|-------|-------------|
| `seedance-2-0` | T2V, I2V, Keyframe, Erweiterung, Bearbeitung |
| `seedance-1-5-pro` | T2V, I2V |
| `seedance-1-0-pro` | T2V, I2V |
| `seedance-1-0-lite-t2v` | T2V, leichtgewichtig |
| `seedance-1-0-lite-i2v` | I2V, leichtgewichtig |

### Andere Anbieter

| Modell | Anbieter | Funktionen |
|-------|----------|-------------|
| `hailuo-2.3` | MiniMax | T2V, I2V |
| `hailuo-2.3-pro` | MiniMax | T2V, I2V, höhere Qualität |
| `hailuo-2.3-fast` | MiniMax | T2V, I2V, schnelle Stufe |
| `wan-2.6` | Alibaba | T2V |
| `wan2.6-i2v` | Alibaba | I2V |
| `runwayml-gen4-turbo-5` | Runway | I2V (5s) |
| `runwayml-gen4-turbo-10` | Runway | I2V (10s) |
| `luma-video-api` | Luma | T2V |
| `luma-video-extend-api` | Luma | Video-Erweiterung |
| `viduq3-pro` | Shengshu | T2V, I2V, keyframe |
| `viduq3-turbo` | Shengshu | T2V, I2V, Turbo-Stufe |
| `viduq2-pro` | Shengshu | I2V, keyframe, reference |
| `grok-video-3` | xAI | T2V |
| `higgsfield-turbo` | Higgsfield | T2V |