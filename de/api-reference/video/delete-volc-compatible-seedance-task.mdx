---
title: "Aufgabe abbrechen (Volc-kompatibel)"
openapi: "DELETE /api/v3/contents/generations/tasks/{id}"
description: "Brechen Sie eine wartende oder abbrechbare Seedance-Kompatibilitätsaufgabe ab."
---

## Überblick

Diese Endpunkte sind für Teams gedacht, die bereits die Volc-ähnliche Seedance-Aufgabenstruktur verwenden. Die Authentifizierung erfolgt mit einem TokenLab Bearer API Key. Die zurückgegebene Aufgaben-ID beginnt mit `cgt-`; fragen Sie den Status ab, bis `status` `succeeded`, `failed`, `cancelled` oder `expired` ist.

Siehe auch [Seedance 2.0 Videomodelle](/de/guides/seedance-2-video) und [Videogenerierung](/de/guides/video-generation).

## Antwortformat

Eine erfolgreiche Abbruchanfrage gibt HTTP 200 mit leerem Body zurück. Fragen Sie die Aufgabe anschließend ab, wenn Sie den endgültigen Status bestätigen möchten.

## Beispiel

```bash
curl -X DELETE https://api.tokenlab.sh/api/v3/contents/generations/tasks/cgt-0123456789abcdef0123456789abcdef \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```
