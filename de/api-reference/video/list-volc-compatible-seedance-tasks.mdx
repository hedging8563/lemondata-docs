---
title: "Seedance-Aufgaben auflisten (Volc-kompatibel)"
openapi: "GET /api/v3/contents/generations/tasks"
description: "Listen Sie aktuelle Seedance-kompatible Aufgaben der aktuellen Organisation auf."
---

## Überblick

Diese Endpunkte sind für Teams gedacht, die bereits die Volc-ähnliche Seedance-Aufgabenstruktur verwenden. Die Authentifizierung erfolgt mit einem TokenLab Bearer API Key. Die zurückgegebene Aufgaben-ID beginnt mit `cgt-`; fragen Sie den Status ab, bis `status` `succeeded`, `failed`, `cancelled` oder `expired` ist.

Siehe auch [Seedance 2.0 Videomodelle](/de/guides/seedance-2-video) und [Videogenerierung](/de/guides/video-generation).

## Antwortformat

Die Listenantwort enthält `items` und `total`. Unterstützte Filter sind `filter.status`, `filter.task_ids`, `filter.model` und `filter.service_tier`; `page_num` und `page_size` steuern die Paginierung.

## Beispiel

```bash
curl "https://api.tokenlab.sh/api/v3/contents/generations/tasks?page_num=1&page_size=20&filter.status=succeeded" \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```
