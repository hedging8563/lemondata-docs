---
title: "Seedance-Aufgabe abrufen (Volc-kompatibel)"
openapi: "GET /api/v3/contents/generations/tasks/{id}"
description: "Lesen Sie eine Volc-ähnliche Seedance-Aufgabenantwort per kompatibler Aufgaben-ID."
---

## Überblick

Diese Endpunkte sind für Teams gedacht, die bereits die Volc-ähnliche Seedance-Aufgabenstruktur verwenden. Die Authentifizierung erfolgt mit einem TokenLab Bearer API Key. Die zurückgegebene Aufgaben-ID beginnt mit `cgt-`; fragen Sie den Status ab, bis `status` `succeeded`, `failed`, `cancelled` oder `expired` ist.

Siehe auch [Seedance 2.0 Videomodelle](/de/guides/seedance-2-video) und [Videogenerierung](/de/guides/video-generation).

## Antwortformat

Die Aufgabenantwort verwendet `queued`, `running`, `succeeded`, `failed`, `cancelled` oder `expired`. Erfolgreiche Aufgaben liefern `content.video_url`; sofern verfügbar erscheinen auch `content.last_frame_url`, gespiegelte Anfrageparameter wie `return_last_frame` und `watermark`, `usage` und `progress`. Ohne Fehler ist `error` `null`.

## Beispiel

```bash
curl https://api.tokenlab.sh/api/v3/contents/generations/tasks/cgt-0123456789abcdef0123456789abcdef \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```
