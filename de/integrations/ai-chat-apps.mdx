---
title: "AI-Chat-Anwendungen"
description: "Konfigurieren Sie beliebte AI-Chat-Anwendungen für die Verwendung der OpenAI-kompatiblen API von LemonData"
---

## Übersicht

Die meisten allgemeinen AI-Chat-Apps verbinden sich über den standardmäßigen OpenAI-kompatiblen Chat-Pfad mit LemonData.

In der Praxis bedeutet das:
- Verwenden Sie `https://api.lemondata.cc/v1` als Base URL, sofern die App nicht ausdrücklich nach einer vollständigen Endpoint-URL fragt
- Verwenden Sie Ihren LemonData API-Schlüssel als API-Schlüssel im OpenAI-Stil
- Wählen Sie Modelle aus, die die App in einem standardmäßigen Chat-Workflow verwenden kann

<Note>
Allgemeine Chat-Apps sind in der Regel auf **chat-completions** ausgerichtet, nicht auf Codex Responses WebSocket-Clients. Für Codex CLI/App, Cursor oder andere Coding-Tools, die Responses-spezifisches Verhalten benötigen, verwenden Sie stattdessen die dedizierten Integrationsleitfäden.
</Note>

## Unterstützte Anwendungen

| Application | Platform | Notes |
|-------------|----------|-------|
| LobeChat | Web, Desktop, Self-hosted | Starke Unterstützung für benutzerdefinierte Provider |
| Cherry Studio | Windows, macOS, Linux | Guter OpenAI-kompatibler Provider-Workflow |
| Chatbox | Windows, macOS, Linux | Einfache OpenAI-kompatible Einrichtung |
| TypingMind | Web, Desktop | Verwendet einen benutzerdefinierten Endpoint-Stil |
| BotGem | iOS, macOS | Benutzerdefinierte Provider-Konfiguration |
| OpenCat | iOS, macOS | OpenAI-kompatibler benutzerdefinierter Endpoint-Workflow |
| ChatWise | Windows, macOS | Standardmuster für benutzerdefinierte Provider |

## Standard-Verbindungswerte

| Field | Value |
|-------|-------|
| Base URL | `https://api.lemondata.cc/v1` |
| API Key | `sk-your-lemondata-key` |
| API Type | OpenAI Compatible |

Wenn eine App statt einer Base URL eine **vollständige Endpoint-URL** verlangt, verwenden Sie:
- Chat Completions: `https://api.lemondata.cc/v1/chat/completions`

## Beispiel-App-Konfigurationen

<Tabs>
  <Tab title="LobeChat">
    **Self-hosted LobeChat**

    ```bash
    OPENAI_API_KEY=sk-your-lemondata-key
    OPENAI_PROXY_URL=https://api.lemondata.cc/v1
    ```

    **LobeChat Cloud**
    1. Öffnen Sie **Settings** → **AI Service Provider**
    2. Wählen Sie **OpenAI**
    3. Geben Sie Ihren LemonData API-Schlüssel ein
    4. Setzen Sie den benutzerdefinierten Endpoint auf `https://api.lemondata.cc/v1`
  </Tab>

  <Tab title="Cherry Studio">
    1. Öffnen Sie **Settings** → **Model Provider**
    2. Fügen Sie einen **OpenAI-compatible** Provider hinzu
    3. Geben Sie ein:
       - **Name**: LemonData
       - **API Base URL**: `https://api.lemondata.cc/v1`
       - **API Key**: `sk-your-lemondata-key`
    4. Fügen Sie die Modelle hinzu, die Sie verwenden möchten
  </Tab>

  <Tab title="Chatbox">
    1. Öffnen Sie **Settings** → **AI Model**
    2. Wählen Sie **OpenAI API Compatible**
    3. Geben Sie ein:
       - **API Host**: `https://api.lemondata.cc/v1`
       - **API Key**: `sk-your-lemondata-key`
       - **Model**: `gpt-5-mini` oder `gpt-4o`
  </Tab>

  <Tab title="TypingMind">
    1. Öffnen Sie **Settings**
    2. Wählen Sie **Custom Endpoint**
    3. Geben Sie ein:
       - **Endpoint URL**: `https://api.lemondata.cc/v1/chat/completions`
       - **API Key**: `sk-your-lemondata-key`
    4. Konfigurieren Sie die Modelle, die Sie verfügbar machen möchten
  </Tab>
</Tabs>

## Sicherere Standardmodelle

| Category | Suggested Models |
|----------|------------------|
| General default | `gpt-5.4`, `gpt-5-mini`, `gpt-4o` |
| Anthropic | `claude-sonnet-4-6`, `claude-opus-4-6` |
| Google | `gemini-2.5-flash`, `gemini-2.5-pro` |
| DeepSeek | `deepseek-r1`, `deepseek-v3-2` |

## Tipps

<AccordionGroup>
  <Accordion title="Base URL bevorzugen, wenn die App sie unterstützt">
    Die meisten Apps lassen sich einfacher konfigurieren und sind weniger fehleranfällig, wenn Sie `https://api.lemondata.cc/v1` als Base URL statt einer vollständigen Endpoint-URL angeben.
  </Accordion>

  <Accordion title="Verwenden Sie zuerst kleinere Modelle">
    Beginnen Sie für alltägliche Chats mit `gpt-5-mini` oder `gpt-4o` und verlagern Sie anspruchsvollere Reasoning-Aufgaben anschließend auf `gpt-5.4` oder `claude-opus-4-6`.
  </Accordion>

  <Accordion title="Coding-Tools getrennt halten">
    Allgemeine Chat-Apps und Coding-Agents benötigen oft unterschiedliches Endpoint-Verhalten. Halten Sie Tools im Codex/Cursor-Stil auf ihren dedizierten Integrationsseiten.
  </Accordion>
</AccordionGroup>

## Fehlerbehebung

<AccordionGroup>
  <Accordion title="Verbindungsfehler">
    - Vergewissern Sie sich, dass die Base URL exakt `https://api.lemondata.cc/v1` ist
    - Entfernen Sie nachgestellte Schrägstriche, wenn die App automatisch Pfade anhängt
    - Wenn die App eine vollständige Endpoint-URL verlangt, verwenden Sie `https://api.lemondata.cc/v1/chat/completions`
  </Accordion>

  <Accordion title="401 Unauthorized">
    - Überprüfen Sie Ihren API-Schlüssel erneut
    - Bestätigen Sie, dass der Schlüssel im LemonData-Dashboard aktiv ist
    - Stellen Sie sicher, dass keine zusätzlichen Leerzeichen oder Zeilenumbrüche vorhanden sind
  </Accordion>

  <Accordion title="Modell nicht verfügbar">
    - Überprüfen Sie den Modellnamen exakt
    - Versuchen Sie einen weithin unterstützten Standard wie `gpt-5-mini`, `gpt-4o` oder `claude-sonnet-4-6`
    - Prüfen Sie die aktuelle Verfügbarkeit in den LemonData-Dokumenten oder im Dashboard
  </Accordion>
</AccordionGroup>