---
title: "Daftar tugas (kompatibel Volc)"
openapi: "GET /api/v3/contents/generations/tasks"
description: "Cantumkan tugas kompatibel Seedance terbaru di organisasi saat ini."
---

## Ringkasan

Endpoint ini ditujukan untuk tim yang sudah memakai bentuk tugas Seedance bergaya Volc. Autentikasi memakai TokenLab Bearer API key. ID tugas yang dikembalikan diawali `cgt-`; polling status sampai `status` menjadi `succeeded`, `failed`, `cancelled`, atau `expired`.

Lihat juga [Model Video Seedance 2.0](/id/guides/seedance-2-video) dan [Pembuatan Video](/id/guides/video-generation).

## Bentuk respons

Respons daftar mengembalikan `items` dan `total`. Filter yang didukung adalah `filter.status`, `filter.task_ids`, `filter.model`, dan `filter.service_tier`, dengan `page_num` dan `page_size` untuk paginasi.

## Contoh

```bash
curl "https://api.tokenlab.sh/api/v3/contents/generations/tasks?page_num=1&page_size=20&filter.status=succeeded" \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```
