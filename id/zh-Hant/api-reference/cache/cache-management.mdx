---
title: "Hapus Cache"
openapi: "DELETE /v1/cache/clear"
description: "Menghapus cache semantik untuk pengguna atau organisasi saat ini"
---

## Ikhtisar

Menghapus semua respons cache yang terkait dengan API key Anda. Ini memengaruhi response cache dan semantic cache secara bersamaan.

<Note>
  Cache dikelola secara otomatis. Gunakan endpoint ini hanya jika Anda perlu memaksa pengambilan respons baru.
</Note>

## Respons

<ResponseField name="success" type="boolean">
  Apakah cache berhasil dihapus.
</ResponseField>

<ResponseField name="cleared" type="integer">
  Jumlah item cache yang telah dihapus.
</ResponseField>

<RequestExample>
```bash cURL
curl -X DELETE "https://api.lemondata.cc/v1/cache/clear" \
  -H "Authorization: Bearer sk-your-api-key"
```

```python Python
import requests

response = requests.delete(
    "https://api.lemondata.cc/v1/cache/clear",
    headers={"Authorization": "Bearer sk-your-api-key"}
)

print(response.json())
```

```javascript JavaScript
const response = await fetch(
  'https://api.lemondata.cc/v1/cache/clear',
  {
    method: 'DELETE',
    headers: { 'Authorization': 'Bearer sk-your-api-key' }
  }
);

const data = await response.json();
console.log(data);
```

```go Go
package main

import (
    "encoding/json"
    "fmt"
    "net/http"
)

func main() {
    req, _ := http.NewRequest("DELETE", "https://api.lemondata.cc/v1/cache/clear", nil)
    req.Header.Set("Authorization", "Bearer sk-your-api-key")

    client := &http.Client{}
    resp, _ := client.Do(req)
    defer resp.Body.Close()

    var result map[string]interface{}
    json.NewDecoder(resp.Body).Decode(&result)
    fmt.Println(result)
}
```

```php PHP
<?php
$ch = curl_init('https://api.lemondata.cc/v1/cache/clear');

curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_CUSTOMREQUEST => 'DELETE',
    CURLOPT_HTTPHEADER => [
        'Authorization: Bearer sk-your-api-key'
    ]
]);

$response = curl_exec($ch);
curl_close($ch);

$data = json_decode($response, true);
print_r($data);
```
</RequestExample>

<ResponseExample>
```json Response
{
  "success": true,
  "cleared": 42
}
```
</ResponseExample>

## Skenario Penggunaan

<AccordionGroup>
  <Accordion title="Menguji respons baru">
    Selama proses pengembangan, hapus cache untuk memastikan Anda mendapatkan respons API terbaru.
  </Accordion>

  <Accordion title="Setelah memperbarui prompt">
    Jika Anda melakukan perubahan signifikan pada prompt, hapus cache untuk menghindari pencocokan semantik yang kedaluwarsa.
  </Accordion>

  <Accordion title="Debugging">
    Saat melakukan debugging terhadap respons yang tidak terduga, hapus cache untuk menyingkirkan gangguan dari hasil cache.
  </Accordion>
</AccordionGroup>