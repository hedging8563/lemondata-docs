---
title: "Instalasi Self-Hosted"
description: "Jalankan LemonClaw di server Anda sendiri dengan LemonData AI API — satu perintah, tanpa Docker"
---

<Note>
**Self-hosted** berarti Anda menjalankan LemonClaw langsung di VPS atau mesin Anda sendiri. Anda hanya membayar penggunaan model AI melalui saldo LemonData — tanpa biaya instans.
</Note>

## Instalasi Cepat

<CodeGroup>
```bash Linux / macOS
curl -fsSL https://raw.githubusercontent.com/hedging8563/lemonclaw/main/deploy/self-hosted/install.sh | bash
```

```bash Non-interactive
LEMONDATA_API_KEY=sk-xxx curl -fsSL https://raw.githubusercontent.com/hedging8563/lemonclaw/main/deploy/self-hosted/install.sh | bash
```
</CodeGroup>

Installer akan meminta **LemonData API Key** Anda. Dapatkan dari [Dashboard](https://lemondata.cc/dashboard).

## Yang Diinstal

<Steps>
  <Step title="Node.js 22+">
    Diinstal otomatis jika belum ada (melalui NodeSource di Linux, Homebrew di macOS)
  </Step>
  <Step title="LemonClaw">
    Paket LemonClaw resmi dari npm, sudah dikonfigurasi dengan LemonData API
  </Step>
  <Step title="15 Model AI">
    Claude, GPT-5, Gemini, DeepSeek, dan lainnya — siap digunakan
  </Step>
  <Step title="Plugin IM">
    Plugin DingTalk, WeCom, dan QQ sudah terinstal
  </Step>
  <Step title="Layanan Sistem">
    Otomatis berjalan saat boot (systemd di Linux, launchd di macOS)
  </Step>
</Steps>

## Persyaratan

| Persyaratan | Detail |
|-------------|--------|
| **OS** | Linux (Ubuntu/Debian/CentOS/RHEL) atau macOS |
| **Arsitektur** | x64 atau arm64 |
| **RAM** | Minimum 1 GB, disarankan 2 GB |
| **API Key** | [Dapatkan di sini](https://lemondata.cc) |

## Setelah Instalasi

Installer menampilkan kredensial akses Anda:

```
✓ LemonClaw installed successfully!

  WebUI:        http://your-ip:18789
  Token:        your-gateway-token
  Auto-login:   http://your-ip:18789?token=your-gateway-token
  Model:        claude-sonnet-4-6
```

<Warning>
**Simpan Token Anda!** Anda membutuhkannya untuk mengakses WebUI dan menghubungkan platform pesan.
</Warning>

## Hubungkan Platform Pesan

Akses WebUI di `http://your-ip:18789` dengan Token Anda, lalu ikuti panduan untuk setiap platform:

<CardGroup cols={2}>
  <Card title="Telegram" icon="paper-plane" href="/claw/telegram" />
  <Card title="Discord" icon="discord" href="/claw/discord" />
  <Card title="WhatsApp" icon="whatsapp" href="/claw/whatsapp" />
  <Card title="Slack" icon="slack" href="/claw/slack" />
  <Card title="DingTalk" icon="bell" href="/claw/dingtalk" />
  <Card title="WeCom" icon="building" href="/claw/wecom" />
</CardGroup>

## Perintah Manajemen

<Tabs>
  <Tab title="Linux">
    ```bash
    systemctl --user status lemonclaw    # Cek status
    journalctl --user -u lemonclaw -f    # Lihat log
    systemctl --user restart lemonclaw   # Mulai ulang
    systemctl --user stop lemonclaw      # Hentikan
    ```
  </Tab>
  <Tab title="macOS">
    ```bash
    tail -f ~/.lemonclaw/lemonclaw.log    # Lihat log
    launchctl stop cc.lemondata.lemonclaw && launchctl start cc.lemondata.lemonclaw  # Mulai ulang
    launchctl stop cc.lemondata.lemonclaw  # Hentikan
    ```
  </Tab>
</Tabs>

## Konfigurasi

Lokasi file konfigurasi: `~/.lemonclaw/config.json`

Setelah mengedit, mulai ulang layanan untuk menerapkan perubahan.

### Ubah Model Default

```json
{
  "agents": {
    "defaults": {
      "model": {
        "primary": "lemondata-claude/claude-opus-4-6"
      }
    }
  }
}
```

Prefiks model yang tersedia:
- `lemondata-claude/` — Model Claude (anthropic-messages API)
- `lemondata-minimax/` — Model MiniMax (anthropic-messages API)
- `lemondata/` — Semua model lainnya (openai-completions API)

## Upgrade

```bash
curl -fsSL https://raw.githubusercontent.com/hedging8563/lemonclaw/main/deploy/self-hosted/install.sh | bash
# Lalu mulai ulang layanan
systemctl --user restart lemonclaw  # Linux
# atau
launchctl stop cc.lemondata.lemonclaw && launchctl start cc.lemondata.lemonclaw  # macOS
```

## Hapus Instalasi

```bash
# Hentikan dan nonaktifkan layanan
systemctl --user stop lemonclaw && systemctl --user disable lemonclaw  # Linux
launchctl unload ~/Library/LaunchAgents/cc.lemondata.lemonclaw.plist  # macOS

# Hapus LemonClaw
rm -rf ~/.lemonclaw
rm -rf ~/.lemonclaw
```

## Self-Hosted vs Terkelola

| | Self-Hosted | Terkelola (Claw) |
|---|---|---|
| **Biaya instans** | Gratis | $20 / 30 hari |
| **Penggunaan AI** | Bayar sesuai pemakaian | Bayar sesuai pemakaian |
| **Infrastruktur** | Server Anda | Kami kelola semuanya |
| **Pembaruan** | Manual (`npm update`) | Otomatis |
| **Dukungan** | Komunitas | Prioritas |

<Info>
**Kode sumber**: [github.com/hedging8563/lemonclaw](https://github.com/hedging8563/lemonclaw)
</Info>
