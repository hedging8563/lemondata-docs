---
title: "TokenLab MCP Server"
description: "將 MCP 客戶端連接至 TokenLab 的 OpenAPI 生成工具，以使用原生 AI 端點、多模態媒體、檔案及非同步任務。"
---

[TokenLab MCP server](https://github.com/hedging8563/tokenlab-mcp-server) 從 TokenLab 的公開 OpenAPI 合約中生成 MCP 工具。支援 MCP 的代理程式 (Agents) 可以探索模型與定價、呼叫 OpenAI 相容及原生 LLM 端點、建立多模態媒體、上傳檔案以及管理非同步任務。

當代理程式需要最新的 TokenLab API 合約而非手動維護的工具列表時，請使用此伺服器。預設的 `core` 設定檔會公開 29 個生成的端點工具以及兩個複合式探索工具。`full` 設定檔則會公開 76 個生成的端點工具以及相同的兩個複合式工具。

<Note>
公開目錄與定價工具不需要 TokenLab API key。若要進行憑證授權的推論 (Inference)、媒體、音訊、檔案、任務、嵌入 (Embedding)、重排序 (Rerank) 及翻譯操作，請設定 `TOKENLAB_API_KEY`。切勿將 API key 放入工具參數中。
</Note>

## 它提供了什麼

- 從 `https://api.tokenlab.sh/v1/models` 進行即時模型探索。
- 從 `/v1/models/{model}` 查詢模型詳細資訊。
- 從 `/v1/models/{model}/pricing` 查詢定價。
- 從 `https://api.tokenlab.sh/llms.txt` 獲取代理程式可讀的 API 概覽。
- OpenAI 相容的 Chat Completions 與 Responses、Anthropic Messages 以及 Gemini generateContent，且無需扁平化其原生請求格式。
- 透過 JSON URL、TokenLab 檔案 ID 或本地 multipart 檔案進行圖像生成與編輯。
- 支援影片、音樂、3D、語音、轉錄、音訊翻譯、嵌入、多模態嵌入、重排序及文字翻譯。
- 針對圖像、影片、音樂、3D 及世界生成操作的標準化非同步任務摘要。
- 每日合約同步：獲取公開 OpenAPI 文件、重新生成工具架構、執行端點與檔案測試，並僅在所有檢查通過時更新 `main` 分支。

## 從 GitHub 安裝

複製公開儲存庫並安裝依賴項目：

```bash
git clone https://github.com/hedging8563/tokenlab-mcp-server.git
cd tokenlab-mcp-server
npm install
npm test
```

透過 stdio 啟動伺服器：

```bash
npm start
```

選用的 `TOKENLAB_API_BASE` 環境變數預設為 `https://api.tokenlab.sh`。當客戶端需要公開更廣泛的開發者 API 而非預設的 `core` 設定檔時，請設定 `TOKENLAB_MCP_TOOL_PROFILE=full`。

## 設定任意 stdio MCP 用戶端

所有支援本機 stdio MCP 伺服器的用戶端都使用同一組值。用戶端可能將它們顯示為表單欄位，或儲存在 JSON、TOML、YAML 中，但欄位含義不變。請將每個參數分別新增為一項。

| 欄位 | 填寫內容 | 說明 |
|---|---|---|
| 伺服器名稱 | `tokenlab-model-catalog` | 可使用任何未重複的名稱。 |
| 傳輸類型 | `stdio` | TokenLab MCP 以本機子程序執行。 |
| 啟動命令 | `npx` | 啟動已發佈的 npm 套件。 |
| 參數 | 先填 `-y`，再填 `@tokenlabai/mcp-server` | 若要固定目前已驗證版本，請使用 `@tokenlabai/mcp-server@0.6.1`。 |
| 環境變數 | `TOKENLAB_API_KEY` = `<TOKENLAB_API_KEY>` | 公開目錄工具可留空；推論與媒體工具需要此值。 |
| 環境變數傳遞 | 留空，或填入 `TOKENLAB_API_KEY` | 僅當 Key 已存在於用戶端的本機環境時才填入變數名稱。請勿在兩個位置重複設定同一把 Key。 |
| 工作目錄 | 留空 | 此套件透過 `npx` 啟動，不需要專案工作目錄。 |

對於接受 `mcpServers` JSON 物件的用戶端，可直接複製以下完整設定：

```json
{
  "mcpServers": {
    "tokenlab-model-catalog": {
      "command": "npx",
      "args": ["-y", "@tokenlabai/mcp-server"],
      "env": {
        "TOKENLAB_API_KEY": "<TOKENLAB_API_KEY>"
      }
    }
  }
}
```

若只需要公開模型與定價工具，請刪除 `env` 區塊。如果用戶端要求單一伺服器物件而不是 `mcpServers` 外層，請只複製包含 `command`、`args` 與 `env` 的內部物件。

<Warning>
請勿發佈含有真實 API Key 的螢幕截圖、日誌或設定檔。範例請一律使用佔位符；若 Key 已曝光，請立即撤銷並重新產生。
</Warning>

## 在 Codex 中安裝

將公開目錄伺服器加入您的 Codex 設定：

```bash
codex mcp add tokenlab-model-catalog -- npx -y @tokenlabai/mcp-server
```

加入伺服器後，請啟動新的 Codex 工作階段。公開目錄工具不需要 `TOKENLAB_API_KEY`。

若要啟用推論工具，請在加入伺服器時包含您的 TokenLab API key：

```bash
codex mcp add --env TOKENLAB_API_KEY=your-tokenlab-api-key tokenlab-model-catalog -- npx -y @tokenlabai/mcp-server
```

## 在 Cline 中安裝

使用 Cline CLI 安裝已發佈的 stdio 伺服器：

```bash
cline mcp add tokenlab --yes -- npx -y @tokenlabai/mcp-server
```

此指令已透過 Cline CLI 驗證，執行時不會出現設定警告。公開目錄工具可立即使用；當您需要推論工具時，請在 Cline 環境中設定 `TOKENLAB_API_KEY`。

## Claude Desktop 設定

將已發佈的 npm 套件加入您的 MCP 客戶端設定：

```json
{
  "mcpServers": {
    "tokenlab-model-catalog": {
      "command": "npx",
      "args": ["-y", "@tokenlabai/mcp-server"]
    }
  }
}
```

儲存設定後請重新啟動 MCP 客戶端。

## Cursor、Windsurf 及其他 MCP 客戶端

在任何支援 stdio MCP 伺服器的客戶端中使用相同的指令與參數：

```json
{
  "command": "npx",
  "args": ["-y", "@tokenlabai/mcp-server"],
  "env": {
    "TOKENLAB_API_BASE": "https://api.tokenlab.sh"
  }
}
```

如果您的客戶端儲存了具名伺服器，請使用 `tokenlab-model-catalog` 作為伺服器名稱。

## 生成的工具設定檔

| 設定檔 | 公開工具數量 | 覆蓋範圍 |
|---|---:|---|
| `catalog` | 6 | 4 個生成的公開目錄與定價工具，加上 `compare_models` 與 `get_api_overview`。不需要 API Key。 |
| `core` (預設) | 31 | 29 個生成的端點工具加上 `compare_models` 與 `get_api_overview`。涵蓋目錄、定價、原生 LLM 端點、圖像、影片、音樂、3D、音訊、任務、檔案、嵌入、重排序及翻譯。 |
| `full` | 78 | 76 個生成的端點工具加上兩個複合式探索工具。增加回應生命週期、批次處理、世界生成、原生模型探索及其他已列入許可清單的開發者操作。 |

工具名稱、說明、輸入架構、HTTP 方法、路徑、內容類型、驗證要求及任務行為均由已簽入的 OpenAPI 快照生成。僅限 MCP 的覆蓋層提供了穩定的工具別名、設定檔成員資格、非同步任務語意、multipart 變體、隱藏機密資訊及非串流限制。

0.6.0 版也會透過 MCP `structuredContent` 回傳 JSON，發佈 3 個契約資源，並提供 2 個用於模型選擇和原生請求建構的提示範本。生成工具和複合工具都帶有標準風險註解，讓相容客戶端能套用更安全的核准策略。

常見的 `core` 工具包括：

| 類別 | 代表性工具 |
|---|---|
| 目錄與定價 | `list_models`, `get_model`, `get_model_pricing`, `get_pricing`, `compare_models` |
| 原生 LLM 端點 | `create_chat_completion`, `create_response`, `create_anthropic_message`, `create_gemini_content` |
| 圖像 | `create_image`, `create_image_file`, `edit_image`, `edit_image_file` |
| 影片、音樂與 3D | `create_video`, `create_music`, `create_3d_model` |
| 音訊 | `create_speech`, `transcribe_audio`, `translate_audio` |
| 嵌入與文字 | `create_embedding`, `create_multimodal_embedding`, `rerank_documents`, `translate_text` |
| 檔案與任務 | `upload_file`, `list_files`, `retrieve_file`, `retrieve_file_content`, `delete_file`, `get_task_status`, `cancel_task` |

MCP 工具呼叫會回傳一個最終結果。不包含僅限串流與即時操作，且生成的請求架構會將支援的 `stream` 欄位限制為 `false`。小型圖像與音訊回應會回傳原生 MCP 內容；較大的二進位回應會寫入 `TOKENLAB_ARTIFACT_DIR` 並回傳其本地路徑、MIME 類型及位元組計數。

## 非同步媒體結果

影片、音樂及 3D 建立會回傳非同步任務摘要。圖像生成與編輯可能會根據所選模型與請求，回傳已完成的結果或非同步任務。

當 `delivery.mode` 為 `async` 時，請持續呼叫 `get_task_status` 並傳入 `{ "id": delivery.task_id }`，直到 `delivery.terminal` 為 `true`。請根據 `status` 判斷完成狀態；`progress` 為選用欄位，不得作為終止條件。

## 建議的代理程式工作流程

1. 當使用者未指定模型時，呼叫 `list_models`。
2. 針對非聊天任務（如圖像、影片、音樂、3D、TTS、STT、嵌入、重排序或翻譯）使用 `recommended_for`。
3. 在建立非聊天請求、重試失敗請求或切換端點系列之前，呼叫 `get_model`。
4. 當模型成本影響使用者選擇時，呼叫 `get_model_pricing`。
5. 當選擇取決於多個候選項目時，使用 `compare_models`。
6. 呼叫符合所需 API 合約的生成工具，或使用 `get_api_overview` 獲取 TokenLab API 系列的精簡地圖。

## 原生端點指南

TokenLab 支援 OpenAI 相容路由與原生端點系列。MCP 伺服器可協助代理程式在編寫程式碼前選擇正確的路由：

| 系列 | 常見路由 |
|---|---|
| OpenAI 相容聊天 | `/v1/chat/completions` |
| 回應 (Responses) | `/v1/responses` |
| Anthropic Messages | `/v1/messages` |
| Gemini 原生 | `/v1beta/models/{model}:generateContent` |
| 圖像 | `/v1/images/generations`, `/v1/images/edits` |
| 影片 | `/v1/videos/generations` |
| 音樂 | `/v1/music/generations` |
| 3D | `/v1/3d/generations` |
| 音訊 | `/v1/audio/speech`, `/v1/audio/transcriptions`, `/v1/audio/translations` |
| 嵌入與重排序 | `/v1/embeddings`, `/v1/rerank` |
| 文字翻譯 | `/v1/translations` |

## 託管模型瀏覽器

支援 Streamable HTTP 的客戶端可以連接至：

```text
https://tokenlab-model-explorer.vercel.app/mcp
```

此託管瀏覽器為公開性質，並公開 `open_tokenlab_model_explorer`、`compare_tokenlab_models` 及 `generate_tokenlab_endpoint_example`。當您需要生成的 core/full 設定檔、本地檔案上傳或憑證授權的 API 操作時，請使用上述的本地 npm 伺服器。

## 與 TokenLab Skills 搭配使用

MCP 伺服器在執行階段非常有用，而 [TokenLab skills repository](https://github.com/hedging8563/tokenlab-skills) 則教導編碼代理程式如何生成與修復整合程式碼。

在可用時同時使用兩者：

- MCP 伺服器：探索當前模型、定價與端點詳細資訊。
- `tokenlab-api-integration` skill：生成可執行的 API 範例並處理結構化的 TokenLab 錯誤。
- `tokenlab-model-picker` skill：為使用者的任務選擇強大的模型。
- `tokenlab-native-endpoints` skill：決定何時使用 Responses、Anthropic Messages、Gemini、媒體、音訊、嵌入、重排序或翻譯路由。

## 疑難排解

<AccordionGroup>
  <Accordion title="客戶端無法啟動伺服器">
    確認 Node.js 版本至少為 18.17，然後在終端機執行一次 `npx -y @tokenlabai/mcp-server` 以顯示 npm 或網路錯誤。
  </Accordion>

  <Accordion title="模型列表為空">
    驗證機器是否能連線至 `https://api.tokenlab.sh/v1/models`。如果您覆寫了 `TOKENLAB_API_BASE`，請確保結尾沒有斜線。
  </Accordion>

  <Accordion title="代理程式仍然選擇過時的模型 ID">
    要求代理程式在硬編碼模型名稱之前呼叫 `list_models` 或 `get_model`。將 MCP 伺服器與 `tokenlab-model-picker` 搭配使用可獲得更好的結果。
  </Accordion>

  <Accordion title="此伺服器可以呼叫付費的 TokenLab 推論 API 嗎？">
    可以。請設定 `TOKENLAB_API_KEY`，然後使用生成的 LLM、媒體、音訊、任務、檔案、嵌入、重排序或翻譯工具。目錄與定價工具無需金鑰即可使用。
  </Accordion>
</AccordionGroup>

## 資源

<CardGroup cols={2}>
  <Card title="TokenLab MCP" icon="plug" href="https://tokenlab.sh/mcp">
    產品概覽，以及 Core 31 與 Full 78 工具集的連線方式
  </Card>
  <Card title="GitHub 儲存庫" icon="github" href="https://github.com/hedging8563/tokenlab-mcp-server">
    原始碼與本地設定說明
  </Card>
  <Card title="TokenLab Skills" icon="sparkles" href="https://github.com/hedging8563/tokenlab-skills">
    維護中的 TokenLab 整合代理程式技能
  </Card>
  <Card title="模型目錄 API" icon="list" href="https://api.tokenlab.sh/v1/models">
    公開模型探索端點
  </Card>
  <Card title="llms.txt" icon="file-lines" href="https://api.tokenlab.sh/llms.txt">
    代理程式可讀的 TokenLab API 概覽
  </Card>
  <Card title="Glama MCP 伺服器" icon="globe" href="https://glama.ai/mcp/servers/hedging8563/tokenlab-mcp-server">
    查看已驗證的 TokenLab MCP 伺服器列表
  </Card>
  <Card title="Glama 模型瀏覽器" icon="search" href="https://glama.ai/mcp/servers/hedging8563/tokenlab-openai-apps-model-explorer">
    探索模型、定價與原生端點範例
  </Card>
  <Card title="MCP.so 列表" icon="compass" href="https://mcp.so/servers/tokenlab-mcp-server">
    在 AI 與代理程式目錄中探索 TokenLab
  </Card>
  <Card title="託管模型瀏覽器" icon="rocket" href="https://tokenlab-model-explorer.vercel.app/widget">
    開啟公開模型瀏覽器與遠端 MCP 端點
  </Card>
</CardGroup>
