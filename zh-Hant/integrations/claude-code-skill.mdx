---
title: "✨ Claude Code 技能"
description: "5 分鐘快速入門指南 - 讓 Claude Code 為您自動整合任何 LemonData API"
---

<Note>
這項技能讓您只需用簡單的英文描述需求，即可整合 LemonData 的**數百個 AI API**。該 API 是 **Agent-First**（智能體優先）的 —— 即使 Claude 猜錯了模型名稱，錯誤回應也會準確告知其修復方法。
</Note>

## 這項技能可以做什麼？

當您想在程式碼中使用 AI 功能（如 GPT-4、圖像生成、語音辨識等）時，此技能將：

1. ✅ **直接嘗試 API** — 無需搜尋
2. ✅ **從錯誤中自我修正** — 結構化錯誤提示將引導下一次嘗試
3. ✅ **以您選擇的語言生成完整的、可執行的程式碼**
4. ✅ **根據即時價格和可用性選擇最佳模型**
5. ✅ **透過原生端點建議優化您的整合**

**簡而言之：只需描述您的需求，即可獲得可用的 API 整合程式碼 —— 即使第一次嘗試並不完美，API 也會引導 Claude 找到正確答案。**

## 步驟 1：安裝技能

<Tabs>
  <Tab title="npx (推薦)">
    最簡單的安裝方式：

    ```bash
    npx add-skill hedging8563/lemondata-api-skill -y
    ```

    這將自動為所有偵測到的編程智能體（Claude Code、Cursor、Copilot 等）安裝此技能。
  </Tab>
  <Tab title="分享 GitHub URL">
    1. 在您的終端機或 IDE 中開啟 **Claude Code**
    2. 在對話中貼上此連結：
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
    3. Claude Code 會將其識別為技能儲存庫並協助您安裝
  </Tab>
  <Tab title="Git Clone">
    **個人安裝**（適用於所有專案）：
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git ~/.claude/skills/lemondata-api-integration
    ```

    **專案特定安裝**（透過 git 與團隊共享）：
    ```bash
    git clone https://github.com/hedging8563/lemondata-api-skill.git .claude/skills/lemondata-api-integration
    ```
  </Tab>
</Tabs>

### 驗證安裝

詢問 Claude Code：
```
What skills are available?
```

如果您看到 `lemondata-api-integration`，則表示安裝成功！

## 步驟 2：取得您的 API Key

在使用此技能之前，您需要一個 LemonData API Key。

<Tip>
**您不需要提前設定 API Key！** 當您使用此技能時，Claude Code 會自動詢問您的 API Key 並將其包含在生成的程式碼中。
</Tip>

<Steps>
  <Step title="造訪 LemonData">
    前往 [lemondata.cc](https://lemondata.cc)
  </Step>
  <Step title="登入">
    建立帳號或登入
  </Step>
  <Step title="取得 API Key">
    導覽至 [Dashboard → API Keys](https://lemondata.cc/dashboard/api) 並建立一個新金鑰
  </Step>
  <Step title="複製金鑰">
    您的金鑰以 `sk-...` 開頭 - 請妥善保存
  </Step>
</Steps>

<Warning>
**重要提示：**
- 所有 LemonData API 都使用相同的 API Key
- 不要與他人分享您的金鑰
- 不要將您的金鑰提交到 GitHub
</Warning>

## 步驟 3：開始使用

使用此技能就像聊天一樣簡單！

### 範例 1：使用 GPT-4

**您說：**
```
I want to use GPT-4 in my Python project
```

**Claude Code 將：**
1. 嘗試使用最佳模型猜測直接呼叫 API
2. 如果模型名稱錯誤，從錯誤中讀取 `did_you_mean` 並自動修正
3. 使用正確的模型生成完整的 Python 程式碼

**您得到：**
```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-api-key",
    base_url="https://api.lemondata.cc/v1"
)

response = client.chat.completions.create(
    model="gpt-4o",
    messages=[{"role": "user", "content": "Hello!"}]
)

print(response.choices[0].message.content)
```

### 範例 2：生成圖像

**您說：**
```
How do I generate images with Flux in Node.js?
```

**Claude Code 將：**
- 呼叫圖像生成 API
- 生成帶有適當錯誤處理的 JavaScript 程式碼
- 包含圖像下載和儲存程式碼

### 範例 3：語音辨識

**您說：**
```
Integrate a speech-to-text API
```

**Claude Code 將：**
- 詢問您正在使用的程式語言
- 使用正確的音訊端點生成程式碼
- 提供音訊檔案處理範例

## Agent-First API 特性

LemonData 的 API 專為 AI Agent 設計。每個錯誤回應都包含結構化提示：

### 智慧錯誤恢復

| 錯誤 | API 回傳內容 | Claude 的操作 |
|-------|---------------------|-----------------|
| 模型名稱錯誤 | `did_you_mean` + `suggestions` | 自動修正並重試 |
| 餘額不足 | `balance_usd` + 更便宜的 `suggestions` | 切換到負擔得起的模型 |
| 模型不可用 | `alternatives` + `retry_after` | 切換到可用模型 |
| 頻率限制 | `retry_after` (精確秒數) | 等待並重試 |
| 上下文過長 | 帶有更大模型的 `suggestions` | 切換到更大上下文的模型 |

### 模型探索

無需搜尋文件。使用這些 API 端點：

```bash
# 讀取機器可讀的 API 概覽
curl https://lemondata.cc/llms.txt

# 按類別列出模型
curl "https://api.lemondata.cc/v1/models?category=chat" -H "Authorization: Bearer sk-KEY"
curl "https://api.lemondata.cc/v1/models?category=image" -H "Authorization: Bearer sk-KEY"

# 按能力篩選
curl "https://api.lemondata.cc/v1/models?tag=coding" -H "Authorization: Bearer sk-KEY"
```

### 原生端點優化

當 Claude 對 Claude 或 Gemini 模型使用 `/v1/chat/completions` 時，API 會回傳優化標頭：

```
X-LemonData-Hint: This model supports native Anthropic format. Use POST /v1/messages for better performance.
X-LemonData-Native-Endpoint: /v1/messages
```

Claude Code 將自動切換到原生端點以獲得更好的效能。

## 支援的功能

此技能可以幫助您整合所有這些 AI 能力：

| 功能類型 | 範例 |
|-------------|----------|
| 💬 對話 | GPT-4o, Claude, Gemini, DeepSeek |
| 🎨 圖像生成 | Midjourney, Flux, Stable Diffusion |
| 🎬 影片生成 | Sora, Runway, Kling, Luma AI |
| 🎵 音樂生成 | Suno |
| 🗿 3D 模型 | Tripo3D |
| 🎤 音訊 | Text-to-Speech, Speech-to-Text |
| 📊 嵌入 | text-embedding-3 |
| 🔄 重排序 | bce-reranker, qwen3-rerank |

## 使用技巧

### 1. 直接說明您的需求

<Tabs>
  <Tab title="❌ 不要這樣做">
    ```
    Help me check if LemonData has an image generation API
    ```
  </Tab>
  <Tab title="✅ 建議這樣做">
    ```
    I want to generate images in Python
    ```
  </Tab>
</Tabs>

### 2. 指定您的語言

如果您有特定的語言需求，直接說明即可：

```
Use JavaScript to call GPT-4
Use Python for speech recognition
Use Go to generate images
```

### 3. 描述您的場景

如果您有特定需求，請詳細描述：

```
I'm building a chatbot, need to use GPT-4
My website needs to let users upload and transform images
I'm making a voice notes app, need speech-to-text
```

## 重要注意事項

### API Key 安全性

<CardGroup cols={2}>
  <Card title="❌ 絕不要這樣做" icon="xmark" color="#ef4444">
    - 將 API Key 直接放在前端程式碼中
    - 將 API Key 提交到 GitHub
    - 與他人分享 API Key
  </Card>
  <Card title="✅ 最佳實踐" icon="check" color="#22c55e">
    - 使用環境變數
    - 對於網站，從後端呼叫 API
    - 使用 Next.js, Express 等框架
  </Card>
</CardGroup>

### 成本管理

- 每次 API 呼叫都會消耗額度
- 在您的[儀表板](https://lemondata.cc/dashboard)中設定使用限制
- 測試時使用小資料量
- 檢查 `error.balance_usd` 和 `error.estimated_cost_usd` 以程式化方式監控成本

### 錯誤處理

生成的程式碼包含 Agent-First 錯誤處理，它可以：
- 讀取結構化錯誤提示（`did_you_mean`、`suggestions`、`retry_after`）
- 從常見錯誤中自動恢復
- 在需要時回退到備選模型

## FAQ

<AccordionGroup>
  <Accordion title="技能沒有自動觸發？">
    嘗試在請求中提到 "LemonData" 或 "LemonData API"，或更具體地描述您的需求：
    ```
    Use LemonData to integrate GPT-4 in my project
    ```
  </Accordion>

  <Accordion title="API 如何幫助 Claude 找到正確的模型？">
    LemonData 的 Agent-First API 回傳結構化錯誤提示。如果 Claude 猜錯了模型名稱，錯誤回應會包含帶有正確名稱的 `did_you_mean` 以及帶有備選模型的 `suggestions`。Claude 會讀取這些內容並在下一次嘗試中自動修正。
  </Accordion>

  <Accordion title="生成的程式碼有錯誤？">
    檢查以下幾點：
    1. API Key 是否正確？
    2. 是否安裝了必要的函式庫？（例如 Python 的 `openai`）
    3. 您是否可以存取 LemonData 的伺服器？
    4. 您的 API Key 是否有足夠的額度？
  </Accordion>

  <Accordion title="如何更新技能？">
    再次將 GitHub 連結傳送給 Claude Code：
    ```
    https://github.com/hedging8563/lemondata-api-skill
    ```
  </Accordion>
</AccordionGroup>

## 資源

<CardGroup cols={2}>
  <Card title="LemonData 網站" icon="globe" href="https://lemondata.cc">
    主網站和儀表板
  </Card>
  <Card title="API 文件" icon="book" href="https://docs.lemondata.cc">
    完整的 API 參考指南
  </Card>
  <Card title="模型" icon="robot" href="https://lemondata.cc/zh-TW/models">
    瀏覽所有可用模型
  </Card>
  <Card title="llms.txt" icon="file-lines" href="https://lemondata.cc/llms.txt">
    為 AI Agent 提供的機器可讀 API 概覽
  </Card>
</CardGroup>

## 開始使用！

現在您已經知道如何使用這項技能了。嘗試詢問 Claude Code：

```
I want to use GPT-4 in my project
```

或者

```
Help me integrate image generation
```

Claude Code 將自動使用此技能為您完成所有工作 —— 如果出現任何問題，API 的結構化錯誤提示將引導它找到正確答案！

<Info>
**有任何問題嗎？** 請查看我們的 [GitHub Issues](https://github.com/hedging8563/lemondata-api-skill/issues) 或聯繫 [support@lemondata.cc](mailto:support@lemondata.cc)
</Info>
