---
title: "Vercel AI SDK"
description: "使用 OpenAI-compatible 或 Responses-native provider 將 LemonData 整合到 Vercel AI SDK"
---

## 概覽

對於 LemonData，Vercel AI SDK 中最穩定的預設選項是 **OpenAI-compatible provider**。

如果你明確需要 **Responses-native** 行為，可以切換到 OpenAI provider，並保持相同的 LemonData base URL。

## 建議的預設：OpenAI-Compatible Provider

```bash
npm install ai @ai-sdk/openai-compatible
```

```typescript
import { createOpenAICompatible } from '@ai-sdk/openai-compatible';

export const lemondata = createOpenAICompatible({
  name: 'lemondata',
  apiKey: process.env.LEMONDATA_API_KEY,
  baseURL: 'https://api.lemondata.cc/v1',
});
```

## 產生文字

```typescript
import { generateText } from 'ai';
import { lemondata } from './lemondata';

const { text } = await generateText({
  model: lemondata.chatModel('gpt-5.4'),
  prompt: 'Explain LemonData in one sentence.',
});

console.log(text);
```

## 串流文字

```typescript
import { streamText } from 'ai';
import { lemondata } from './lemondata';

const result = await streamText({
  model: lemondata.chatModel('gpt-5.4'),
  prompt: 'Write a short poem about coding.',
});

for await (const textPart of result.textStream) {
  process.stdout.write(textPart);
}
```

## 工具呼叫

```typescript
import { generateText, tool } from 'ai';
import { z } from 'zod';
import { lemondata } from './lemondata';

const result = await generateText({
  model: lemondata.chatModel('gpt-5.4'),
  prompt: 'What is the weather in San Francisco?',
  tools: {
    weather: tool({
      description: 'Get weather in a location',
      parameters: z.object({
        location: z.string(),
      }),
      execute: async ({ location }) => ({
        location,
        temperature: 72,
        condition: 'sunny',
      }),
    }),
  },
});

console.log(result.text);
```

## 結構化輸出

```typescript
import { generateObject } from 'ai';
import { z } from 'zod';
import { lemondata } from './lemondata';

const { object } = await generateObject({
  model: lemondata.chatModel('gpt-5.4'),
  schema: z.object({
    name: z.string(),
    role: z.string(),
  }),
  prompt: 'Generate a fake developer profile.',
});

console.log(object);
```

## 如果你明確需要 Responses-Native 行為

```bash
npm install ai @ai-sdk/openai
```

```typescript
import { createOpenAI } from '@ai-sdk/openai';

export const lemondataResponses = createOpenAI({
  apiKey: process.env.LEMONDATA_API_KEY,
  baseURL: 'https://api.lemondata.cc/v1',
});
```

```typescript
import { generateText } from 'ai';
import { lemondataResponses } from './lemondata-responses';

const { text } = await generateText({
  model: lemondataResponses('gpt-5.4'),
  prompt: 'Explain LemonData in one sentence.',
});
```

<Note>
將 `@ai-sdk/openai-compatible` 作為 proxy 風格整合的安全預設選項。只有在你明確希望使用基於 `/v1/responses` 建立的 provider 路徑時，才切換到 `@ai-sdk/openai`。
</Note>

## 環境變數

```bash
# .env.local
LEMONDATA_API_KEY=sk-your-lemondata-key
```

## 最佳實踐

<AccordionGroup>
  <Accordion title="優先使用 openai-compatible">
    對於第三方 gateway 和 proxy 後端，`@ai-sdk/openai-compatible` 通常是最不容易出錯的起點。
  </Accordion>

  <Accordion title="僅在需要時改用 Responses">
    如果你需要與 `/v1/responses` 綁定的 provider 行為，請有意識地切換 provider 套件，而不是在同一個 client 中混用兩種模式。
  </Accordion>

  <Accordion title="將密鑰保留在伺服器端">
    絕不要在 client-side 程式碼中暴露你的 LemonData API key。請將 provider 設定放在伺服器檔案或 API 路由中。
  </Accordion>
</AccordionGroup>