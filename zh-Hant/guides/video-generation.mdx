---
title: "影片生成"
description: "透過 11 家供應商的模型生成影片 — 文字轉影片、圖片轉影片、關鍵影格控制、影片延展與編輯"
---

## 概覽

LemonData 透過單一統一 API 提供來自 11 家供應商的影片生成模型存取。影片生成為**非同步**流程 — 你提交請求後，會收到 task ID 與 `poll_url`，然後再輪詢取得結果。

<Info>
模型清單會頻繁更新。若要查看最新可用模型與價格，請造訪 [Models page](https://lemondata.cc/zh-Hant/models) 或使用 [Models API](/api-reference/models/list-models)。
</Info>

<Warning>
音訊行為取決於具體模型。Veo 3 家族預設有聲；部分模型預設無聲，或僅支援無聲。對於 passthrough 治理的模型，可顯式傳 `output_audio=true`，由上游盡力判斷是否生成音訊。
</Warning>


### 非同步工作流程

```mermaid
sequenceDiagram
    participant Client
    participant LemonData
    participant Provider

    Client->>LemonData: POST /v1/videos/generations
    LemonData->>Provider: Forward request
    Provider-->>LemonData: task_id + pending
    LemonData-->>Client: { task_id, poll_url, status: "pending" }

    loop Poll every 5-10s
        Client->>LemonData: GET poll_url
        LemonData->>Provider: Query status
        Provider-->>LemonData: Status update
        LemonData-->>Client: { status, video_url? }
    end
```

<CodeGroup>

```python Python
import requests
import time

API_KEY = "sk-your-api-key"
BASE = "https://api.lemondata.cc/v1"
headers = {"Authorization": f"Bearer {API_KEY}"}

# Step 1: Submit generation request
resp = requests.post(f"{BASE}/videos/generations",
    headers=headers,
    json={
        "model": "sora-2",
        "prompt": "A calm cinematic shot of a cat walking through a sunlit garden.",
        "operation": "text-to-video",
        "duration": 4,
        "aspect_ratio": "16:9"
    }
)
create_data = resp.json()
task_id = create_data["task_id"]
poll_url = create_data["poll_url"]

# Step 2: Poll for result
while True:
    status = requests.get(f"https://api.lemondata.cc{poll_url}", headers=headers).json()
    if status["status"] in ("completed", "succeeded"):
        print(f"Video URL: {status['video_url']}")
        break
    elif status["status"] == "failed":
        print(f"Failed: {status.get('error')}")
        break
    time.sleep(10)
```

```javascript JavaScript
const API_KEY = 'sk-your-api-key';
const BASE = 'https://api.lemondata.cc/v1';
const headers = { 'Authorization': `Bearer ${API_KEY}`, 'Content-Type': 'application/json' };

// Step 1: Submit
const { task_id, poll_url } = await fetch(`${BASE}/videos/generations`, {
  method: 'POST', headers,
  body: JSON.stringify({
    model: 'sora-2',
    prompt: 'A calm cinematic shot of a cat walking through a sunlit garden.',
    operation: 'text-to-video',
    duration: 4,
    aspect_ratio: '16:9'
  })
}).then(r => r.json());

// Step 2: Poll
const poll = setInterval(async () => {
  const status = await fetch(`https://api.lemondata.cc${poll_url}`, { headers }).then(r => r.json());
  if (['completed', 'succeeded'].includes(status.status)) {
    console.log('Video URL:', status.video_url);
    clearInterval(poll);
  } else if (status.status === 'failed') {
    console.error('Failed:', status.error);
    clearInterval(poll);
  }
}, 10000);
```

</CodeGroup>

## 模型能力矩陣

不同模型各自擅長不同任務。請使用此矩陣為你的使用情境選擇合適的模型。

**圖例**：✅ 支援 | ❌ 不支援

| Series | Provider | T2V | I2V | Keyframe | Extension | Editing | Max Duration |
|--------|----------|-----|-----|----------|-----------|---------|-------------|
| **Sora** | OpenAI | ✅ | ❌ | ❌ | ❌ | ❌ | ~20s |
| **Kling** | Kuaishou | ✅ | ✅ | ❌ | ✅ | ❌ | 10s |
| **Veo** | Google | ✅ | ✅ | ✅ | ❌ | ❌ | 8s |
| **Seedance** | ByteDance | ✅ | ✅ | ✅ | ✅ | ✅ | 10s |
| **Hailuo** | MiniMax | ✅ | ✅ | ❌ | ❌ | ❌ | 10s |
| **Wan** | Alibaba | ✅ | ✅ | ❌ | ❌ | ❌ | 5s |
| **Runway** | Runway | ❌ | ✅ | ❌ | ❌ | ❌ | 10s |
| **Luma** | Luma | ✅ | ❌ | ❌ | ✅ | ❌ | 5s |
| **Vidu** | Vidu | ✅ | ✅ | ✅ | ❌ | ❌ | 8s |
| **Grok** | xAI | ✅ | ❌ | ❌ | ❌ | ❌ | ~15s |
| **Higgsfield** | Higgsfield | ✅ | ❌ | ❌ | ❌ | ❌ | ~5s |

### 能力定義

- **T2V (Text-to-Video)**：根據文字 prompt 生成影片
- **I2V (Image-to-Video)**：將靜態圖片製作成動畫影片。為了獲得最廣泛的相容性，請提供公開的 `image_url`。
- **Keyframe**：使用 `start_image` + `end_image` 控制起始與結束影格
- **Extension**：延長現有影片的時長
- **Editing**：修改現有影片的特定方面

## 各系列可用模型

### Sora (OpenAI)

| Model | Quality | Notes |
|-------|---------|-------|
| `sora-2` | 標準 | 預設範例模型 |
| `sora-2-pro` | 高 | 畫質更高，生成時間更長 |
| `sora-2-characters` | 標準 | 以角色為重點的生成 |

### Kling (Kuaishou)

| Model | Capability | Notes |
|-------|-----------|-------|
| `kling-v2.6-pro` | T2V | 最新一代，專業品質 |
| `kling-v2.6-std` | T2V | 最新一代，速度快 |
| `kling-v2.5-turbo-std` | T2V | Turbo 級別 |
| `kling-v2.5-turbo-pro` | T2V | Turbo 速度，pro 品質 |
| `kling-v2.1-master` | T2V/I2V | Master 品質 |
| `kling-v2.1-pro` | T2V/I2V | 專業品質 |
| `kling-v2.1-standard` | T2V/I2V | 標準品質 |
| `kling-video` | T2V/I2V | 基礎模型 |
| `kling-video-extend` | Extension | 延展現有影片 |
| `kling-video-o1-pro` | T2V | O1 推理，pro 品質 |
| `kling-video-o1-std` | T2V | O1 推理，標準 |
| `kling-effects` | Effects | 套用視覺效果 |
| `kling-omni-video` | T2V | Omni 模型 |
| `kling-3.0-motion-control` | Motion Control | 動作控制生成 |

### Veo (Google)

| Model | Quality | Notes |
|-------|---------|-------|
| `veo2` | 標準 | Veo 2 系列模型 |
| `veo3.1` | 標準 | Google 最新影片模型 |
| `veo3.1-pro` | 高 | 專業品質 |
| `veo3.1-4k` | Ultra | 4K 解析度輸出 |
| `veo3.1-fast` | 快速 | 生成更快 |
| `veo3.1-fast-4k` | Fast + 4K | 快速生成並輸出 4K |
| `veo3.1-components` | 標準 | 基於元件的生成 |
| `veo3` | 標準 | 前一代 |
| `veo3-pro` | 高 | 前一代，專業版 |
| `veo3-fast` | 快速 | 前一代，快速版 |

### Seedance (ByteDance)

| Model | Capability | Notes |
|-------|-----------|-------|
| `seedance-2-0` | T2V/I2V/Keyframe/Extension/Editing | 最新、能力最完整 |
| `seedance-1-5-pro` | T2V/I2V | 前一代，pro 品質 |
| `seedance-1-0-pro` | T2V/I2V | 第一代，pro |
| `seedance-1-0-pro-fast` | T2V/I2V | 第一代，快速版 |
| `seedance-1-0-lite-t2v` | T2V | 輕量級文字轉影片 |
| `seedance-1-0-lite-i2v` | I2V | 輕量級圖片轉影片 |

<Note>
Seedance 2.0 支援最廣泛的能力範圍，包括 multimodal-to-video、影片延展與影片編輯 — 全部都透過同一個 API endpoint 完成。
</Note>

### Hailuo (MiniMax)

| Model | Quality | Notes |
|-------|---------|-------|
| `hailuo-2.3` | 標準 | 品質良好 |
| `hailuo-2.3-pro` | 高 | 輸出品質更高 |
| `hailuo-2.3-fast` | 快速 | 生成更快 |
| `hailuo-2.3-standard` | 標準 | 標準級別 |
| `video-01` | 標準 | MiniMax video-01 |
| `video-01-live` | 標準 | 即時風格生成 |

### Wan (Alibaba)

| Model | Capability | Notes |
|-------|-----------|-------|
| `wan-2.6` | T2V | 最新文字轉影片 |
| `wan2.6-i2v` | I2V | 最新圖片轉影片 |
| `wan-2.5` | T2V | 前一代 |
| `wan2.5-i2v-preview` | I2V | 前一代 I2V |
| `wan-2.2-plus` | T2V | 更早期版本 |
| `vace-14b` | T2V | VACE 架構 |

### Runway

| Model | Duration | Notes |
|-------|----------|-------|
| `runwayml-gen4-turbo-5` | 5s | 快速生成 |
| `runwayml-gen4-turbo-10` | 10s | 更長的片段 |

### Luma

| Model | Capability | Notes |
|-------|-----------|-------|
| `luma-video-api` | T2V | 文字轉影片 |
| `luma-video-extend-api` | Extension | 延展現有影片 |

### Vidu (Shengshu)

| Model | Quality | Notes |
|-------|---------|-------|
| `viduq3-turbo` | 快速 | Turbo 級別 |
| `viduq3-pro` | 高 | 最新一代 |
| `viduq2-pro` | 高 | 前一代，pro |
| `viduq2-pro-fast` | 快速 | 前一代，快速 pro |
| `viduq2` | 標準 | 前一代，標準版 |
| `viduq2-turbo` | 快速 | Turbo 速度 |
| `vidu2.0` | 標準 | 基礎模型 |

### Grok (xAI)

| Model | Notes |
|-------|-------|
| `grok-video-3` | xAI 的影片生成模型 |
| `grok-video-3-10s` | 10 秒版本 |

### Higgsfield

| Model | Notes |
|-------|-------|
| `higgsfield-turbo` | 最快、成本更低 |
| `higgsfield-standard` | 標準品質 |
| `higgsfield-lite` | 輕量版 |

## 使用範例

### Text-to-Video (T2V)

最常見的使用情境。所有模型都支援此功能。

```python
response = requests.post(f"{BASE}/videos/generations",
    headers=headers,
    json={
        "model": "sora-2",
        "prompt": "A calm cinematic shot of a cat walking through a sunlit garden.",
        "operation": "text-to-video",
        "duration": 4,
        "aspect_ratio": "16:9"
    }
)
```

### Image-to-Video (I2V)

將靜態圖片製作成動畫影片。為了在各種影片模型之間獲得最廣泛的相容性，請託管來源圖片並傳入 `image_url`。

```python
# Using image URL
response = requests.post(f"{BASE}/videos/generations",
    headers=headers,
    json={
        "model": "hailuo-2.3-fast",
        "prompt": "The scene begins from the provided image and adds gentle natural motion.",
        "operation": "image-to-video",
        "image_url": "https://example.com/portrait.jpg",
        "duration": 6,
        "aspect_ratio": "16:9"
    }
)

```

### Keyframe Control (Start + End Image)

同時控制第一幀與最後一幀，以實現精確轉場。目前由 Seedance 2.0 支援。

```python
response = requests.post(f"{BASE}/videos/generations",
    headers=headers,
    json={
        "model": "viduq2-pro",
        "prompt": "Smooth transition from day to night, city lights gradually turning on",
        "operation": "start-end-to-video",
        "start_image": "https://example.com/city-day.jpg",
        "end_image": "https://example.com/city-night.jpg",
        "duration": 5,
        "resolution": "720p",
        "aspect_ratio": "16:9"
    }
)
```

### 影片延展

延長現有影片的時長。請使用具備延展能力的模型。

```python
response = requests.post(f"{BASE}/videos/generations",
    headers=headers,
    json={
        "model": "kling-video-extend",
        "prompt": "Continue the scene naturally",
        "image_url": "https://example.com/last-frame.jpg"
    }
)
```

## 參數說明

| Parameter | Type | Description |
|-----------|------|-------------|
| `model` | string | 模型 ID（預設：`sora-2`） |
| `prompt` | string | **必填。** 影片的文字描述 |
| `image_url` | string | 起始圖片的 URL（用於 I2V） |
| `image` | string | 以 data URL 形式傳入的內嵌圖片。相容模型支援此方式，但 `image_url` 具有最廣泛的相容性。 |
| `duration` | integer | 影片時長（秒）（1-60，視模型而定） |
| `aspect_ratio` | string | `16:9`、`9:16`、`1:1` 等 |
| `resolution` | string | `1080p`、`720p`、`4k` |
| `fps` | integer | 每秒影格數（1-120） |
| `negative_prompt` | string | 生成時要避免的內容 |
| `seed` | integer | 用於重現結果的隨機 seed |
| `cfg_scale` | number | 引導強度（0-20） |
| `motion_strength` | number | 動作強度（0-1） |
| `start_image` | string | 起始關鍵影格的 URL |
| `end_image` | string | 結束關鍵影格的 URL |

<Warning>
並非每個模型都支援所有參數。不支援的參數會被靜默忽略。請查看模型文件以確認支援的參數。
</Warning>

## 模型選擇指南

<CardGroup cols={2}>
  <Card title="最佳品質" icon="crown">
    **Seedance 2.0** 或 **Kling v2.6 Pro** — 電影級品質、細節豐富、動作自然
  </Card>
  <Card title="最快生成" icon="bolt">
    **Higgsfield Turbo** 或 **Hailuo 2.3** — 適合原型設計與反覆迭代的快速結果
  </Card>
  <Card title="最全能" icon="wand-magic-sparkles">
    **Seedance 2.0** — 單一模型即支援 T2V、I2V、keyframe、extension 與 editing
  </Card>
  <Card title="最佳性價比" icon="coins">
    **Wan 2.6** 或 **Hailuo 2.3** — 以較低的單次生成成本提供具競爭力的品質
  </Card>
</CardGroup>

## 計費

影片生成採用**固定單次生成計費**。任務提交時會先收費一次，與影片時長無關。若生成失敗，費用將自動退還。

請在 [Models page](https://lemondata.cc/zh-Hant/models) 查看目前價格，或透過 [Pricing API](/api-reference/pricing/get-pricing) 查詢。