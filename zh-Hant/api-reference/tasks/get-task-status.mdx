---
title: "取得任務狀態"
description: "使用 create 端點回傳的 poll_url 來擷取任何非同步任務的狀態與結果"
---

## 概覽

使用此端點可對影片、圖片、音樂與 3D 生成等任務類型進行統一的非同步輪詢。

<Info>
如果 create 回應包含 `poll_url`，請呼叫該確切路徑。對於較新的非同步工作流程，`poll_url` 可能會指向 `/v1/tasks/{taskId}`。
</Info>

## 路徑參數

<ParamField path="taskId" type="string" required>
  create 請求回傳的任務 ID。
</ParamField>

## 回應

<ResponseField name="id" type="string">
  任務識別碼。
</ResponseField>

<ResponseField name="status" type="string">
  任務狀態，例如 `pending`、`processing`、`completed` 或 `failed`。
</ResponseField>

<ResponseField name="video_url" type="string">
  當任務完成並產生影片時的結果資產 URL。
</ResponseField>

<ResponseField name="error" type="string">
  當任務失敗時的錯誤訊息。
</ResponseField>

## 範例

```bash cURL
curl "https://api.lemondata.cc/v1/tasks/video_abc123" \
  -H "Authorization: Bearer sk-your-api-key"
```

```python Python
import requests

poll_url = "/v1/tasks/video_abc123"
response = requests.get(
    f"https://api.lemondata.cc{poll_url}",
    headers={"Authorization": "Bearer sk-your-api-key"},
)
print(response.json())
```

```javascript JavaScript
const pollUrl = '/v1/tasks/video_abc123';
const response = await fetch(`https://api.lemondata.cc${pollUrl}`, {
  headers: { Authorization: 'Bearer sk-your-api-key' },
});
console.log(await response.json());
```