---
title: "查詢素材詳情"
openapi: "GET /v1/videos/assets/{assetId}"
description: "透過 TokenLab 素材 ID 查詢一個 Seedance 素材。"
---

查询一个素材并查看处理状态。只有 `ready` 为 `true` 且 `status` 为 `ACTIVE` 时，才可以用于视频生成。

如果生成视频时返回 `Seedance material asset not found or not accessible`，请确认传入的是这个素材 `id`，不是素材组 `group_id`。
## 用例請求

```bash
curl https://api.tokenlab.sh/v1/videos/assets/seedance_asset_123 \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```

## 用例響應

```json
{
  "id": "seedance_asset_123",
  "object": "seedance.material_asset",
  "group_id": "seedance_group_virtual_123",
  "library_type": "aigc_avatar",
  "asset_name": "Front view",
  "asset_type": "Image",
  "asset_url": "https://example.com/reference.png",
  "cover_url": null,
  "status": "ACTIVE",
  "ready": true,
  "error_message": null,
  "metadata": null,
  "created_at": 1782998400000
}
```
