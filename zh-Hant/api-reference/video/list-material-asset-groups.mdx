---
title: "列出素材資源群組"
openapi: "GET /v1/videos/assets/groups"
description: "列出當前組織擁有的 Seedance 素材群組。"
---

列出經身份驗證的組織所擁有的素材群組。透過 `library_type` 和 `status` 進行篩選，以區分虛擬人像（virtual-avatar）群組與真人（real-person）群組。

上傳素材時請使用回傳的 `group_id`。在影片生成過程中，請勿將 `group_id` 作為 `material_asset_id` 傳遞；生成作業需要的是素材資源的 `id`。

## 請求範例

```bash
curl "https://api.tokenlab.sh/v1/videos/assets/groups?library_type=aigc_avatar&page_size=20" \
  -H "Authorization: Bearer $TOKENLAB_API_KEY"
```

## 回應範例

```json
{
  "groups": [
    {
      "id": "group-20260720123456-vrt01",
      "object": "seedance.material_asset_group",
      "library_type": "aigc_avatar",
      "group_name": "Product references",
      "status": "ACTIVE",
      "created_at": 1782998400000
    }
  ],
  "next_page_token": null
}
```
